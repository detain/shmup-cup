# Generated / vendored
node_modules/
dist/
coverage/
.turbo/
assets/generated/
pnpm-lock.yaml
test-results/
playwright-report/

# Hand-laid pixel art (sprite rows / font glyphs one per line — Prettier would fold short
# row arrays onto one line and destroy the picture)
assets/source/

# Standalone tool projects own their formatting
tools/

# Markdown keeps its hand-made layout (Prettier would re-pad every table, causing noisy
# diffs and merge conflicts between docs authors)
*.md
/LICENSE

# Hand-formatted XML (Tizen widget config)
*.xml

# Golden replays: written by `pnpm golden:update` (JSON.stringify layout — re-bless, never hand-edit)
test/golden/*.replay.json

# Attract demos: written by `pnpm golden:update` like the golden replays (M2-15)
content/demos/*.replay.json
