# Caliber Learnings

Accumulated patterns and anti-patterns from development sessions.
Auto-managed by [caliber](https://github.com/caliber-ai-org/ai-setup) — do not edit manually.

- **[convention]** Adding a pilot to `test/golden/golden.ts` is three edits: widen the `GoldenScenario.bot` union, export the bot factory, and add its branch to `pilotOf` — a missing `pilotOf` branch silently falls through to the 4-way bot and records a misleading golden.
- **[pattern]** To assert a new golden actually exercised a mechanic, thread the `observe` callback of `playGolden(replay, undefined, (w) => …)` and count World state per tick; asserting only on the recorded `expected` block (status/ticks/score) cannot tell a run that used the feature from one that never triggered it.
- **[pattern]** Inspect a recorded golden without decoding it in TS: `python3 -c "import json;d=json.load(open('test/golden/<name>.replay.json'));print(d['expected'])"` — the `expected` block (status, ticks, score, lives, deathTicks, bossDefeated) shows at a glance whether the run was long enough and cleared the boss, and comparing it against a sibling golden's tick count is the quickest sanity check.
- **[gotcha]** `CLAUDE.md` / `CALIBER_LEARNINGS.md` at the repo root are regenerated by Caliber, and whether they are *tracked* has changed over time — a stray `updates` commit added them to HEAD, after which `git status` showed a staged `D CLAUDE.md` (Caliber's rewrite looked like a deletion) instead of the usual `??`. Check `git ls-files -- CLAUDE.md CALIBER_LEARNINGS.md` before assuming; either way keep both out of a feature commit's staged set (`git reset -q -- CLAUDE.md CALIBER_LEARNINGS.md`) unless the commit is specifically a Caliber sync.
- **[pattern]** Prove a regression test actually regresses: `git stash push -- <changed src files> && pnpm --filter @shmup/core exec vitest run <test paths>; git stash pop` — stashing only the `src/` files leaves the new tests in place, so they must fail. Note `pnpm --filter … exec vitest` exits non-zero (`ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL`), so chain the `git stash pop` with `;` not `&&`. For a one-line fix, the lighter variant is `cp <file> /tmp/x.bak`, `sed -i '<line>s/.*/  \/\/ removed/' <file>`, run the tests, `cp` back, then confirm with `git diff --stat <file>` printing nothing.
- **[fix]** Wrapping a wait in a *background Bash* task (`until grep -q EXIT= log; do sleep 5; done`) does not help — that task's own output file is also empty until it exits, so you end up polling the poller (this session burned ~10 turns that way before switching). Go straight to `Monitor` with `tail -f -n 0 "$LOG" | grep -E --line-buffered "<done marker>|✘|failed|Error:"` and a generous `timeout_ms`, and redirect the real command's output to a log file you can `tail` directly meanwhile.
- **[convention]** Adding a pilot to `test/golden/golden.ts` is three edits: widen the `GoldenScenario.bot` union, export the bot factory, and add its branch to `pilotOf` — a missing `pilotOf` branch silently falls through to the 4-way bot and records a misleading golden. Keep the `bot` field's own TSDoc listing every member of the union; the file docblock alone is not enough.
- **[gotcha]** A new `test/golden/*.replay.json` is picked up automatically by `test/e2e/determinism.spec.ts`, which replays every golden **and** every `content/demos/*.replay.json` in the browser — so a long golden (11 k ticks ≈ 2.4 s per engine) also lengthens `pnpm test:e2e`, and any non-determinism shows up there rather than in `pnpm test`. Grep the e2e log for the new file's name to confirm it replayed in sync.
- **[gotcha]** Vitest's asymmetric matchers fail this repo's type-aware lint in tests: `expect(issues).toEqual([expect.objectContaining({ path: expect.stringContaining('x'), … })])` trips `@typescript-eslint/no-unsafe-assignment` ("Unsafe assignment of an `any`/error typed value"). Write the assertion positionally instead — `expect(issues).toHaveLength(1); expect(issues[0].message).toBe(…); expect(issues[0].path).toContain(…)`.
- **[gotcha]** `while (page.menu.focus !== SomeItem.Crt) press(Down);` makes TS narrow `focus` to a literal, so a later `expect(page.menu.focus).toBe(SomeItem.Aspect)` fails typecheck with `TS2367: This comparison appears to be unintentional because the types '6' and '8' have no overlap` — even though the loop mutates it. Read the field through a tiny accessor (`const focus = (): number => page.menu.focus;` with its own docblock) so nothing narrows. This bites any menu/scene test that drives a focus cursor in a loop.
- **[gotcha]** SoA pool field names come from the `*_SCHEMA` constant, not from the system's prose — `bullets.points.fields` has `player` (the credited slot), not `owner`, and a wrong name reads back `undefined` with a `Cannot read properties of undefined` at the assertion. Grep the `..._SCHEMA = Object.freeze({` block in the module before naming a field in a test.
- **[gotcha]** Passing a non-JS path to ESLint fails the gate: `npx eslint --max-warnings=0 test/golden shmup_plan.md` reports `File ignored because no matching configuration was supplied` as a *warning*, which `--max-warnings=0` turns into a failure. Lint only source dirs; check Markdown with `npx prettier --check <file>` instead. The root-level invocation is `npx eslint --max-warnings=0 --ignore-pattern "packages/**" --ignore-pattern "apps/**" <paths>` (mirrors the `lint:root` script).
- **[gotcha]** `pnpm test` / `pnpm build` output is huge — pipe through `grep -E "Test Files|Tests |FAIL"` and `grep -E "Tizen bundle OK|Tasks:|ERROR"` respectively, and give the Bash call a long timeout (the full suite and e2e each run minutes; `pnpm test:e2e` ≈ 3.5 min for 259 specs). `pnpm test`'s unfiltered tail is only Vitest's per-project "Transform … took Ns" advisory, never the pass/fail line — always grep for `Test Files`.
- **[gotcha]** `pnpm format:check && pnpm lint && pnpm typecheck` chained in one Bash call exceeds the 120 s foreground timeout and only the first command's output survives. Run them as separate background tasks, or check just the changed files with `pnpm exec prettier --check <paths>` while the full gates run. Rough budget when planning the gate: `pnpm lint` ≈ 75 s (9 turbo tasks, uncached), `pnpm typecheck` ≈ 26 s, `pnpm build` ≈ 18 s, `pnpm test` minutes, `pnpm test:e2e` ≈ 3.5 min.
- **[gotcha]** Passing a non-JS path to ESLint fails the gate: `npx eslint --max-warnings=0 test/golden shmup_plan.md` reports `File ignored because no matching configuration was supplied` as a *warning*, which `--max-warnings=0` turns into a failure. Lint only source dirs; check Markdown with `npx prettier --check <file>` instead. The root-level invocation is `npx eslint --max-warnings=0 --ignore-pattern "packages/**" --ignore-pattern "apps/**" <paths>`.
- **[pattern]** Edit long prose lines in Markdown/README files with a `python3 - <<'PY'` heredoc that asserts the old substring exists before replacing (`assert old in s; s = s.replace(old, new, 1)`) — the anchors in these docs are 100-column wrapped sentences that are hard to match exactly with Edit, and the `assert` turns a silent no-op into a visible failure.
- **[pattern]** After adding a doc page, verify its cross-links mechanically rather than by eye: a short `python3` pass over `\]\(([^)#\s]+)(#[^)]*)?\)` in the new/edited files, resolving each relative target with `os.path.normpath` and checking existence, plus a second pass comparing `#anchor` fragments against the target file's GitHub-slugged `#` headings. Nothing in `pnpm test` checks doc links generally — only `test/integration/e2e-docs.test.ts` pins the `pnpm test:e2e` instructions to the Playwright config and `ci.yml`.
- **[convention]** A new feature's dev/test entry point must be added in *two* places in `apps/web/README.md`: the prose "Since M<x>-<nn> `?stage=…`" paragraph and the one-line `?scene=`/`?stage=` list under the `pnpm dev` code block — the list is easy to miss and drifts silently.
- **[convention]** `shmup_progress.md`'s per-step row records the suite's test count; when a later verification run shows a different total than the agent that wrote the row, amend the row to the new figure and note the earlier one in parentheses rather than silently overwriting it.
- **[gotcha]** `pnpm test` / `pnpm build` output is huge — pipe through `grep -E "Test Files|Tests |FAIL"` and `grep -E "Tizen bundle OK|Tasks:|ERROR"` respectively, and give the Bash call a long timeout (the full suite and e2e each run minutes; `pnpm test:e2e` ≈ 3.5 min for ~259 specs). `pnpm test`'s unfiltered tail is only Vitest's per-project "Transform … took Ns" advisory, never the pass/fail line — always grep for `Test Files`.
- **[pattern]** The whole gate does fit in one Bash call when you pass an explicit long `timeout` (e.g. 1_800_000) and tail/grep each stage: `pnpm format:check 2>&1 | tail -2 && pnpm lint 2>&1 | tail -2 && pnpm typecheck 2>&1 | tail -2 && pnpm test 2>&1 | grep -E "Test Files|Tests  " && pnpm build 2>&1 | tail -2` — ≈ 4–5 min end to end (lint ≈ 75 s uncached, typecheck ≈ 26 s, build ≈ 5–18 s). Without an explicit timeout the chain dies at the 120 s default and only the first command's output survives.
- **[pattern]** Iterate on one Playwright spec without the rebuild: `npx playwright test --config test/e2e/playwright.config.ts options.spec.ts` (≈ 25 s) — `pnpm test:e2e` first runs `turbo run build:test` for web + tizen and then all specs (≈ 3.5 min). Skip it while the test builds in `dist/` are current, then do one full `pnpm test:e2e` at the end.
- **[gotcha]** The e2e specs hard-code shipped content ids (`test/e2e/options.spec.ts` asserts the saved `options.input.profileId` string), and `pnpm test` never runs them. After retiring or renaming an id in `content/`, grep `test/e2e/` for the old string — otherwise the only signal is a single failure 3.5 minutes into `pnpm test:e2e`.
- **[pattern]** Re-check the Tizen size budgets without a rebuild: `cd apps/tizen && node scripts/check-bundle.mjs` reads the existing `dist/` and prints `Tizen bundle OK: app.js … KB (… gzip)`. Run it straight after a root `pnpm build` instead of paying for `pnpm --filter @shmup/tizen build` again.
- **[convention]** A plan step's outcome is recorded as an `- **As built:**` block appended directly after that step's `- **Refs:**` line in `shmup_plan.md`, with one nested bullet per area (profiles, loop, shell option, tests…), and it explicitly records what was *left out* and why, plus anything still manual (hardware / account checks). Later review fixes get their own nested bullet under that list (e.g. `- **Review fixes (round 1).** …`), naming the symptom, the call site that was missing, and whether any golden hash moved — do not rewrite the original "As built" prose.
- **[fix]** Wrapping a wait in a *background Bash* task (`until grep -q EXIT= log; do sleep 5; done`) does not help — that task's own output file is also empty until it exits, so you end up polling the poller. Go straight to `Monitor` with `tail -f -n 0 "$LOG" | grep -E --line-buffered "<done marker>|✘|failed|Error:"` and a generous `timeout_ms`, and redirect the real command's output to a log file you can `tail` directly meanwhile.
- **[gotcha]** Vitest's asymmetric matchers fail this repo's type-aware lint in tests: `expect(issues).toEqual([expect.objectContaining({ path: expect.stringContaining('x'), … })])` trips `@typescript-eslint/no-unsafe-assignment`. Write the assertion positionally instead — `expect(issues).toHaveLength(1); expect(issues[0].message).toBe(…)` — or compare against a plain `[{ path, message }]` literal.
- **[gotcha]** SoA pool field names come from the `*_SCHEMA` constant, not from the system's prose — `bullets.points.fields` has `player` (the credited slot), not `owner`, and a wrong name reads back `undefined`. Grep the `..._SCHEMA = Object.freeze({` block in the module before naming a field in a test.
- **[pattern]** Edit long prose lines in Markdown/README/plan files with a `python3 - <<'PY'` heredoc that asserts the old substring exists before replacing (`assert old in s; s = s.replace(old, new, 1)`) — the anchors in these docs are 100-column wrapped sentences that are hard to match exactly with Edit, and the `assert` turns a silent no-op into a visible failure. Follow the edit with `npx prettier --write <files>` (Markdown at the repo root *is* Prettier-checked by `pnpm format:check`, even though `docs/` tables are hand-kept).
- **[pattern]** After adding a doc page, verify its cross-links mechanically rather than by eye: a short `python3` pass over `\]\(([^)#\s]+)(#[^)]*)?\)` in the new/edited files, resolving each relative target with `os.path.normpath` and checking existence, plus a second pass comparing `#anchor` fragments against the target file's GitHub-slugged `#` headings. Nothing in `pnpm test` checks doc links generally.
