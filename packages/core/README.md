# @shmup/core

The platform-agnostic heart of Shmup Cup: the deterministic fixed-step simulation, every
game system, and the `Platform` contract the host apps implement.

**Hard rule:** pure TypeScript — no DOM, WebGL, Web Audio, Node or platform APIs
(`tizen.*`, `webapis.*`, `electron`), no key codes, no clocks, no `Math.random`, and no
engine-dependent maths (`Math.sin`/`cos`/`atan2`/`pow`/… or `**` — use the committed tables
in `math`). Enforced by `tsconfig.json` (`lib: ["ES2018"]`, no `types`) and ESLint
(`no-restricted-globals` / `no-restricted-imports` / `no-restricted-properties` /
`no-restricted-syntax` for `packages/core/src`).

## Implemented now

| Export | Module | What |
|---|---|---|
| `Platform` + parts, `createHeadlessPlatform`, `createMemoryStorage` | `platform` | Host contract (`shmup_tech.md` §3.2) and a Node/test implementation |
| `Action`, `InputSnapshot`, `PlayerInput`, `commitPlayerInput`, `InputContext`, `INPUT_CONTEXTS`, … | `input` | Action bitmasks + per-tick snapshots with edge latching; the `game` / `menu` binding context (D15) the input adapters resolve keys with |
| `GameConfig`, `DEFAULT_GAME_CONFIG`, `resolveGameConfig`, `DIFFICULTY_PRESETS`, `DifficultyRules`, `DifficultyTable`, `DEFAULT_DIFFICULTY_TABLE`, `difficultyOverrides`, `withDifficulty`, `DEATH_PENALTY_PRESETS`, `StartingLoadout`, `StageSkip`, `MeterSlotName`, `METER_SLOT_NAMES`, `DEFAULT_AUTO_POWER_UP_ORDER`, `MegaChoice`, `MEGA_CHOICES`, `ShieldChoice`, `SHIELD_CHOICES`, `OptionChoice`, `OPTION_CHOICES`, `WeaponEdit`, `ArsenalChoice`, `withArsenal`, `arsenalMatches`, `ShipChoice`, `withShip`, `shipMatches`, `POWER_UP_MODES`, `DEFAULT_SHIP_ID`, `HUD_BAR_HEIGHT`, `PLAYFIELD_Y/W/H`, `UserOptions`, `DEFAULT_USER_OPTIONS`, `resolveUserOptions`, `volumeGain`, `VOLUME_LEVELS`, `InputProfileChoice`, `BULLET_PALETTES`, `BulletPalette` | `config` (partial) | Sim-affecting session options (384×216, 60 Hz, remote-first defaults, `aimDirections` 32 for aimed enemy shots, `autofireInterval` 4 / `missileInterval` 10 and the starting `loadout` for the player weapons, the debug `stageSkip` (`'boss'` starts a stage just before its boss — M1-18), the power meter's `autoPowerUp` / `autoPowerUpOrder` / `pickupMagnet`; `powerUpMode` `'meter'` — `'direct'` accepted since M2-05); since M2-01 the **difficulty presets** Easy / Normal / Hard / Arcade (`DEFAULT_DIFFICULTY_TABLE` = `content/rules/difficulty.rules.json`: `rankBase`, `rankGrowth`, lives, extends, `continues`, death penalty, aim directions, `bulletSpeedMul` — `resolveGameConfig(overrides, table)` fills the chosen preset under explicit overrides, `withDifficulty` switches presets; the death penalty is validated) and the D20 screen layout (8-px HUD bars around a 384×200 playfield); since M1-17 the presentation-only `UserOptions` (volume levels 0–10, the input profile id — never in `GameConfig` or replays), `DEFAULT_USER_OPTIONS`, `resolveUserOptions` (defensive), `volumeGain` (`(level / 10)²`), `InputProfileChoice`; since M2-02 the first display option, `display.bulletPalette` (`BULLET_PALETTES`: standard / deuteranopia / protanopia / tritanopia); since M2-03 the **meter arsenal** — `weaponPreset` (Type A–D), `weaponEdit` (Weapon Edit), `megaChoice` (the `!` slot), `shieldChoice` (the `?` slot), all replay-recorded, and `withArsenal` / `arsenalMatches` (the weapon select's choice on top of a config); since M2-04 `optionChoice` (`OPTION_CHOICES`: trail / snake / formation / rotate) and the other `?` shields (`shield`, `freeShield`, `rotateShield`, `reduce`); since M2-05 the ship choice — `shipId` (default `kestrel`) with `powerUpMode: 'direct'` for the MANTA, `withShip` / `shipMatches` (the ship select's choice on top of a config); since M2-06 two-player co-op — `coop` (the title's 2 PLAYERS, `withCoop`) and the drop scaling `coopExtra` (`DEFAULT_COOP_EXTRA` 0.5, `MAX_COOP_EXTRA` 4); since M2-08 the display options `scaleMode` (`SCALE_MODES`: integer / fit / stretch), `screenShake`, `reduceFlashing`, `showHitbox` in `UserOptions.display`; since M2-16 the autofire modes (`autofireMode`, `AUTOFIRE_MODES` always / toggle / hold, `AUTOFIRE_INTERVALS`), the controls options (`SocdChoice`, `MAX_DEBOUNCE_OPTION`, the rebinding `BindingOverrides` / `resolveBindingOverrides`, `BINDING_TOKEN_PATTERN`) and the game options (`UserGameOptions`, `userGameOverrides`, `withUserGameOptions` — folded into the next games' configs) ([guide](../../docs/dev/options-rebinding-and-accessibility.md)) |
| `createFixedStepLoop` | `loop` | 60 Hz accumulator with delta snapping, per-frame cap, reset on resume |
| `createGame`, `GameOptions` | `game` | Composition root: platform + loop + content; bare gameplay (one `World`, one `stepWorld` per tick) or, with `options.scenes`, the scene flow (M1-16 — `game.scenes`; `game.world` is then the game scene's World, a fresh one per start; `options.save` / `options.inputProfiles` give it the save and the profile choices — M1-17; `options.soundTest` the music titles of the sound test — M2-15); suspend/resume (a resume in a running game opens the pause menu); one `game.events` queue for every World; `renderFrame()` returns the reused render contract; `inputContext` (the top scene's); `inputSeats` (M2-06: 2 while a co-op game is on top — the player seats the input adapter routes); the session's debug switches (`game.debug`) and the debug timing — frame advance with `game.requestStep(n)`, slow motion 2× / 4× — in `game.frame` (M1-19) |
| `createSceneFlow`, `SceneFlow`, `SceneStack`, `createSceneStack`, `Scene`, `SceneId`, `BootScene`, `TitleScene`, `DifficultyScene`, `GameScene`, `PauseScene`, `OptionsScene`, `StageClearScene`, `ContinueScene`, `GameOverScene`, `ConfirmDialog`, `WeaponSelectScene`, `AutoOrderScene`, `ShipSelectScene`, `SHIP_MODE_LABELS`, `SHIP_MODE_HINTS`, `WeaponSelectItem`, `InputProfileSetup`, `mergeMenuInput`, `NameEntryScene`, `HiScoreScene`, `DemoScene`, `StoryScene`, `PracticeScene`, `SoundTestScene`, `SoundTestSetup`, `TITLE_ATTRACT_TICKS`, … | `scenes` | The scene stack (depth 8, transitions deferred to the end of the tick) and the M1 flow (M1-16): boot → title (`PRESS OK`, START / OPTIONS / EXIT) → game (owns the World, HUD, WARNING band) ⇄ pause (RESUME / OPTIONS / RETRY STAGE / QUIT TO TITLE) → stage clear (tally, `TO BE CONTINUED`) / game over → title; the YES / NO dialog (Tizen exit confirmation: `platform.exit()` only after YES); the **Options** overlay (M1-17: MASTER / MUSIC / SFX sliders and CONTROLS applied live through `UserOption` events, BACK saves; BULLETS — the colour-blind bullet palette — since M2-02); finished games recorded in the save's hi-score table (`NEW HI-SCORE`), the title's HI from the saved best; since M2-01 the **difficulty menu** under START (a config and a hi-score table per preset) and the **continue countdown** (10 s; OK continues at the last checkpoint, Back / timeout → game over); since M2-03 the **weapon select** after the difficulty menu (TYPE A–D / EDIT, the slot weapons, `?`, `!`, AUTO, ORDER — the Auto Power-Up order editor overlay —, START; since M2-04 an OPTION row — TRAIL / SNAKE / FORMATION / ROTATE — and five `?` shields; a **live preview**: a private, silent World flying the chosen weapons and Option type on the `weapon-range` stage behind the panel; the loadout applied to every difficulty's config with `withArsenal`); Back walks the stack; the frame's world / HUD / UI list / dim; since M2-05 the **ship select** between the difficulty menu and the weapon select (the content's ships — KESTREL / MANTA —, `withShip` for every difficulty; a Direct-mode ship starts the game at once; skipped with a single ship) and session hi-scores per power-up mode and preset; since M2-06 **1 PLAYER / 2 PLAYERS** on the title (`TitleItem` Options 2, Exit 3; `SceneFlow.coop`, `inputSeats`), a co-op player's START joining instead of pausing, the per-player continue countdown and both scores on the end screens (`2p` hi-score rows) ([guide](../../docs/dev/coop.md)); since M2-10 **campaign runs** over the content's zone map (`SceneFlow.run` — `RunState`, the players carried between zones by `captureCarry` / `applyCarry`, `runWorldConfig` / `prepareRunWorld`): the zone title card, the zone result tally (`tallyZone`, `awardZoneBonus`: kill rate, boss time bonus), the **zone map** (`MapScene`: Up / Down, OK launches with `PrepareStage`), the **ending** hook (`EndingScene`), hidden bonus stages (entry, lock-out, the boss skipped), `startPractice`; since M2-14 the **ending scenes** (`EndingScene`: the campaign ending's sprite scene — the citadel falling, the ship rising out of the deep, a dawn for a no-death run — and its epilogue line by line before the result card) and the **credits** (`CreditsScene`: the campaign's credits scrolling to the final zone's credits theme) ([guide](../../docs/dev/zones-h-and-i.md#the-endings)); since M2-15 the complete **front end** (`implemented`): the title's menu is the **mode select** (1 PLAYER / 2 PLAYERS / PRACTICE / OPTIONS / SOUND TEST / EXIT — `TitleItem` Options 3, Exit 5), the **attract loop** (12 s idle on `PRESS OK` → `DemoScene`: a bundled zone demo through `createDemoPlayback`, silent → `HiScoreScene` → `StoryScene` → title; any input → title), the **name entry** after a game whose score entered its table (`NameEntryScene`, `finishGame()`) and the result table, **hi-score tables** per difficulty × ship × mode, the **practice select** (`PracticeScene`: zone, checkpoint, loadout; its own tables), the **sound test** (`SoundTestScene`: the host's tracks through `SimEventKind.SoundTest`, every SFX cue) and the continue countdown's bar and prompt ([guide](../../docs/dev/front-end-and-attract.md)); since M2-16 the Options pages (`ControlsScene`, `DisplayScene`, `GameOptionsScene`; `ControlsItem`, `DisplayItem`, `GameOptionsItem`), the `RebindScene` over the host's `ControlsSetup`, the `InputTestScene`, the save's game options in the next games' configs (a run keeps its `runConfig`), every label from the UI string table (`SceneFlow.text`, `SceneLabels`, `buildSceneLabels`); 512 UI string slots ([guide](../../docs/dev/options-rebinding-and-accessibility.md)) |
| `createListMenu`, `ListMenu`, `menuTick`, `Slider`, `Toggle`, `Choice`, `createChoice`, `Confirm`, `confirmTick`, `repeatDirections`, `MenuResult`, `drawMenu`, `drawPanel`, `drawConfirm`, `buildHud`, `createHud`, `Hud`, `METER_LABEL_FRAMES`, `meterLabelFrame`, `UI_SPRITES`, `resolveUiSprites`, `HUD_COMMAND_COUNT`, `HUD_STRING_COUNT`, `HUD_FAMILY_COLORS`, `HUD_ARM_COLORS`, `bossHpBarFill`, `BOSS_HP_BAR_WIDTH`, … | `ui` (partial) | Canvas UI kit (M1-16): list menus with disabled items and wrap, sliders, toggles, choices (one of several labels, wrapping — M1-17), the YES / NO prompt (default NO); held-duration auto-repeat (18 / 6 ticks) and a 4-tick Confirm buffer; draw builders over `DrawList` string slots; the HUD (1P / HI / 2P, stock icons, the 7-slot power meter with its flashing highlight and greyed slots — MISSILE / DOUBLE / LASER named after the session's weapons since M2-03 —, Force Field pips; in Direct mode — M2-05 — the tier pips SHOT / SUB / ARM / SPD and the family label instead; in a co-op game — M2-06 — a blinking `PRESS START`, two compact halves while both ships fly and `PRESS START` / `GAME OVER` for an out player: `HudPlayerState`, `hudPlayerState`, `METER_SHORT_LABELS`; since M2-09 the boss HP bar — `BOSS` and a red bar in place of the hi-score with `Hud.showBossHp`, [guide](../../docs/dev/advanced-bosses.md#the-boss-hp-bar)) rebuilt only on change, without allocation; since M2-15 the 3-letter **name entry** (`NameEntry`, `createNameEntry`, `nameEntryTick`, `drawNameEntry`, `NAME_ENTRY_GLYPHS` — the four directions and OK only); since M2-16 the **rebind widget** (`RebindPanel`, `createRebindPanel`, `rebindTick`, `drawRebindPanel`, `RebindStatus`, `CaptureStatus`, `RebindEvent`, `REBINDABLE_ACTIONS`) and the **UI string table** (`ui/strings.ts`: `DEFAULT_UI_TEXT`, `UI_TEXT_IDS`, `resolveUiText`, `formatUiText` — every builder takes a table) |
| `createWorld`, `WorldOptions`, `stepWorld`, `World`, `WORLD_PHASES`, `WorldPhase`, `syncWorldView`, `PoolRegistry`, `resolveWorldStage`, `ENGINE_SPRITES`, `DEATH_HIT_STOP_TICKS`, `DEATH_SHAKE_TICKS`, `DEATH_MUSIC_DUCK_TICKS`, `updateWorldRank`, `canContinue`, `continueWorld` | `world` | The session state (tick, RNG streams, events, players, camera, status, hit-stop, the fx timers, debug flags, pools, the stage runner / terrain / parallax of `config.stage`, the enemy system, the rank, the bullet system, the weapon system, the power-up system, the scoring system and the boss system, `view`) and the fixed 9-phase tick pipeline of plan §3.2; hit-stop skips phases 2–8 for exactly the requested ticks; the death sequence (phase 7), respawns with the `arcade` checkpoint restart and game over (phase 2) — M1-12; the rank recomputed at the end of phase 3 from `world.rankInputs` and continues after a game over (`continueWorld`: fresh lives, the last checkpoint, the continue digit) — M2-01; `world.patterns`, the pattern DSL's interpreter (installed as the bullet system's program runner) — M2-02; the config's ship (`shipId`) and, in Direct mode, the Direct loadout, death penalty and power rank — M2-05; two-player co-op — the drop-in join in phase 1 (`JOIN_ACTIONS`, `playerCanJoin`, `joinPlayer`), per-player continues (`continuesLeft`, `continueWorld(world, who)`), player 2 drawn in its `@p2` palette swap — M2-06 ([guide](../../docs/dev/coop.md)); the stage gimmicks `world.gimmicks` in phases 2 / 3 / 5 / 9 (pull fields, region triggers, moving blocks, terrain regrowth, shots breaking tiles, the block and chain batches) — M2-07 ([guide](../../docs/dev/advanced-stages.md)); the hidden bonus entrances `world.bonus` (phase 3) and the stage-clear fly-out (phase 2, `flyOutPlayer`) — M2-10; zero allocation per tick |
| `createStageRunner`, `StageRunner`, `StageHooks`, `StageEventCode`, `createStageTerrain`, `createParallaxView`, `createTerrainView`, `findEventCursor`, … | `stage` | Stage runtime (M1-07): camera keys with ramps, vertical pans and scroll locks, the WARNING's `brake()` to a lock (M1-13); the sorted event timeline fired through a cursor; checkpoints with restart by binary search, and `jumpTo(x)` — the same restart at any scroll x (M1-18, the debug stage skip); the terrain map and the parallax / terrain views the renderer draws; since M2-07 timed scroll stops (`hold`), diagonal pans (`yOver`), in-stage branches (`eventActive`, `setFlag`) and region triggers (`probe`), and in `stage/systems.ts` the World's `StageGimmicks` (`createStageGimmicks`, `MovingBlockSystem`, pull fields, chains) ([guide](../../docs/dev/advanced-stages.md)); since M2-08 `createStageEffectsView` (the stage's `raster` / `cycles` as the view the renderer draws — [guide](../../docs/dev/presentation-polish.md)); since M2-09 `follow(target)` / `StageCameraTarget` (a battleship raid's camera path, the timeline held where the follow began — [guide](../../docs/dev/advanced-bosses.md#battleship-raids)); since M2-10 the hidden bonus-stage entrances in `stage/bonus.ts` (`BonusEntrances`: a marked gap, all ground targets destroyed, a score digit; `lock`, the restart re-arm) |
| `PlayerShip`, `createPlayer`, `spawnPlayer`, `updatePlayer`, `readPlayerIntent`, `resolvePlayerShip`, `playerHit`, `PlayerHitCause`, `DIAGONAL_SCALE`, … | `player` | Ship movement (the KESTREL; since M2-05 the config's `shipId` — the MANTA too): speed levels from `content/player/`, diagonal × 0.7071 (D4), no inertia, clamp to the camera view minus margins, rides the camera scroll, banking, 40-tick fly-in; hits recorded by `playerHit` after the ship's Force Field (`PlayerShip.shield`) had its say; the life cycle (M1-12): `killPlayer` (`dying` 24 ticks, a life gone), `dead` 60 ticks, `respawnPlayer` (a blinking fly-in, 150 invulnerable ticks once control returns), `playerOut` (implemented for P0; joining a co-op game and continuing mid-game are `world`'s `joinPlayer` — M2-06); since M2-10 the stage-clear fly-out (`PlayerState` `leaving`, `flyOutPlayer`, `LEAVE_ACCELERATION` / `LEAVE_MAX_SPEED` / `LEAVE_END_X`) |
| `circleCircle`, `aabbAabb`, `circleAabb`, `capsuleCircle`, `segmentAabb`, `CollisionLayer`, `createSpatialGrid`, `TerrainMap`, `terrainAt`, `boxHitsTerrain`, `terrainRectHit`, `findFloor`, `findCeiling`, `TerrainBlocks`, `DestructibleTerrain`, `TerrainHit` | `collision` | Scalar-argument shape tests (closed shapes: touching hits), layer masks, uniform grid broad phase rebuilt by counting sort, pixel-exact terrain queries over per-tile column-height masks (the bending lasers' circle chains live in `bullets`); since M2-07 moving blocks inside every terrain query (`TerrainBlocks`) and destructible tiles (`DestructibleTerrain`: per-cell damage, regrowth, placed tiles, the checkpoint rollback, the `TerrainChanges` log; `TerrainHit`) |
| `hashWorld`, `createDebugFlags`, `DebugFlags`, `createDebugControls`, `DebugCommand`, `collectDebugCounters`, `DebugCounters`, `skipToBoss`, `jumpToCheckpoint`, `jumpToNextCheckpoint`, `BOSS_SKIP_LEAD`, … | `debug` | FNV-1a 32 state hash over tick, RNG states, camera, the stage runner's state, status, hit-stop, rank, players, every pool's live slots (the enemy bullets and lasers and the player shots among them), the enemies, the formation table, the player weapons' loadouts, Option trails, timers and cooldown tables, the power meters, pending Mega Crashes, shields and taken drops, the shake / flash timers, scores (with their extend thresholds and continue counts) and credit counters — not the session hi-score —, the continues used and the rank inputs (M2-01), the point item pool, the bending lasers and the pattern interpreter's runners in use (M2-02), and the boss, its parts and the WARNING (M1-13) (golden replays); the debug switches of a session (`game.debug`, shared by every World: god mode, outlines, frame advance, slow motion, overlay); the debug controls (M1-19: god mode, outline cycle, frame advance and single steps — run by `Game.frame` / `game.requestStep` —, slow motion, jump to the next checkpoint, skip to the boss — acting only while a stage is played); the overlay counters (pools, rank, RNG calls, a hash every 60 ticks); the stage skip to 96 px before the first `warning` / `boss` event (`GameConfig.stageSkip`, M1-18); since M2-07 the hash covers the stage gimmicks, since M2-10 the bonus entrances and the enemy totals, since M2-16 the toggle autofire mode's firing switch (only in that mode) |
| `IRenderer`, `IAudio`, `RenderFrame`, `WorldView`, `SpriteBatchView`, `LaserView`, `BendingLaserView`, `WarningView`, `StageEffectsView`, `RasterEffectView`, `RasterKind`, `ColorCycleView`, `HitboxView`, `createHitboxBatch`, `createSpriteBatch`, `pushSprite`, `SpriteFlag`, `LayerId`, `DrawList`, `createDrawList`, `TextMetrics` | `presentation` | Back-end contracts and the render contract (plan §3.4): world sprite batches in typed arrays, the enemy lasers (M1-09), the bending lasers (M2-02), the boss WARNING (M1-13), HUD / UI command lists (rect, sprite, text slot, number), draw layers, screen effects — implemented by `@shmup/render-pixi` / `@shmup/audio-web` — since M2-07 `TerrainView.changes` (`TerrainChanges`, the destructible terrain's change log); since M2-08 `WorldView.effects` (the stage's raster effects and palette cycles) and `WorldView.hitboxes` (the ships' hurtboxes for the show-hitbox option) — presentation only, not hashed |
| `createRng`, `createRngStreams`, `RNG_STATE_WORDS` | `rng` | sfc32 seeded from one 32-bit seed; independent gameplay + cosmetic streams, zero-alloc state snapshots; `nextFloatInto` (M2-02) draws a float into a typed array (no boxed return) |
| `sinB`, `cosB`, `atan2B`, `quantizeAngle`, `angleDelta`, `turnToward`, `wrapAngle`, `clamp`, `lerp`, `approach`, `EASINGS` | `math` | Binary angles (1024/turn) on committed lookup tables + easing curves |
| `createEventQueue`, `SimEventKind`, `SFX_CUES`, `SfxPriority`, `MUSIC_CUES`, `FX_CUES` | `events` | Sim → presentation ring of typed arrays (drop-oldest) and the canonical cue registries (`SFX_CUES.Clink` for armour hits since M1-10; `SimEventKind.PowerUp`, `SFX_CUES.PowerUpDenied` and `FX_CUES.ShieldBreak` since M1-11; `SimEventKind.MusicDuck` and `FX_CUES.Debris` since M1-12; `SimEventKind.Dim` / `BossDefeated`, `FX_CUES.BossChain` / `BossBlast` and the `SfxPriority` hints since M1-13; `SimEventKind.Score` — points at a place, for the score popups — since M1-14; `SimEventKind.UserOption` + `UserOptionKind` — the Options screen's live changes — since M1-17; `UserOptionKind.BulletPalette` since M2-02; `SFX_CUES.PlayerJoin` — a co-op player joined — since M2-06; `SimEventKind.BossEscaped` since M2-09; `SimEventKind.PrepareStage` — prepare a stage's presentation (its music set) before it plays — since M2-10; `UserOptionKind.InputSettings` — re-apply the save's input settings — since M2-16) |
| `createSoaPool`, `createPool` | `pools` | Struct-of-arrays typed-array pools (deferred free + swap-remove) and object pools |
| `loadContent`, `ContentDb`, `EMPTY_CONTENT_DB`, `CONTENT_MIGRATIONS`, `s`, `Schema`, `Infer`, `bakePath`, the `…Spec` types (`BossSpec` and its parts since M1-13) | `data` | Schema-validated `content/` (player, weapons, enemies with their boss sections, rank modifiers and revenge bullets, paths, stage, tileset, since M2-01 `rules` — the difficulty presets as `ContentDb.difficulty`, since M2-02 its `scoring` section as `ContentDb.scoring` — and since M2-02 `patterns`, compiled into `ContentDb.patterns`, with the enemy `pattern` reference): issues with `<file>:<json path>`, migrations, every `s.ref` string resolved to a numeric `<field>Id` at load, stage tilemaps expanded (heightfield generator / RLE rows), paths baked into arc-length tables, the engine's own sprites interned (`extraSprites`); since M2-05 a ship's `mode` / `startSpeedLevel`, the `weapons` files' Direct-mode `families` (`WeaponFamilySpec`, `ContentDb.weaponFamilies`), a stage's `directItems` plan and the `powerup` drop; since M2-06 player 2's palette-swap sprite of every ship (`P2_SPRITE_SUFFIX`, `PlayerShipSpec.spriteP2Id`); since M2-07 destructible tiles (`hp`, `regen`, `score`), camera `hold` / `yOver`, stage `branches`, `trigger` / `block` events and the `ballistic` mover; since M2-10 the `campaign` kind (`ContentDb.campaign`: the zone map's graph checks, routes, endings — `selectCampaignEnding`), stage type `bonus`, `bonus` events and the `oneUp` / `bonusCapsule` drops; since M2-15 the `replay` kind — the attract demos (`ContentDb.demos`, `DemoSpec`, `MAX_DEMO_TICKS`) — and the campaign's `story` (`CampaignStoryPage`, `STORY_SCENES`); since M2-16 the `strings` kind — the UI string tables (`ContentDb.uiStrings`, `UiStringsSpec`) |
| `createEnemySystem`, `EnemySystem`, `Enemy`, `EnemyFlag`, `ScriptApi`, `FormationTable`, `DropKind`, `MAX_ENEMIES`, … | `enemies` (partial) | Enemies (M1-08): 64 pooled instances spawned by stage `spawn` / `formation` events, formations with kill tracking (bonus + capsule when all killed), off-screen / settle / despawn rules, hit flash and deaths, contact with the ships, ground / air sprite batches; `ScriptApi` fire primitives with the fire rule (M1-09); boss entries are never spawned here (M1-13); per-enemy rank modifiers (`BulletSystem.setShooterRank` around its script) and revenge bullets (`aimed` / `spread3` / `ring8` on on-screen player kills at `minRank`+, never on a Mega Crash) — M2-01; `ScriptApi.bendingLaser`, `startPattern` / `stepPattern` (DSL patterns on the enemy's emitter) — M2-02; the **Option Hunter** (M2-04: the `optionHunter` flag — spawns only while a ship has Options, alarm, armour, harmless contact; `huntOptions` steals the touched Option and the chain behind it, `carriedBatch` draws them grey, a kill frees them as `DropKind.FreeOption`), the blue capsule's `clearOnScreen` and the shield pods' contact test; since M2-05 the mode-agnostic `powerup` drop (`DropKind.PowerUp` 3 — `FreeOption` is 4); since M2-07 `Ballistic` landings, `destroy` (removal without a kill), behaviour death callbacks and the stage-gimmick calls of `ScriptApi` (`setMoverOf`, `pull`, `release`, `chain`, `placeTile`, `tileId`, `landed`, `destroy`; `EnemyGimmicks`); since M2-10 the running totals `stats` (`EnemyStats`: spawned / killed, ground apart) and the `oneUp` / `bonusCapsule` drops (`DropKind` 4 / 5 — `FreeOption` is 6) |
| `Script`, `resumeScript`, `SLEEP_FOREVER`, `MoverKind`, `setMover`, `updateMover`, `FollowTrack`, `samplePath`, `fireAimed`, `fireNWay`, `fireRing`, `fireSpiral`, `fireStack`, `fireSpray`, `fireHoming`, `fireDelayed`, `rankedWait`, `compileExpression`, `compilePatternBank`, `PatternBank`, `PATTERNS_FILE_SCHEMA`, `createPatternVm`, `PatternVm`, `PatternOp`, `ExprOp`, … | `patterns` | Behaviour coroutines resumed only on wake (D29), the per-tick movers (straight, sine, arc-length path, waypoint, follow, ground crawl, homing, aimed dash), the rank-scaled fire primitives of M1-09 and (M2-02) the BulletML-inspired **pattern DSL**: `content/patterns/` compiled at load (recursive-descent expressions over `$rank` / `$rand` / `$loop` / `$i` / params, folded into postfix code in one `Float64Array`; references inlined, params as values) and run by the zero-allocation `PatternVm` — enemy emitters stepped by the script runner, bullets' own programs inside the bullet update ([guide](../../docs/dev/pattern-dsl.md)); since M2-07 the `Ballistic` mover (gravity, a proximity trigger, `BallisticLand` pass / stop / shatter) |
| `DEFAULT_BEHAVIORS`, `DEFAULT_BOSS_BEHAVIORS`, `KNOWN_SCRIPT_IDS`, `defineBehavior`, `defineBossBehavior`, `createBehaviorRegistry`, `createBossBehaviorRegistry`, `checkEnemyBehaviors`, … | `behaviors` | Registries of the behaviour scripts content refers to (`script` ids) with per-enemy / per-phase tunables; the M1 roster (`drifter.sine`, `fan.loop`, `carrier.straight`, `turret.floor`, `walker.floor`, `hatch.spawner`, `rammer.aimed`, `orbiter.loop`) — turrets, walkers and orbiters fire since M1-09 — plus `pattern.loop` (M2-02: runs the enemy's DSL `pattern`) and `hunter.option` (M2-04: the Option Hunter's rear / front / dive line-up and charge), and the boss roster (`boss.hover`, `boss.lanes` — M1-13; `boss.bulwark`, HALCYON BULWARK's attached lane lasers — M1-18); since M2-05 `cube.pincer` (the Direct mode's six-cube pincer waves); since M2-07 the stage gimmicks `rock.fall`, `bubble.split` (a death callback), `volcano.lob`, `field.suction`, `tentacle.grab` and `cube.stack` ([guide](../../docs/dev/advanced-stages.md)); since M2-09 the captains `captain.ram`, `captain.launcher`, `captain.circler`, `captain.crab` and the raid turrets' `boss.raid` ([guide](../../docs/dev/advanced-bosses.md#behaviours)); since M2-11 zones B and C's `rocket.homing` (homing rockets), `worm.burst` (sand worms bursting from the dunes) and their bosses `boss.maw` (GALVANIC MAW: a mouth that opens with its jaws, cutters, rockets) and `boss.widow` (SANDGRAVE WIDOW: sidesteps, spider drones, silk-line lasers); since M2-12 zone E's `rear.swoop` (rear attackers) and the bosses `boss.bastion` (CINDER BASTION: rotating shield arms, lane lasers) and `boss.steed` (SQUALL STEED: a chest that opens to launch homing minis); since M2-13 zone F's `cell.chase` (chasing cells) and the bosses `boss.squid` (MANTLE REGENT: tentacles curling in front of its eye) and `boss.facet` (FACET MONARCH: a core behind crystals, waving tentacle arms); since M2-14 (the roster complete) zone H's `emitter.laser` (laser emitters), zone I's `mine.burst` (depth mines — the enemy system's proximity wake `ScriptApi.sleepUntilNear`) and the final bosses `boss.sovereign` (IRON SOVEREIGN's four-phase finale — its spiral is the boss system's stream, `BossScriptApi.spiral`), `boss.ark` (the ABYSS ARK raid: turret rows, hooks) and `boss.angler` (THE HOLLOW KING: a mouth that opens, a swaying lure) |
| `createBulletSystem`, `BulletSystem`, `spawnBullet`, `fireLaser`, `cancelAllBullets`, `BulletKind`, `BULLET_KINDS`, `BulletFlag`, `LaserPhase`, `AIM_AT_TARGET`, `MAX_ENEMY_BULLETS`, … | `bullets` | Enemy bullets and lasers (M1-09): a 512-slot SoA bullet pool that is also the `ENEMY_BULLETS` sprite batch (acceleration, turning, delays, changes, capped homing; riding the camera; culled outside the view ± 16 px and on terrain), 16 telegraphed lasers (capsule hitbox only at full width, attached to their enemy or fixed), brute-force collision with the ships (`playerHit(Bullet / Laser)`), sparkle cancel; the session's rank scales × `bulletSpeedMul` and a shooter's own (`setShooterRank` / `clearShooterRank`, M2-01); since M2-02 **bending lasers** (8 stable slots of 64-node rings, homing heads, circle-chain hitboxes — `fireBendingLaser`, `BendingLaserTable`), **cancel into point items** (`CancelMode.Points`: a boss's death, a Mega Crash — items fly to the credited score, `bulletCancel` points each) and bullets running DSL programs (`BulletProgramRunner`, timed speed / turn terms) |
| `createWeaponSystem`, `WeaponSystem`, `Loadout`, `applyLoadoutPreset`, `MainWeapon`, `WeaponRole`, `ShotKind`, `ShotFlag`, `checkWeaponBehaviors`, `resolveArsenal`, `weaponsOfSlot`, `weaponLabel`, `WEAPON_BEHAVIOR_PARAMS`, `WEAPON_BEHAVIOR_LABELS`, `WEAPON_SCRIPT_IDS`, `RIPPLE_RING_WIDTH`, `applyDirectLoadout`, `resolveFamilies`, `DIRECT_MAX_LEVEL`, `WEAPON_ROLE_SLOTS`, `MAX_PLAYER_SHOTS`, … | `weapons` | Player weapons (M1-10): meter mode's Type A arsenal from `content/weapons/` (main shot, Double pair, piercing Laser, ground-sliding Missile) in a 96-slot SoA shot pool; per-player loadouts (`GameConfig.loadout`); always-on autofire with caps per shooter (ship + Options); shots ride the camera and die on terrain; grid-based hits (armour clinks, per-enemy cooldowns for piercing shots, kill credit); since M2-03 the **Types B–D** behaviours (Spread Bomb blast, 2-Way Missile, Photon Torpedo, Tail Gun, Vertical, Free Way, Ripple ring, Cyclone and Twin Lasers), the presets and Weapon Edit of the config (`resolveArsenal`) and the in-place `setArsenal` of the weapon select's preview; since M2-05 the **Direct-mode shot families** — every weapon a family fires gets a direct role after the meter roles (`WEAPON_ROLE_SLOTS`), each level a volley of emitters on the main / missile timers (`direct.bolt`, `direct.bomb`), `Loadout.shot` / `sub` / `family`, `applyDirectLoadout`; since M2-16 the autofire modes (`WeaponSystem.firing` — the toggle mode's per-player switch) |
| `OptionGroup`, `createOptionGroup`, `OptionMode`, `MAX_OPTIONS`, `OPTION_SPACING`, `OPTION_TRAIL_CAPACITY`, `OPTION_SPRITE`, `STOLEN_OPTION_SPRITE`, … | `options` | Up to four Options per ship of the session's type (`GameConfig.optionChoice`, M2-04): the **trail** on a screen-space ring buffer that advances only with movement input (D26) — they bunch while the ship is idle during scrolling and spread when it moves —, the **Snake** (a chain pulled along behind the ship that keeps its shape when it stops), the **Formation** (a `>` behind the ship spreading into a `V`) and **Rotate** (an orbit that widens); Formation / Rotate spread while PowerUp is held 15 ticks or after a Special toggle (remote Ch+) — `OptionGroup.steer` |
| `createPowerUpSystem`, `PowerUpSystem`, `PowerMeter`, `advanceMeter`, `canEquipSlot`, `equipSlot`, `MeterSlot`, `MeterChoices`, `MegaEffect`, `meterChoicesOf`, `lifeOptionCount`, `ItemKind`, `applyDirectDeathPenalty`, `directMaxLevel`, `DEFAULT_DIRECT_ITEM_PLAN`, `DIRECT_ITEM_TICKS`, `MAX_ITEMS`, `CAPSULE_SCORE`, … | `powerups` | Meter mode (M1-11): the 7-slot power meter per player (capsules advance and wrap, the PowerUp press equips on its edge, maxed slots greyed, Double / Laser exclusive), Auto Power-Up in `GameConfig.autoPowerUpOrder`, a 32-slot SoA pool of world-space capsules from enemy / formation drops (16-px pickup magnet, every pickup counts, 300 points), Mega Crash (cancels bullets, destroys non-immune enemies, screen flash); the death penalties `applyDeathPenalty` / `loseOneLevel` (M1-12, D6: classic one level, arcade everything, casual only the shield); since M2-03 the **`!` choices** (Mega Crash, NORMAL, SPEED DOWN, LIFE OPTION, FULL BARRIER — each with its greyed rule) and the `?` choice, from the config (`MeterChoices`); since M2-04 the pod shields placed and spun, Free Shield pairs at the last 8-way direction, FULL BARRIER's `refillShield`, the rare **blue capsule** (`clearScreen`: every enemy on screen) and the **freed Options** of a dead Option Hunter (drifting items, `regainOption`); since M2-05 **Direct mode**: `capsule` / `powerup` drops become the stage's next planned colour item (`directItems`, cycling), six drifting items (red / green levels, blue Arm, orange 1UP, yellow smart bomb, octagon family switch — `collectDirect`, `dropDirect`), the Speed toggle, `applyDirectDeathPenalty`; since M2-06 the co-op drop scaling (`coopCredit`, `COOP_EXTRA_OFFSET` — extra items while two ships play); since M2-10 the bonus stages' items (`ItemKind.OneUp` — a life —, `ItemKind.BonusCapsule` — 1,000 points) |
| `ScoreBoard`, `PlayerScore`, `addScore`, `markContinue`, `createScoringSystem`, `ScoringSystem`, `MAX_SCORE`, `MAX_LIVES`, … | `scoring` (partial) | Per-player scores (M1-12): `addScore` clamped at 99,999,990 with HUD dirty flags, the session hi-score (`setHiScore`; not hashed — the scene flow starts it from the save's best since M1-17), the `ScoringSystem` crediting kills to their killer, formation bonuses to the killer of the last member and 300 per capsule, each exactly once; a `Score` event per credited kill for the popups (M1-14, not hashed); `ScoringRules` / `DEFAULT_SCORING_RULES` (M2-02: the points of a cancelled bullet); `HiScoreEntry` is the row of the save's tables; **extends** (M2-01: 20,000 then every 70,000 — `GameConfig.extendFirst` / `extendEvery` —, +1 life up to `MAX_LIVES` 9 with a critical `ExtraLife` sound, checked after every crediting) and the **continue digit** (`markContinue`: the score's last digit counts continues) (the Direct-mode 1UP item of M2-05 goes through the same `MAX_LIVES` cap; since M2-06 `PlayerScore.continues` is also each co-op player's own continue budget; name entry: M2-15) |
| `FxState`, `requestHitStop`, `requestShake`, `requestFlash`, `tickFx`, `shakeAmount`, `ShakeMagnitude`, `FlashKind`, … | `fx` (partial) | Sim-side game feel (M1-12): hit-stop requests (exactly `n` frozen ticks), decaying integer screen shake (3 magnitudes), flash kinds — hashed timers plus the `HitStop` / `Shake` / `Flash` events the presentation (`@shmup/render-pixi` `effects`) draws since M1-14 (authentic slowdown: M3-02) |
| `ShieldState`, `grantShield`, `refillShield`, `canGrantShield`, `absorbShieldHit`, `absorbPodHit`, `placeShieldPods`, `tickShield`, `shieldWearFrame`, `podWearFrame`, `reduceHurtScale`, `FORCE_FIELD`, `FRONT_SHIELD`, `FREE_SHIELD`, `ROTATE_SHIELD`, `REDUCE`, `ShieldKind`, `shieldSpecOf`, `ARM`, `collectArm`, `armTierOf`, `armWearFrame`, `ARM_TIER_HITS`, `ARM_TIER_BLUE`, … | `shields` | The meter shields on every ship (`PlayerShip.shield`, the `?` choice `shieldSpecOf(config.shieldChoice)`): the Force Field (5 hits, absorbs bullets, lasers and contact inside `playerHit`, never terrain) and since M2-04 the front Shield, Free Shield (pairs attached where the ship last flew, up to four pods) and Rotate Shield — pods of 14 hits each that stop the bullets and bodies touching them and wear independently — and Reduce (2 hits, the hurt radius ⅓ → ⅔ → full; the terrain box unchanged); 8-tick shield-hit i-frames (per pod), wear frames, break events; since M2-05 the Direct mode's **Arm** (green 3 / silver 4 / gold 5 hits after 1 / 4 / 9 blue items, `collectArm` repairs, absorbs terrain, a break starts the count over) |
| `createBossSystem`, `BossSystem`, `Boss`, `BossPart`, `BossState`, `BossRole`, `BossHit`, `BossScriptApi`, `BossHpBar`, `RaidCamera`, `EndingFlag`, `WarningState`, `WARNING_TEMPLATE`, `formatWarningText`, `turnedFrame`, `MAX_BOSSES`, `BOSS_PART_ID_BASE`, `WARNING_TICKS`, … | `bosses` | Bosses (M1-13): multi-part bosses (16 parts placed parent + offset, riding the camera), weak points that clink (armour, `afterParts`, `whenOpen`, the intro), cores, phases on HP / destroyed parts / time swapping the boss behaviour, contact, the stage `warning` → the WARNING (status `bossWarning`, camera brake, siren / dim / flash / music events, the game's own text in `view.warning`) → the invulnerable fly-in, and the death sequence (bullet cancel into point items for the killer — M2-02 —, chain explosions, final blast with hit-stop, score tally, `stageClear`, lock released); M2-09: four boss slots, turned parts (binary-angle transforms, circle hurtboxes, heading frames), captains (mid-bosses), battleship raids (boss-relative camera segments), a boss inside a boss, double bosses (turns, the survivor's enrage), time limits (escape, the `BossEscaped` ending flag), the HP bar's model, boss rushes; M2-11: `BossPart.restX` / `restY` (the part's offsets from the boss data, what a behaviour's `setPartOffset` moves are measured from) |
| `computeRank`, `createRankInputs`, `powerRank`, `RANK_POWER`, `directPowerRank`, `RANK_ARM_TIER`, `rankScale`, `rankSensitivity`, `RankCurve`, `DIFFICULTY_RANK_BASE`, `BULLET_SPEED_RANK_CURVE`, `FIRE_RATE_RANK_CURVE`, `difficultyRankInputs`, … | `rank` | Rank 0–31 (M2-01): `base + floor(growth × (8·(loop − 1) + (stage − 1) + power + special))`, at most 16 on loop 1, the power term of the strongest ship (Missile +1, Double +2, Laser +3, each Option +1, a shield +4, Reduce +2); curves that are exactly 1 on Normal, scaling enemy bullet speed and fire rate; `rankSensitivity` for per-enemy modifiers ([guide](../../docs/dev/difficulty-and-rank.md)); since M2-05 the Direct-mode power term `directPowerRank` (`floor((shot + sub) / 2)` + the Arm's tier: +2 / +3 / +4 — max 12, like the meter) |
| `loadSave`, `parseSave`, `SaveData`, `SaveStore`, `createSaveStore`, `SAVE_MIGRATIONS`, `migrateSave`, `sanitizeSave`, `serializeSave`, `writeSave`, `insertHiScore`, `createHiScoreEntry`, `hiScoreModeKey`, `SAVE_STORAGE_KEY`, `SAVE_CORRUPT_KEY`, … | `save` | Versioned persistence through `Platform.storage` (M1-17, feat §21): one JSON document under `save.v1` (options, hi-score tables per mode key — top 10 —, stats); JSON → forward migrations (the pre-release layout is version 0) → a field-by-field sanitiser; a corrupt or unreadable save falls back to defaults and is copied to `save.corrupt`; `SaveStore.flush()` writes only when the canonical text changed and never rejects ([guide](../../docs/dev/saves-and-options.md)); since M2-15 a table per difficulty × ship × mode (`hiScoreModeKey(config, mode)`, `HI_SCORE_MODES`, `parseHiScoreModeKey`; old co-op rows moved when read) and `SaveStore.renameScore` for the name entry; since M2-16 **format 2** (the controls and game options; `SAVE_MIGRATIONS` 1 → 2 moves the old co-op / practice rows) (unlocks: later) |
| `createReplayHeader`, `createReplayRecorder`, `createPlayback`, `createReplayGame`, `playReplay`, `encodeReplay`, `decodeReplay`, `DesyncReport`, `Replay`, `ReplayHeader`, `REPLAY_HASH_INTERVAL`, … | `replay` | Replays (M1-19, feat §21): a header with everything needed to recreate the start (the whole `GameConfig`, stage, checkpoint, loadout, `assisted` = god mode), `held \| pressed << 16` per tick and player, a state hash every 600 ticks and at the end; the recorder and the playback are `PlatformInput`s; `DesyncReport` names the first diverging tick; JSON with run-length + varint + base64 input; build lock reported (`buildMatches`). Used by the golden replays of `test/golden/`; since M2-15 also the attract loop's demo play (`createDemoPlayback`, `DemoPlayback`, `DEMO_BUILD_ID` — `replay/demo.ts`; the session-free parts moved to `replay/format.ts`); since M2-16 the header records `autofireMode` (older headers decode to `'always'`) |

No placeholder module is left: `replay` was the last one, implemented with M1-19 (modules
marked *partial* above still have planned work — see
[`docs/dev/api-reference.md`](../../docs/dev/api-reference.md#placeholder-modules)).

## Scripts

```sh
pnpm --filter @shmup/core test        # Vitest (Node, headless; workers get --expose-gc --allow-natives-syntax for the allocation guard)
pnpm trig:tables                      # regenerate src/math/trig-table.ts (repo root; a test diffs it)
pnpm content:check                    # validate content/ with loadContent() (repo root)
pnpm --filter @shmup/core typecheck   # src (pure) + test/ (Node) programs
pnpm --filter @shmup/core build       # tsc → dist/ (ES2018 + .d.ts)
```

The deterministic primitives (`rng`, `math`, `events`, `pools`) have their own guide:
[`docs/dev/engine-foundations.md`](../../docs/dev/engine-foundations.md); the content
loader and schema combinators (`data`) theirs:
[`docs/dev/content-data.md`](../../docs/dev/content-data.md); the render contract
(`presentation`) is explained with its renderer in
[`docs/dev/rendering-and-shell.md`](../../docs/dev/rendering-and-shell.md); the World, its tick
pipeline, the player ship, collision and the state hash (`world`, `player`, `collision`,
`debug`) in [`docs/dev/sim-world.md`](../../docs/dev/sim-world.md); the stage runtime (`stage`,
the terrain queries of `collision`, the `stage` / `tileset` data and the tilemap expansion) in
[`docs/dev/stage-runtime.md`](../../docs/dev/stage-runtime.md); the advanced stage systems of
M2-07 (destructible terrain, moving blocks, branches and triggers, holds and diagonal pans, the
gimmick behaviours, `stage/systems.ts`, the Tiled importer) in
[`docs/dev/advanced-stages.md`](../../docs/dev/advanced-stages.md); the stage's raster effects and palette cycles, the
hitbox mirror and the display options (M2-08):
[`docs/dev/presentation-polish.md`](../../docs/dev/presentation-polish.md); the advanced bosses of
M2-09 (four boss slots, turned parts, captains, raids, double and inner bosses, timers, the HP bar,
boss rushes) in [`docs/dev/advanced-bosses.md`](../../docs/dev/advanced-bosses.md); zones B and C's behaviours
(`rocket.homing`, `worm.burst`, `boss.maw`, `boss.widow`) and `BossPart.restX` / `restY` (M2-11) in
[`docs/dev/zones-b-and-c.md`](../../docs/dev/zones-b-and-c.md); zones D and E's behaviours (`rear.swoop`,
`boss.bastion` — rotating shield arms as parts on a spinning hub —, `boss.steed`, M2-12) in
[`docs/dev/zones-d-and-e.md`](../../docs/dev/zones-d-and-e.md); zones F and G's behaviours (`cell.chase`,
`boss.squid`, `boss.facet` — tentacle arms on the curling-arm rule —, M2-13) in
[`docs/dev/zones-f-and-g.md`](../../docs/dev/zones-f-and-g.md); zones H and I's behaviours
(`emitter.laser`, `mine.burst`, `boss.sovereign`, `boss.ark`, `boss.angler`), the proximity wake
`ScriptApi.sleepUntilNear`, the spiral stream `BossScriptApi.spiral`, the campaign's ending scenes,
epilogues and credits (`ENDING_SCENES`, `CampaignCreditsSection`, `EndingScene`, `CreditsScene`, M2-14)
in [`docs/dev/zones-h-and-i.md`](../../docs/dev/zones-h-and-i.md); the front end — the mode select, the
attract loop and its demos (`DemoScene`, `createDemoPlayback`, the `replay` kind), the story crawl,
the hi-score tables per mode, the name entry (`NameEntry`, `NameEntryScene`, `renameScore`), the
practice select and the sound test (`SoundTestScene`, `SimEventKind.SoundTest`, M2-15) — in
[`docs/dev/front-end-and-attract.md`](../../docs/dev/front-end-and-attract.md); the enemies, formations,
behaviour coroutines, movers and spline paths (`enemies`, `patterns`, `behaviors`, the
`enemies` / `paths` data and `data/paths.ts`) in
[`docs/dev/enemies-and-behaviors.md`](../../docs/dev/enemies-and-behaviors.md); the enemy
bullets, lasers, fire primitives and rank (`bullets`, the fire half of `patterns`, `rank`) in
[`docs/dev/bullets-and-patterns.md`](../../docs/dev/bullets-and-patterns.md); the player weapons,
loadouts, autofire and Options (`weapons`, `options`) in
[`docs/dev/weapons-and-options.md`](../../docs/dev/weapons-and-options.md); the meter arsenal —
Types B–D, Weapon Edit, the `!` / `?` choices, the weapon select and its live preview (`weapons`,
the arsenal fields of `config`, `MeterChoices` of `powerups`, `WeaponSelectScene` /
`AutoOrderScene`, `meterLabelFrame` of `ui`) — in
[`docs/dev/meter-arsenal.md`](../../docs/dev/meter-arsenal.md); the Option types, the meter
shields (pods, Reduce), the Option Hunter and the blue capsule (`options`, `shields`,
`huntOptions` / `hunter.option`, the freed Options of `powerups`) in
[`docs/dev/options-shields-hunter.md`](../../docs/dev/options-shields-hunter.md); Direct mode, the MANTA and the ship select
(the direct half of `powerups` and `weapons`, the Arm of `shields`, `withShip` of `config`,
`ShipSelectScene`, the HUD's tier pips, `directPowerRank`, `cube.pincer`) in
[`docs/dev/direct-mode.md`](../../docs/dev/direct-mode.md); two-player co-op (the join and per-player continues of
`world`, `coop` / `coopExtra` of `config`, the co-op HUD of `ui`, `1 PLAYER` / `2 PLAYERS` and
`inputSeats` of `scenes` / `game`, the co-op credit of `powerups`) in
[`docs/dev/coop.md`](../../docs/dev/coop.md); the power meter, capsules,
Force Field and Mega Crash (`powerups`, `shields`) in
[`docs/dev/powerups-and-shields.md`](../../docs/dev/powerups-and-shields.md); the life cycle, score
and game-feel timers (`scoring`, `fx`) in
[`docs/dev/death-and-scoring.md`](../../docs/dev/death-and-scoring.md); the bosses, the WARNING and
the death sequence (`bosses`, the boss roster of `behaviors`, the boss section of `data`, the
stage brake) in [`docs/dev/bosses-and-warning.md`](../../docs/dev/bosses-and-warning.md); zone A,
`boss.bulwark`, the stage skip and the playtest in
[`docs/dev/zone-a-and-playtest.md`](../../docs/dev/zone-a-and-playtest.md); the
scene stack and flow, the canvas UI kit and the HUD (`scenes`, `ui`, `GameOptions.scenes` of
`game`) in [`docs/dev/scenes-and-ui.md`](../../docs/dev/scenes-and-ui.md); the save, the user
options and the Options screen (`save`, `UserOptions` of `config`, `OptionsScene`, `Choice`) in
[`docs/dev/saves-and-options.md`](../../docs/dev/saves-and-options.md); the debug switches,
controls and counters (`debug`), the debug timing of `Game.frame` and the replays (`replay`) in
[`docs/dev/debug-and-replays.md`](../../docs/dev/debug-and-replays.md); the difficulty presets, rank
growth, per-enemy modifiers, revenge bullets, extends, continues and their two scenes (`rank`,
the presets of `config`, the `rules` kind of `data`, extends of `scoring`, `continueWorld` of
`world`, `DifficultyScene` / `ContinueScene`) in
[`docs/dev/difficulty-and-rank.md`](../../docs/dev/difficulty-and-rank.md); the pattern DSL, its
compilers and interpreter (`patterns/dsl.ts`, `PatternVm`, the `patterns` kind of `data`,
`pattern.loop`), bending lasers, cancel point items and the bullet palette option in
[`docs/dev/pattern-dsl.md`](../../docs/dev/pattern-dsl.md).
`src/math/trig-table.ts`
is **generated** — edit `scripts/gen-trig-tables.mjs`, not the table.

Consumers inside the workspace resolve `@shmup/core` to `src/index.ts` through the
`@shmup/source` export condition (no build needed for dev/test); `dist/` is for `tsc`
builds of dependent packages and any future external consumer.

`test/helpers/alloc.ts` is the **allocation guard** (plan §1.4) of every package — shell,
render-pixi and input-web import it by relative path: `measureHeapGrowth(fn, iterations,
warmup?)` measures the bytes a hot path allocates (V8 `GCProfiler`, without compiled code; needs
`--expose-gc --allow-natives-syntax`, which `vitest.config.ts` passes to the workers as
`ALLOCATION_GUARD_EXEC_ARGV`). Every per-tick entry point (`stepWorld`,
`updatePlayer`, the grid, the stage runner, a 64-enemy World running every mover kind, a World
with 512 live bullets and 16 lasers, a fully powered World firing lasers and missiles from
four Options, a World collecting capsules, equipping the meter and wearing the Force Field down — one and two players, deaths and restarts, a whole boss fight — `hashWorld`, `game.frame` (also slowed and frame-advanced), the debug counters, replay recording and playback, the scene flow through a whole game and 20,000 menu ticks, the UI widgets and the HUD, rank-modified shooters and revenge kills, the difficulty menu and the continue countdown, every Types B–D weapon firing from four Options and the weapon select flying its live preview, the MANTA's family volleys, colour items, Arm and speed toggle and the HUD's tier pips) has a test that keeps it under budget. The helper
runs its warm-up and windows through one loop (every call on a new index, so values derived from
it are new in every window, as in play), lands V8's background compiles before every round
and keeps the steadiest of up to three measured windows (`attempts`, stopping at the first within
`settled` bytes), so the full suite's load does not show in a guard
([docs/dev/build-test-deploy.md](../../docs/dev/build-test-deploy.md#test-concurrency)).
