# test/ — repo-level integration tests

Tests that span several workspace packages or check the repository itself. Unit tests
live in each package's own `test/` folder (never next to sources).

| File | Checks |
|---|---|
| `integration/headless-game.test.ts` | `@shmup/core` + `@shmup/input-web` together: 600 frames → 600 ticks; a remote key press (keyCode only) reaches the simulation as an action, taps between ticks are latched |
| `integration/input-replay.test.ts` | A session driven by real web input (remote keys + a player-2 gamepad) is recorded per tick with `copyInputSnapshot` and replayed into a headless game with identical per-tick input (determinism contract, `shmup_feat.md` §22) |
| `integration/input-profiles.test.ts` | The shipped input profiles of `content/input/` (M1-05) through a real `WebInput`: every key / button a profile binds, in the `game` and the `menu` context, reaches the `InputSnapshot` as exactly the actions the file names (compiled tables, keyboard source, gamepad reader and snapshot agree); a remote session with fake key-up/key-down pairs under `tizen-remote-safe` shows one press and one release to the simulation and replays tick for tick into a headless game |
| `integration/world-flight.test.ts` | Free flight across the packages (M1-06): the shipped KESTREL carries the plan's tunables (D3 speeds, 1.5 px hurt radius, 5×3 terrain box, 8×6 pickup box, 40-tick fly-in) — the same as the core's `DEFAULT_PLAYER_SHIP`; the atlas has a frame for every bank step; a Samsung-remote session under `tizen-remote-safe` flies the ship (fly-in ignores input, 1.5 px/tick, fake key-up/key-down pairs never stall it, the 2-tick release debounce, the left-margin clamp) and replays into a fresh game with an equal `hashWorld` |
| `integration/stage-terrain.test.ts`, `stage-runtime.test.ts` | Stages across content, assets and core (M1-07): every shipped tileset mask equals the opaque pixels of its atlas frame; `test-range` expands into a map drawn with existing frames, its generated tile grid matches a pinned FNV-1a fingerprint (re-pin only on purpose), every shipped stage with terrain (test-range, zone A, the real zones since) leaves a ≥ 48-px flyable corridor in every pixel column and a clear spawn at every checkpoint — since M2-12 measured in the rows the camera shows when the ship reaches the column (zone D dives 200 px into its caves); headless games play it to `stageClear` deterministically, also after a restart at every checkpoint of every shipped stage (a stage with a boss defeats it once it fights — M1-13); the World collides with the tiles the renderer draws |
| `integration/enemies-runtime.test.ts` | The enemies of the shipped content (M1-08), in headless games validated with the engine's script registry: the `test-range` timeline spawns every roster enemy (all eight behaviours, the hatch's children too) within the 64-slot pool, every formation spawns all its members and resolves (no bonus while nothing shoots); ground enemies appear on the generated terrain and walkers stay on the surface over the slopes; killing every enemy on its first on-screen tick yields one `FormationBonus` per timeline formation with the file's bonus and a capsule per carrier and per formation without `drop: null`; two sessions fed the same input hash equal to `stageClear` |
| `integration/bullets-runtime.test.ts` | Enemy bullets of the shipped content (M1-09), loaded with the engine's own sprites like the shell does: over the whole `test-range` timeline the roster fires as specified (turrets aimed round pink 1.5 px/tick on 32 directions, walkers aimed 3-ways of red ovals 48 units apart, orbiters rings of 8 purple), nothing else fires and no laser is ever fired; after every tick at most 512 bullets, all within the view ± 16 px, none that dies on terrain inside solid terrain, all drawn with a resolved sprite; a ship that never moves records bullet hits; Arcade's turret shots are `1.5 × rankScale(6)` fast; two sessions fed the same wandering input keep equal bullet pools and `hashWorld`s |
| `integration/weapons-runtime.test.ts` | The player weapons of the shipped content (M1-10), loaded like the shell does: Type A passes `checkWeaponBehaviors` and fills the four roles (`type-a` preset, `shots/*` sprites, `PlayerShot` / `PlayerMissile` cues, config-driven intervals); the `'full'` loadout plays the whole `test-range` with, after every tick, at most 96 shots, one laser and one missile per ship / Option, every shot within the view ± 16 px (lasers by head and tail), no straight / Double shot inside rock, sliding missiles resting exactly on the floor, the shot batch within capacity and drawn with `shots/*` sprites, four Options while alive, kills credited to player 1 and formation bonuses; the Double plays it in pairs that never refire early; remote mode kills with no button ever held (and the same config without remote mode never fires); lockstep sessions keep equal hashes / shot pools / trails, a different autofire interval diverges |
| `integration/arsenal-runtime.test.ts` | The meter arsenal of M2-03 on the shipped content: presets `type-a` … `type-d` over named weapons drawn with `shots/*` sprites and labelled on the HUD's meter; every Weapon Edit (64 arsenals) flies `test-range` with the full loadout and a weaving pilot, and after every tick holds at most 96 shots, each role's per-shooter cap, no shot outside the view ± 16 px (a blast by its world anchor), beams within their `maxLength`, rings within 20, finite positions, sliding missiles on the floor, straight flyers out of rock and a batch drawn with `shots/*` sprites — every role firing; lockstep zone A sessions for Types B–D; the weapon range is harmless (no enemy bullet over a loop) and the select's preview keeps flying across its restart |
| `integration/options-shields-runtime.test.ts` | The M2-04 Option types, meter shields and Option Hunter on the shipped content: every Option type × `?` shield (20 sessions) flies `test-range` with the full loadout, a weaving pilot pressing Special and god mode — after every tick the Options fly as owned at finite positions within the view ± 64 px, the spread in range, pods at their orbit summing to the shield's hits (one sprite per standing pod, one per field), Reduce's hurt scale matching its hits; lockstep zone A sessions per Option type (the types differ); under `tizen-remote-safe` Ch+ toggles the Formation once per press (repeats and fake key-up / key-down pairs never again) and holding OK spreads the Rotate Options after 15 ticks with one equip; a recorded `hunter-range` run (Snake, Free Shield) with steals plays back without a desync |
| `integration/powerups-runtime.test.ts` | The power meter on the shipped `test-range` (M1-11), in headless games driven only through the input snapshot: the autofiring KESTREL wipes out the first drifter formation, flies to its capsule and one OK press equips Speed; the red carrier drops a capsule where it dies, drawn as `items/capsule` on the `Items` layer; Auto Power-Up grows the loadout in the default order with no button; a mid-stage Mega Crash clears the view of enemies and bullets; a capsule-hunting bot keeps the item pool, batch, meter, loadout and shield in bounds (items inside the view ± 32 px, 300 points per pickup) with equal `hashWorld`s in two sessions |
| `integration/powerups-remote.test.ts` | The power meter under a Samsung remote (M1-11): real `keydown` / `keyup` events through `@shmup/input-web` and the shipped `tizen-remote-safe` profile (OK = PowerUp, 2-tick debounce) into a game on the shipped content — one equip per OK press (auto-repeat and the fake release pairs of a held key never re-equip), OK while an arrow is held equips and the ship keeps moving, OK on an empty meter is denied (`SFX PowerUpDenied`), and the recorded session replays to the same meter, loadout and `hashWorld` |
| `integration/death-runtime.test.ts` | The life cycle on the shipped `test-range` (M1-12), in headless games stepped through `Game.step()`: unattended sessions under each death penalty (`classic`, `arcade`, `casual`) die three times and reach `gameOver` exactly 92 ticks (hit-stop + explosion + dead time) after the last death; after every tick lives never grow and drop by one only on a death, the hit-stop stays ≤ 8, frozen ticks move nothing, respawns fly in from the view's left edge, no hit lands during the 150 invulnerable ticks after control returns, the score (and hi-score) equals what the tick outcomes credited to player 1; `arcade` respawns restart at the last checkpoint with empty pools; two sessions per preset hash equal every tick. A fully powered KESTREL crashed into the terrain on purpose shows each preset's outcome: `classic` one level per death (cursor kept), `casual` only the shield, `arcade` everything plus the cursor and a restart at the checkpoint at x 1500 with the score kept |
| `integration/difficulty-continue-remote.test.ts` | The M2-01 menus under a Samsung remote (key codes only, a real `WebInput`, the shipped content): OK / START open the difficulty menu, Up / Down move and wrap, Back returns to the title menu choosing nothing, OK starts zone A on the chosen preset with the shipped `rules` table's values (HARD: rank 4); a game over with continues left opens the countdown and OK after its lock continues zone A at its last checkpoint (fresh lives, the continue digit, the stage theme queued again, the same World runs on); ARCADE (no continues) goes straight to the game-over screen |
| `integration/boss-runtime.test.ts` | The shipped test boss end to end (M1-13) through `createGame` on `?stage=test-boss`: the fully powered KESTREL (god mode) shoots TRIAL WARDEN down with its shots alone — both shield plates, then the core — through the three phases in order to `stageClear`, with the host's events in order (music stop, dim, three critical siren pulses with WARNING flashes 60 ticks apart, the boss theme 180 ticks after the first, the chain, the final blast, one `BossDefeated` with the 20,000-point tally, the stage-clear jingle) and `stageClear` exactly 185 ticks after the kill; the camera scrolls on afterwards; `renderFrame().world.warning` carries the game's own WARNING text (none of the arcade's phrases); two sessions stay in `hashWorld` lockstep through the whole fight |
| `playtest/zone-a.test.ts` (+ `harness.ts`, `four-way-bot.ts`, `rules.ts`) | The headless playtest of zone A (M1-18): `runStage(stageId, bot, flags)` plays a shipped stage through `createGame` with a bot at the controls and reports the run (status, length, boss kill, deaths, pickups, equips) with its recorded input (`replayStage` replays it); `fourWayBot()` plays like a Samsung-remote player — never two directions, x ≈ 64, up / down to the lowest-danger 16-px lane (bullets, bodies, lasers and terrain scanned in 2-tick slots), OK only when the meter reaches Speed / Missile / Option. With god mode it kills HALCYON BULWARK and reaches `stageClear` in 3–6 minutes with no enemy bullet over 2 px/tick and no two laser lanes closer than 16 px (`rules.ts`); without god mode the run is recorded, replays to the same deaths and hash, and its deaths are printed (not asserted); the debug stage skip reaches the boss within seconds |
| `playtest/harness.test.ts`, `four-way-bot.test.ts`, `rules.test.ts`, `zone-a-recovery.test.ts` | The playtest tooling itself (M1-18 test round): the harness with scripted bots (recorded masks, one observer call per tick, the tick limit, diagonal counting, a one-life crash into zone A's floor reported as a `terrain` death and `gameOver` that replays to the same death and hash — only with the same options —, the flags passed through, `describeRun`); the bot's lane geometry and danger scan (bullets heading into the ship's column, lasers from 12 ticks before the beam grows, zone A's corridor rock) and decisions (nothing during the fly-in, dodging up / down only — also out of an active beam or from a bullet about to hit, regression —, back to x ≈ 64, one-tick PowerUp presses on Speed / Missile / Option only); the 4-way rules on hand-made lasers and bullets (merging, clipping, a beam off the playfield is no lane — regression —, the 20-violation cap); and the recovery rule at runtime: restarted at each zone A checkpoint with perfect play, the capsules dropped before the next source beyond 900 px are exactly the ≥ 3 sources of the window |
| `playtest/zone-b.test.ts`, `zone-c.test.ts`, `zone-bc-recovery.test.ts` (+ `recovery.ts`) | Zones B and C (M2-11): the 4-way bot with god mode clears BRINE NEBULA and DUNE EXPANSE in 3–6 minutes through their bosses' three phases (and zone B's captain) under the 4-way rules; the no-god runs are recorded, replayed and their deaths reported; the recovery rule — restarted at each of the eight checkpoints and played perfectly, ≥ 3 capsules in the 900-px window, all dropped — through the shared `recovery.ts` (`capsuleSources`, `perfectFrom`, `recoveryAt`), which `zone-a-recovery.test.ts` uses too |
| `integration/zones-bc-runtime.test.ts`, `zones-bc-direct.test.ts` | Zones B and C through the real game (M2-11): a practice start at zone B's third checkpoint, the gap into PEARL GROTTO and its clear as the zone's; zone B's music set prepared; SPUME HERALD on the scrolling camera before the WARNING; zone C's worms rising — and the MANTA clearing both zones in Direct mode, the carriers handing out each stage's own item plan in order |
| `playtest/zone-d.test.ts`, `zone-e.test.ts`, `zone-de-recovery.test.ts` | Zones D and E (M2-12): the 4-way bot with god mode clears MAGMA DEEP (its camera reaching the caves, y 200) and TEMPEST RIDGE (kites and jumpers from behind) in 3–6 minutes through their bosses' three phases under the 4-way rules; the no-god runs are recorded, replayed and their deaths reported; the recovery rule at all eight checkpoints (zone D's third down in the caves) through `recovery.ts` |
| `integration/zones-de-runtime.test.ts`, `zones-de-runtime-edge.test.ts`, `zones-de-direct.test.ts` | Zones D and E through the real game (M2-12): the dive at MAGMA DEEP's scroll stop, a practice start in the caves (zone D's music set prepared), the brick maze broken by shots, jumpers and kites overtaking the ship; every practice start of D and E in open space at its camera height, the stage skip into the caves, Arcade restarts in the caves (the maze rolled back) and before the dive (back on the surface, diving again), jumper needles only in view — and the MANTA clearing both zones in Direct mode with each stage's own item plan |
| `playtest/zone-f.test.ts`, `zone-g.test.ts`, `zone-fg-recovery.test.ts` | Zones F and G (M2-13): the 4-way bot with god mode clears CELL VAULT (its shots breaking tissue, the claws lunging) and PRISM LABYRINTH (the cube rush stacking cubes into the terrain) in 3–6 minutes through their bosses' three phases under the 4-way rules; the no-god runs are recorded, replayed and their deaths reported; the recovery rule at all eight checkpoints |
| `integration/zones-fg-runtime.test.ts`, `zones-fg-runtime-edge.test.ts`, `zones-fg-direct.test.ts` | Zones F and G through the real game (M2-13): the prism gallery's `ground` entrance into GLIMMER CACHE through the scene flow from a practice start (the zone's clear, FACET MONARCH skipped), shut when a turret survives (from checkpoint 0 and 1) and opened from the zone's start when all four are shot; tissue growing back but not into a ship; a claw's chain, lunge, pull and retract; the rush stacking and a restart rolling it back; every practice start in open space, the stage skips with every boss part in view, Arcade restarts in the tissue passage (the walls rolled back) and in the gallery (re-armed afresh), a death in GLIMMER CACHE locking the entrance, MANTLE REGENT's launches; the MANTA clearing both zones in Direct mode with each stage's own plan |
| `playtest/zone-h.test.ts`, `zone-i.test.ts`, `zone-hi-recovery.test.ts` | The final zones H and I (M2-14): the 4-way bot with god mode clears IRON CITADEL (the four parade echoes fought, the pistons swinging, emitter lanes, hatch mites, IRON SOVEREIGN's four phases) and ABYSSAL THRONE (the raid followed, both ABYSS ARK phases and THE HOLLOW KING's three, mines armed, eels risen) in 3–6 minutes under the 4-way rules; without god mode the runs are recorded, replayed and their deaths reported; the recovery rule at all eight checkpoints — guide: [`docs/dev/zones-h-and-i.md`](../docs/dev/zones-h-and-i.md) |
| `integration/zones-hi-runtime-edge.test.ts` | Zones H and I through the real game (M2-14): every practice start in open space (the pistons included), both stage skips (zone H's before the parade), an echo outlasting its time limit without the `bossEscaped` flag, and the review fix end to end — a flawless campaign run whose ARK escaped ends with THE FLAGSHIP SLIPS AWAY under the dawn, to the ending theme |
| `integration/attract-flow.test.ts`, `attract-flow-edge.test.ts` | The front end on the shipped content (M2-15): all nine demos played through the scene flow in sync (in zone order, each opening on its zone card), then the high-score tables (the shipped ships named) and every story page; the practice select offering every zone and checkpoint and starting there. `campaign-flow.test.ts` also enters `ACE` in the name entry after a whole run's credits, with the four directions and OK |
| `golden/golden.test.ts` (+ `golden.ts`, `zone-a-*.replay.json`, `gimmick-range-*.replay.json`, `raster-range-god.replay.json`, M2-09 `captain-range-god`, `raid-range-god`, `raid-range-escape`, `twin-range-god`, M2-10 `bonus-range-god`, `bonus-range-digit`, `bonus-vault-god`, M2-11 `zone-b-god`, `zone-b-deaths`, `zone-c-god`, `zone-c-bot`, `brine-grotto-god`, M2-12 `zone-d-god`, `zone-d-bot`, `zone-d-boss`, `zone-e-god`, `zone-e-bot`, M2-13 `zone-f-god`, `zone-f-arcade`, `zone-f-deaths`, `zone-f-boss`, `zone-g-god`, `zone-g-bot`, `zone-g-boss`, `glimmer-cache-god`, M2-14 `zone-h-god`, `zone-h-boss`, `zone-h-arcade`, `zone-h-deaths`, `zone-i-god`, `zone-i-boss`, `zone-i-escape`, M2-16 `zone-a-manta-toggle`, `zone-a-hold`) | The golden replays (M1-19, plan §1.3): zone A runs recorded with `core/replay` — `zone-a-god` (4-way bot, god mode, stage clear), `zone-a-arcade` (Arcade difficulty, no god mode), `zone-a-deaths` (a careless weaving pilot: three deaths, game over), `zone-a-boss` (stage skip, full loadout, Arcade penalty) and the M2-03 arsenal against the boss: `zone-a-type-b`, `zone-a-type-c`, `zone-a-type-d` (full Types B–D loadouts) and `zone-a-edit` (a Weapon Edit, LIFE OPTION), and the M2-04 Option types and `?` shields: `zone-a-rotate` (Rotate Options, Rotate Shield), `zone-a-reduce` (Formation, Reduce), `zone-a-snake` (Snake, front Shield, the whole stage) and `zone-a-free-shield` (the Free Shield, the whole stage at Arcade) — play back into a fresh session with every state hash (every 600 ticks and the final one) and the recorded outcome reproduced. With `SHMUP_GOLDEN_UPDATE=1` (`pnpm golden:update`) it re-records and rewrites the files first — guide: [`docs/dev/debug-and-replays.md`](../docs/dev/debug-and-replays.md#golden-replays-testgolden). Later steps added the MANTA runs (M2-05), the co-op runs (M2-06) and three runs of the M2-07 dev stage `gimmick-range` (`gimmick-range-god`, `-weaver`, `-deaths`), for which the test also checks what the World went through (the branch taken, the trigger, broken cells, terrain rollbacks), and one of the M2-08 dev stage `raster-range` (`raster-range-god`), also played back on the stage with its `raster` / `cycles` stripped — every hash matches, so the effects are presentation only |
| `golden/golden-autofire.test.ts` | M2-16: the autofire modes as golden replays — `zone-a-manta-toggle` (the MANTA in the toggle mode, `Shot` tapped every 150 ticks by `fireButtonBot('tap')`) and `zone-a-hold` (the KESTREL in the hold mode at the fastest rate, `Shot` / `Sub` held in bursts) record the mode with `remoteMode: false` in their headers, clear the stage, fire only as their mode says (the toggle switch flipping per tap, the hold run's shots only while held) and desync when replayed under `'always'` — guide: [`docs/dev/options-rebinding-and-accessibility.md`](../docs/dev/options-rebinding-and-accessibility.md#determinism) |
| `golden/golden-edge.test.ts` | Guards of the golden replays: every file is byte-for-byte what `golden:update` writes, no file without a scenario, re-recording the boss scenario is byte-identical, a tampered replay (other input for a burst, another seed) desyncs at the first hash after the change, consistent expectations, `scripts/golden-update.mjs` runs this folder with the variable the test reads (skipped while re-blessing) |
| `golden/demos.test.ts` (+ `demos.ts`, `content/demos/*.replay.json`) | The attract loop's demos (M2-15): every committed demo — the 4-way bot with god mode, 40 s of each zone — plays back through the attract playback (`createDemoPlayback`) and the golden path (`playReplay`) with **every state hash** reproduced; the content lists exactly `DEMO_SCENARIOS`; the format sample is a valid demo. `pnpm golden:update` re-records them with the goldens |
| `bench/stress.perf.ts` (**`pnpm bench`**, own `bench/vitest.config.ts`, not part of `pnpm test`) | The stress benchmark (M1-19): 20,000 ticks after a 3,000-tick warm-up with 512 enemy bullets, 64 enemies, the full loadout and four lasers kept topped up; prints median / p95 / max ms per tick, heap growth and the average load; fails at a median ≥ 1.0 ms/tick, ≥ 512 KB retained heap growth, or a load under 90 % of the pools. CI runs it after the build |
| `integration/build-info-plugin.test.ts` | The `shmupBuildInfo()` Vite plugin (M1-19): `isDevBuild` (dev server and `--mode development \| test` → dev / test build, everything else release), `buildId` (`SHMUP_BUILD_ID` wins, else the short SHA with `+` for changed tracked files, untracked files ignored, `unknown` without git), the defines as JSON literals, both apps' `build` / `build:test` / `build:dev` scripts |
| `integration/workspace-layout.test.ts` | Skeleton invariants: every package/app has `package.json`, `tsconfig.json`, `README.md`, `src/`, `test/`; no tests inside `src/`; `tools/*` stays out of the workspace; every `content/` folder has a README and an `example.*.json`, and every JSON in it has `formatVersion` 1 and a `kind` |
| `integration/module-layout.test.ts` | Every `src/<module>/index.ts` in every package/app has a module docblock (responsibility, implemented sections, public API), a `moduleInfo` descriptor matching its folder, a mirrored `test/<module>/` folder, and spec references that point at existing numbered sections of `shmup_feat.md` / `shmup_tech.md` |
| `integration/tooling-config.test.ts` | Root tooling: `engines.node` = `devEngines.runtime` and supported by **every installed package** that declares `engines.node` (regression for review round 1 — the old `>=20.19` floor was below the toolchain's); exact pnpm pin; workspace = `packages/*` + `apps/*`; Turborepo tasks; ES2018 strict tsconfigs; TypeScript 6.0 pin; `chrome >= 69` browserslist; CI step order and `ELECTRON_SKIP_BINARY_DOWNLOAD`; `.gitignore` entries |
| `integration/engine-determinism.test.ts` | The M1-01 engine foundations (`rng`, `math`, `events`, `pools`) driven together as a miniature fixed-step simulation: the same seed reproduces the same state hash (committed golden hashes), a mid-run RNG checkpoint resumes identically, cosmetic randomness never moves the simulation, and the pool stays packed inside its budget (`shmup_feat.md` §22) |
| `integration/content.test.ts` | **`pnpm content:check`**: the shipped `content/` files load with `loadContent()` with zero issues, and the foreign kinds (`fx`, `input-profiles`) through their owners; the `example.*.json` samples load as an independent set; every file is named `<folder>/<name>.<kind>.json`; presets use weapons of the matching slot; every shipped weapon has a cue; the JSONC samples in the content READMEs still validate; KESTREL has the D3 speed levels; every sprite name of the shipped content — the particle presets' included (`fxSpriteNames`, M1-14) — exists in the atlas (`findMissingSprites`); zone A (M1-18) keeps its five sections, HB-01's parts, 6–8 enemy types, every aimed `bulletSpeed` ≤ 2 px/tick, a whole HB-01 fight (4-way bot, god mode) without a bullet over 2 px/tick or two laser lanes closer than 16 px, ≥ 12 capsule sources before the boss and ≥ 3 within 900 px after each checkpoint, each section's archetypes (popcorn and carriers; fans on paths and rammers; floor / ceiling turrets, walkers and hatches; orbiters; two carriers in the calm), its terrain (floors, a ceiling only in the corridor, an open boss arena), HB-01's two lanes ≥ 16 px apart around the core's lane in every phase, about three minutes of scroll to the WARNING, and atlas frames for every animated enemy / boss part (hit-flash frames for zone A's); the audio (M1-15): every `SFX_CUES` cue bound, the plan's tracks bound to their cues for every shipped stage, every cue a shipped stage references in its prepared set (`stageMusicCues`) with a track, every synthesized sound audible, unclipped and ≤ 1.5 s, the siren ending before its next wail, whole-screen sounds centred and menu sounds on the `ui` bus, zone A's ≈ 45-s loop after an intro, jingles one-shot, no song clipping, every looping song looping sample-exactly; the naming rule allows the kinds `sfx` and `music` in `audio/`; the zones block (M2-11, B–E since M2-12, B–G since M2-13): each real zone's name, boss, three phases, ≥ 4 checkpoints, own tileset and songs, 2.5–4.5 minutes of scroll to the WARNING, a fast section and a calm with two carriers, 4–6 enemy types new to every zone a run can have flown before, every aimed speed ≤ 2 px/tick (rear attackers and homing bodies too), the capsule budget, ground enemies on rock at the camera height their event fires at, a god-mode boss fight under the 4-way rules with the boss's own lane limit, and each zone's archetypes (zone D's volcanoes, falling rocks, the dive, the brick maze with a gap in every wall, the shield arms on a hub; zone E's rear attackers, weather bands, jagged floors, the chest and minis — [docs/dev/zones-d-and-e.md](../docs/dev/zones-d-and-e.md#the-content-checks-testintegrationcontenttestts); zone F's chasing and dividing cells, regenerating tissue walls with a gap in each, claws on floor and ceiling, the squid's two tentacles and the tentacle-break phase change; zone G's hanging and rising crystal walls, seeded cube rushes, the core behind its crystals, the arms, the `ground` entrance over the gallery's turrets into its own bonus stage with bonus capsules and a 1UP — [docs/dev/zones-f-and-g.md](../docs/dev/zones-f-and-g.md#the-content-checks-testintegrationcontenttestts)); and (M2-13 review) every shipped stage with a `ground` bonus entrance played from its start: no ground enemy of an earlier event standing when the window arms; and (M2-16) `content/strings/en.strings.json` equals the core's built-in UI string table `DEFAULT_UI_TEXT` (the string-table rule — [docs/dev/options-rebinding-and-accessibility.md](../docs/dev/options-rebinding-and-accessibility.md#the-string-table-coreuistringsts-content-kind-strings)) |
| `integration/content-plugin.test.ts`, `content-plugin-edge.test.ts` | The `shmupContent()` Vite plugin and `readContentFiles()`: only its own virtual id, every shipped file in path order, examples skipped, custom roots, JSON errors name the file, byte-stable output, dev-server watcher (full reload only for `*.json` inside the root — not a sibling `content-old/`), and a real Vite IIFE build whose inlined content `loadContent()` accepts |
| `integration/content-wiring.test.ts` | Content wiring: `turbo.json` hashes `content/**` and `types/**` (regression for review round 2 of M1-02), both apps use the plugin and type `virtual:shmup-content`, `content:check` targets the content test, a headless game runs on the shipped content |
| `integration/eslint-rules.test.ts` | The ESLint flat config really enforces the architecture: DOM/WebGL/audio/Node/platform globals and imports, `Math.random`/`Date.now`/`performance.now` and `console` are errors in `packages/core`; APIs newer than Chrome 69 and `import.meta` are errors in shipped runtime code (but `import.meta` is allowed in `apps/web`); exported runtime symbols need docblocks; `tools/` is ignored |
| `scripts/audio-preview.test.ts`, `audio-preview-edge.test.ts` | `scripts/audio-preview.mjs` (`pnpm audio:preview`, M1-15): 16-bit mono WAV encoding (header, an empty sound, clipping and rounding), a cue and a looping song from the shipped content, a full run whose files hold exactly the samples the game's loader prepares (hashes, lengths, intro + loop + loop), the CLI with `--out` / `--only` / `--quiet` and exit code 1 for an unknown option |
| `scripts/clean.test.ts` | `scripts/clean.mjs` deletes only paths inside the working directory and never `node_modules` |
| `scripts/content/tiled-import.test.ts`, `tiled-import-edge.test.ts` (+ `fixtures/tiled-sample.tmj`, `.stage.json`, `.paths.json`) | `scripts/content/tiled-import.mjs` (`pnpm content:tiled`, M2-07): the committed fixture map converts to exactly the expected stage and paths JSON; `cameraYAt` and camera-relative spawn `y` in vertical / diagonal sections (the real `StageRunner` places every imported spawn within 1 px of its object), blocks and triggers in world coordinates, the timed-pan warning, RLE rows, gids and every refused map (flipped tiles, compressed / base64 layers, unknown classes, unnamed polylines, other tile sizes) — guide: [`docs/dev/advanced-stages.md`](../docs/dev/advanced-stages.md#importing-a-tiled-map-pnpm-contenttiled) |
| `scripts/gen-trig-tables.test.ts` | `scripts/gen-trig-tables.mjs` (`pnpm trig:tables`): `--check` / `--out` and their exit codes, byte-identical output across runs, the stale-file error path (staged in a throw-away tree), and that the generator itself uses `Math` only for its self-check |
| `scripts/generate-assets.test.ts` | `pnpm assets` CLI: writes the atlas into `--out`, the second run is skipped by the input-hash cache, `--force` rebuilds identically, `--quiet`, exit 1 listing invalid sources, exit 2 on bad arguments |
| `scripts/generate-assets-edge.test.ts` | CLI corner cases: repeated / `--out=` / dash-named arguments, a relative `--out` below the caller's working directory, a custom `--source`, every issue listed with its count and no stack trace (also for a real-art frame too large for a page — regression), usage only on argument errors |
| `scripts/assets/*.test.ts` | The asset pipeline modules: PNG encode → pngjs decode round trip and chunk layout; packer never overlaps (border + padding), power-of-two pages ≤ 2048², deterministic, spills onto more pages; sprite-source validation paths, Aseprite sidecars (hash/array, tags, pivot, trimmed), PNG overrides; `@flash` silhouettes; the pixel font covers ASCII 32–126 + specials; the whole atlas: frames pixel-exact on the page, 1-px extrusion, the initial sprite set, byte-identical runs, cache invalidation (including a pipeline-script edit after the code was loaded: the long-lived process cache-hits, the next fresh process rebuilds), stale-page removal, `findMissingSprites` |
| `scripts/assets/*-edge.test.ts`, `image`, `rng`, `manifest`, `procedural` | Edge suites per pipeline module: raster helpers (bounds errors, flips, colour parsing); the asset RNG pinned by known-answer vectors and its sfc32 step checked against `@shmup/core`'s `rng`; PNG filter choice per row, CRC, decoding indexed / 1–2-bit / 16-bit / Adam7 PNGs assembled by hand; packer option validation, page-size choice and tie-breaks, exact fits, a seeded fuzz; name patterns, palette limits, sidecar corner cases (a frameless export is an issue — regression), loader error paths; font glyph-key rules and `loadFontSources()`, the pixel font's design rules; the manifest's exact layout and the real manifest's self-consistency; every procedural generator's documented shapes (bullets outlined by the dark rim — regression, terrain slope profiles, seamless star tiles, …); `collectSprites`, oversized frames as source issues (regression), input-hash framing, every stale / damaged cache state, multi-page output with stale-page removal, a failed run keeping the last good atlas, parallel runs into one folder |
| `integration/assets-plugin.test.ts` | The `shmupAssets()` Vite plugin: virtual module with the inlined manifest and relative page URLs, a real IIFE build emits `assets/atlas/main.png`, the dev server serves the atlas, regenerates on asset-source edits and never in-process on pipeline edits (it warns when no restart is coming); wiring (both apps, `//#assets` before `build` / `dev` / `test:e2e`, ignores) |
| `integration/assets-plugin-edge.test.ts` | `shmupAssets()` corner cases: exact virtual id, the dev middleware under the apps' relative base and a custom base, refused names and malformed URLs (404, not 500 — regression), the watcher cycle (edit → module invalidated, regenerated, full reload; invalid edit → error logged, last good atlas kept; add / unlink; sibling folders ignored; config-dependency pipeline edits silent), both app configs list every `scripts/assets/**/*.mjs` as a config dependency (so Vite restarts on a pipeline edit — the premise of the review-round-1 fix), a build failing on invalid sources, and multi-page emission |
| `e2e/boot.spec.ts` (Playwright, **`pnpm test:e2e`**) | Browser smoke tests in headless Chromium with SwiftShader WebGL: the web build (via `vite preview`; since M1-19 `pnpm test:e2e` builds the test builds, `build:test` — release code plus the debug tools) and the Tizen `dist/` (opened from disk via `file://`, one classic script) boot to `data-shmup-state="running"`, the atlas page loads, the canvas screenshot is not uniform and has known pixels (by default the title — `data-shmup-scene="title"`, the logo's colours, the hi-score, no ship; free flight's bitmap title, HUD bar and the KESTREL's hull colour with `?scene=flight`; the sprite showcase with `?scene=showcase`; the calibration border with `?scene=calibration`), no console errors / page errors / failed requests. `pnpm test:e2e` builds both apps first; config `e2e/playwright.config.ts` (Chromium flags and why `DISPLAY` is scrubbed are documented there) |
| `e2e/scenes.spec.ts` (Playwright, `pnpm test:e2e`) | The scene flow (M1-16): both builds boot to the title (the logo, no ship); in the web build **Enter starts the game from the title** (past `PRESS OK`, then START) and the KESTREL flies in under the HUD with the power meter, Esc opens the pause menu over the dimmed, frozen game and closes it again, and Back on the title only backs out of the menu (a browser cannot exit); in the Tizen build opened from disk the remote's OK (13) starts the game and Back (10009) pauses and resumes it without exiting, and with a fake `window.tizen` Back on the title opens the exit confirmation — NO and Back keep the app running, `tizen.application.getCurrentApplication().exit()` runs only after YES. The gameplay specs below open `?scene=flight` since M1-16 |
| `e2e/continue.spec.ts` (Playwright, `pnpm test:e2e`) | Difficulty and continues (M2-01), driven through the test builds' `window.__shmupDebug`: in the web build START → ArrowDown → Enter starts on HARD (rank 4), a game over opens the continue countdown (its red panel drawn over the game) and Enter after the lock continues the same game (3 lives, one continue used, the score's last digit 1); in the Tizen build from `file://` the remote's Back (10009) on the countdown opens the game-over screen; no console errors |
| `e2e/options.spec.ts` (Playwright, `pnpm test:e2e`) | The Options screen and the save (M1-17): in the web build OPTIONS on the title opens the Options screen (`data-shmup-scene="options"`), a MUSIC change is written to `localStorage` `shmup-cup:save.v1` when Esc (Back) closes it, and after a reload the next change starts from the saved level; the canvas carries `data-shmup-boot-ms` below 10 s (shmup_feat.md §23); a corrupt save boots the title with the defaults, is kept under `shmup-cup:save.corrupt` and is replaced by a valid document when the Options screen closes. The Tizen build opened from disk does the same with the remote's key codes only (arrows, OK, Back 10009) and keeps SFX and the CONTROLS profile across a reload — the manual check of plan M1-17, automated |
| `e2e/shell.spec.ts` (Playwright, `pnpm test:e2e`) | The shared shell in the web build: an aborted atlas page request ends on the boot error screen (overlay canvas, `data-shmup-state="error"`, the failure logged) instead of a black canvas; a 1000×600 window gets the ×2 frame centred on the letterbox colour and a resize to 1920×1080 re-fits it to ×5 edge to edge; free flight animates (captures 20 frames apart differ) |
| `e2e/flight.spec.ts` (Playwright, `pnpm test:e2e`) | Free flight (M1-06): after the fly-in, holding an arrow key moves the KESTREL (found by its hull colour, a pixel diff between captures) while it stays put without input; holding a direction stops it at the playfield margin, never over the HUD bars; the Tizen build opened from disk moves it with the remote's arrow key codes (`tizen-remote-safe`) |
| `e2e/stage.spec.ts` (Playwright, `pnpm test:e2e`) | The stage runtime (M1-07) in the web build: `?stage=test-range` shows the generated terrain inside the playfield (never in the HUD bars) and scrolls it left — since M1-19 with the sim frozen (`frame-advance.ts`): captures at World tick 90 and exactly 30 ticks later, a 29–31 px shift; an unknown `?stage=` warns and boots free flight without terrain, no errors |
| `integration/raster-range-runtime.test.ts` | The `raster-range` dev stage across content, assets, core and render-pixi (M2-08): its art matches its effects (the sea band's rows / size / repeat are the wave's, the checker floor's the line-band floor's rows, strips and wrap, the cycle's ramp is the `raster-bands` generator's `SEA_RAMP`); in the packed atlas the sea uses only the ramp's exact colours and nothing else on the cycled layer comes within 1 per channel of them; a headless session of the whole stage drives the renderer's layer effects (fake filters) frame by frame — the sea / floor filter on from the first frame, the far layer's only inside the haze's range, every table within its effect's bounds, the colour pairs following the ramp every 8 ticks |
| `e2e/raster.spec.ts` (Playwright, `pnpm test:e2e`) | The presentation polish (M2-08): the layer shader compiles and links in a real WebGL1 context; `?stage=raster-range` draws the sea's wave and palette cycle and the line-band floor (differing from the same frame with the effects off) within 12 WebGL draw calls, the heat haze only inside its camera range; `stretch` fills a display `integer` letterboxes; the hitbox markers show on the ship only while on — screenshots attached |
| `e2e/display-options.spec.ts` (Playwright, `pnpm test:e2e`) | The display options (M2-08) on the web build (keyboard) and the Tizen build (remote key codes): OPTIONS → SCALE / SHAKE / FLASHES / HITBOX reach the renderer live, are written to the save on Back and applied at the next boot before the first frame; without a save the frame is letterboxed and no marker shows |
| `integration/advanced-bosses-runtime.test.ts` | The advanced bosses (M2-09) on their shipped dev stages through `createGame`, god mode on: CAPTAIN RANGE's four captains in order while the camera scrolls, each short death paying its tally, then the stage's `end`; RAID RANGE's IRON LEVIATHAN (the WARNING, the camera following its segments, its death easing the camera back, LEVIATHAN HEART revealed by the blast, the clear after the heart) and, left alone, its escape after its time limit (`EndingFlag.BossEscaped`, no tally); TWIN RANGE's turns and the survivor's enrage; GAUNTLET RANGE's boss rush; BROOD LAUNCHER's bubbles launched from its tubes; the HP bar over the raid; two raid sessions in lockstep |
| `e2e/advanced-bosses.spec.ts` (Playwright, `pnpm test:e2e`) | The advanced bosses (M2-09): `?scene=flight&stage=raid-range` — IRON LEVIATHAN's hull across the whole playfield, then the camera panned round it; zone A's scene flow with the saved `bossHpBar` option — `BOSS` and the red bar in the top HUD bar during HALCYON BULWARK; a captain fighting while the camera keeps scrolling; the twins on the Tizen build from `file://` (the resting one on the back layer, the swap at the turn) — without console errors or atlas warnings |
| `e2e/gimmicks.spec.ts` (Playwright, `pnpm test:e2e`) | The advanced stage systems (M2-07) in the web build: `?stage=gimmick-range` boots without errors or atlas warnings and draws the destructible brick pillar once the camera reaches it; breaking its cells in the sim takes them off the screen on the next frame (the terrain change log) and the checkpoint rollback draws them again |
| `e2e/enemies.spec.ts` (Playwright, `pnpm test:e2e`) | The enemies (M1-08) in the web build: `?stage=test-range`'s first formation of drifters (their placeholder colours, used by no other sprite) appears inside the playfield, never in the HUD bars, and flies left (since M1-19 stepped in exact ticks: 15-tick steps until they show, then 20); no console errors or unknown-sprite warnings while the timeline spawns |
| `e2e/bullets.spec.ts` (Playwright, `pnpm test:e2e`) | The enemy bullets (M1-09) in the web build: once `?stage=test-range`'s turrets have scrolled in and settled, bullets in the readability palette's body colours appear inside the playfield (never in the HUD bars) and move between two screenshots; no console errors or unknown-sprite warnings |
| `e2e/weapons.spec.ts` (Playwright, `pnpm test:e2e`) | The player weapons (M1-10): in the web build the KESTREL autofires its main shot to the right of the ship (never in the HUD bars) and the shots move; `?loadout=full` draws the Options' orbs and laser beams; the Tizen build opened from disk autofires with no key held and ignores the web-only `?loadout=full` (no orbs, no beams); no console errors or unknown-sprite warnings |
| `e2e/powerups.spec.ts` (Playwright, `pnpm test:e2e`) | The Force Field (M1-11): in the web build `?loadout=full` draws its fresh cyan ring (`shields/force-field` frame 0) around the KESTREL, inside the playfield; the default web boot and the Tizen build opened from disk (which ignores `?loadout=full`) never show it; no console errors or unknown-sprite warnings |
| `e2e/option-hunter.spec.ts` (Playwright, `pnpm test:e2e`) | The Option Hunter (M2-04) in the web build: on `?stage=hunter-range&loadout=full` a hunter spawned on the Options takes all four and is drawn violet (`enemies/option-hunter`) with its grey haul (`options/stolen`); a Mega Crash kills it and frees four grey Options that drift on screen and give an Option each back when flown into; no console errors or unknown-sprite warnings |
| `e2e/lives.spec.ts` (Playwright, `pnpm test:e2e`) | Lives and game over (M1-12) in the web build: on `?stage=test-range` an unattended KESTREL is shot down three times — the bottom bar's stock icons (`hud/life`) go 2 → 1 → 0, the ship vanishes while it explodes and flies back in, and `GAME OVER` (red) replaces the stage title in the top bar once the last ship is out; no console errors or unknown-sprite warnings |
| `e2e/boss.spec.ts` (Playwright, `pnpm test:e2e`) | The boss WARNING and the boss (M1-13) in the web build: on `?stage=test-boss` the camera reaches the `warning` event after about five seconds, the flight scene draws the WARNING band (its red edge rows across the whole width) for three seconds, then TRIAL WARDEN flies in from the right and stays in the right part of the playfield (its `bosses/hull-block` colour — polled after the WARNING, two band-free captures in a row, since a loaded machine runs up to 4 ticks a frame and the autofire could destroy the boss before a fixed late capture); no console errors or unknown-sprite warnings |
| `e2e/zone-a.spec.ts` (Playwright, `pnpm test:e2e`) | Zone A (M1-18) in the web build: the scene flow plays AZURE VERGE; with the debug stage skip `?skip=boss`, Enter past `PRESS OK` and on START reach the WARNING band within seconds, then HALCYON BULWARK (its hull colour) holds the right half of the playfield; no console errors or unknown-sprite warnings |
| `e2e/zones-bc.spec.ts` (Playwright, `pnpm test:e2e`) | Zones B and C (M2-11): zone B's palette-cycled brine sea and, jumped to the end, GALVANIC MAW fighting with its mouth opening; zone C's dune ridge and SANDGRAVE WIDOW; PEARL GROTTO's reef over the sea; the widow on the Tizen build from `file://` |
| `e2e/zones-de.spec.ts` (Playwright, `pnpm test:e2e`) | Zones D and E (M2-12): MAGMA DEEP's volcano peaks and, past the dive, the palette-cycled lava lake risen into the bottom of the view (only its four ramp colours); CINDER BASTION fighting with its shield arms turning; TEMPEST RIDGE's palette-cycled storm clouds over the jagged ridge and SQUALL STEED opening its chest; both bosses on the Tizen build from `file://` |
| `e2e/zones-fg.spec.ts` (Playwright, `pnpm test:e2e`) | Zones F and G (M2-13): CELL VAULT's cell wall and PRISM LABYRINTH's crystal facets recoloured by their palette cycles over the fleshy folds and the spires; MANTLE REGENT curling its tentacles; FACET MONARCH waving its arms; both bosses on the Tizen build from `file://` |
| `e2e/zones-hi.spec.ts` (Playwright, `pnpm test:e2e`) | Zones H and I (M2-14): the citadel wall's cycling lights and IRON SOVEREIGN, the deep's cycling murk and the ABYSS ARK raid, the ending scenes drawn (the flagship sailing off under the dawn, the citadel under its blasts); IRON SOVEREIGN, the ARK and the citadel's ending on the Tizen build from `file://` |
| `e2e/attract.spec.ts` (Playwright, `pnpm test:e2e`) | The attract loop (M2-15) on the test builds, the sim frozen and stepped by exact ticks: web — title → demo (zone A, its World and HUD drawn, in sync) → hi-score tables → story → title on the game's timings, the next round zone B's demo, a key → title; Tizen from disk — the demo starts and the remote's OK returns |
| `e2e/front-end.spec.ts` (Playwright, `pnpm test:e2e`) | The mode select's screens (M2-15): web — PRACTICE → zone B at its first checkpoint with FULL POWER → game over → `BB` typed with the arrows and Enter → the practice table, saved in `localStorage` (the 1P table empty); the sound test playing a library track and a sound, Esc closing it; Tizen from disk — a name entered after a game over with the remote's arrows and OK; no console errors |
| `e2e/rebind.spec.ts`, `e2e/game-options.spec.ts` (Playwright, `pnpm test:e2e`) | The Options pages (M2-16): web — SHOT rebound to J on the rebind screen, saved (`options.input.bindings`), shown in the input test and applied after a reload; a version-1 save boots and is rewritten as version 2 (the old options kept, its co-op row moved), LIVES 5 and ONE BUTTON reach the save and the next game (5 ships, Auto Power-Up, the casual penalty, autofire always), Escape cancels a capture, RESET restores a rebound key, SOCD / DEBOUNCE reach the save; Tizen from `file://` with the remote's key codes — POWER-UP ↔ CH− swapped and kept across a relaunch, LIVES 1 saved on Back and the next game starting with one ship |
| `e2e/fx-gallery.spec.ts` (Playwright, `pnpm test:e2e`) | The fx gallery (M1-14) in the web build and the Tizen build opened from disk: `?scene=fx-gallery` shows its station label (cyan) and, within its first stations, warm additively blended fireball pixels in the middle of the playfield — the screenshot is not blank and is attached to the report; no console errors or unknown-sprite warnings |
| `e2e/audio.spec.ts` (Playwright, `pnpm test:e2e`) | The audio engine (M1-15) in real Chromium with `createBufferSource` wrapped to log every started sound: in the web build on `?stage=test-range` the first key press unlocks audio and the zone theme starts as a looping 22,050 Hz buffer whose loop points are the song's exact sample indices (intro 64 rows × 2,205 samples); in the Tizen build opened from disk (audio unlocked at boot, forced autofire) the shots play as short one-shot buffers; no console errors |
| `e2e/input.spec.ts` (Playwright, `pnpm test:e2e`) | The input profiles in the web build: `keyboard-default` prevents the default of the keys it binds and leaves others alone; `?profile=keyboard-remote-emulation&debounce=2` only knows the remote's keys (arrows, Enter, Backspace, PgUp/PgDn — `KeyZ` passes through); an unknown `?profile=` logs one `Shmup Cup` warning and boots with `keyboard-default`, no errors |
| `e2e/smoke.spec.ts` (Playwright, `pnpm test:e2e`) | The M1 gameplay smoke (M1-19) on the web build and the Tizen `dist/` via `file://`: title → OK, OK → hold → then ↑ for 2.5 s each → `window.__shmupDebug.sceneId === 'game'`, the World ticked, the ship alive or flying in → no console errors; F1 / F2 on the web (overlay, god mode); on the TV build the tools stay locked until Pause, Ch+, Ch+, Ch+ (key codes 10252 / 427), then show the overlay and answer the number keys |
| `e2e/debug-tools.spec.ts` (Playwright, `pnpm test:e2e`) | The debug tools (M1-19): web — F4 freezes the World, each F5 runs one tick, `game.requestStep(n)` exactly n, F7 / F8 move the camera to the next checkpoint / just before the WARNING, F3 / F6 cycle, F4 again runs; Tizen from disk — after the unlock 4 freezes and 5 steps one tick; the `frame-advance.ts` helpers (`freezeSim` holds a dev scene, `stepTo` reaches the exact tick) |

Run: `pnpm test:integration` (part of `pnpm test`, which runs every Vitest project — all
packages + this one — in one process). The playtest's run summaries (`[playtest]
zone-a four-way (god mode): stageClear after … s, …` and the deaths of the run without god mode)
show with the verbose reporter: `pnpm exec vitest run --project integration test/playtest
--reporter=verbose` — guide: [`docs/dev/zone-a-and-playtest.md`](../docs/dev/zone-a-and-playtest.md). The golden
replays (`golden/`) run in the same project; re-bless them with `pnpm golden:update` only for an
intended simulation change (say why in the commit message). The stress benchmark runs with
`pnpm bench` (its own config in `bench/`). Later: the cross-engine determinism check (M2-18 —
the golden replays in Chromium and Firefox, `shmup_feat.md` §22).

Build-output tests live with the apps they build: `apps/tizen/test/build/` runs the real
Tizen Vite build into a temp folder and checks that `app.js` is one classic IIFE script
that parses with acorn as ES2018 and runs in a Chrome-69-like realm without
`globalThis`; `apps/web/test/build/` checks the web build is relocatable (relative asset
URLs, needed by Electron's `app://`).
