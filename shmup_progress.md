# Shmup Cup — Execution Progress

> Tracks execution of [`shmup_plan.md`](shmup_plan.md). One row per plan step, in plan order.
> Maintained by the DOCS agent at the end of each step's pipeline (build → review/fix → tests → docs → CI gate).
>
> **Status values:** `pending` · `in progress` · `done` · `done (review capped)` · `blocked`

## Current state — resume point (2026-09-16)

- **Done: 41 of 44 steps.** M1 (playable vertical slice) and M2 (complete v1.0) are finished; `package.json` is at
  **`1.0.0-rc.1`**. M3-01, M3-02, **M3-02b** (remote & hardware tuning from the 2026-09-15 input-probe run) and
  **M3-02c** (render profiling: the overlay's `REB` / `RT` figures and the Playwright render bench) are done.
  **Next steps: M3-02d and M3-02e** (the render fixes M3-02c's bench now gates —
  [`docs/dev/render-performance-review.md`](docs/dev/render-performance-review.md)), then M3-03 (the last one).
- **`master` = the M3-02c docs commit**, CI green. Working tree clean, no agent worktrees.
- **To continue:** open a new Claude Code session in the repo root and paste [`shmup_prompt.md`](shmup_prompt.md)
  (everything below its line). With `SCOPE = all remaining steps` it runs its preflight and then a single batch of
  M3-02d, M3-02e and M3-03.
- **Board:** GitHub Project [Shmup Cup](https://github.com/users/detain/projects/4) (private) has one issue per step
  (#2 = M1-01 … #41 = M3-03), repo milestones M1/M2 (closed) and M3 (open). Statuses mirror this file. The pipeline
  agents do not update GitHub; the orchestrator re-syncs it after each batch (see the prompt's "Running batches").

### Infrastructure changes made between batches (not plan steps)

- **Test concurrency** (after M2-14): `pnpm test` is one `vitest run` over every project with a single forked worker
  pool (cores − 1, `VITEST_MAX_WORKERS` overrides) and a longest-file-first sequencer. e2e runs `fullyParallel` with
  `max(2, ⌊cores / 5⌋)` workers (`E2E_WORKERS` overrides). CI runs parallel jobs: format·lint, typecheck, 3 test
  shards, build·benchmark, 5 Chromium e2e shards, a Firefox determinism job and the input probe. CI went from
  16–18 min to about 5–8 min. Details: [`docs/dev/build-test-deploy.md`](docs/dev/build-test-deploy.md#test-concurrency).
  Commits `c13f931`, `41f7fb5`, `af4d360`, `9f8b23d`.
- **Allocation guards** now all use one helper, `packages/core/test/helpers/alloc.ts` `measureHeapGrowth`:
  - it runs GC + `%FinalizeOptimization()` before every round, excludes the compiled-code heap spaces, and runs every
    measured window on fresh indices;
  - workers need `ALLOCATION_GUARD_EXEC_ARGV` (`--expose-gc --allow-natives-syntax`);
  - the rules are in [`docs/dev/conventions.md`](docs/dev/conventions.md).
  - Three review rounds; 60+ back-to-back full runs green. Commits `c171189`, `0e42287`, `84c3248`, `ee5ed35`,
    `6c7411b`.
- **Node 24.15+ only** (`db93b10`): `engines` / `devEngines` = `^24.15.0 || >=26.0.0`. The guards fail on Node 22's
  V8, so pnpm now refuses Node 22 (the Windows deploy desktop needs Node 24). `tools/input-probe` keeps `>=20.19`.
- **`pnpm test:e2e` needs both browsers** since M2-18: `pnpm exec playwright install --with-deps chromium firefox`.

### Open items and risks for the remaining steps

- **Tizen bundle size:** `app.js` is at **383.9 KB gzip**; `APP_JS_GZIP_BUDGET` was **384 KB and is now 512 KB**
  (350 → 384 during M2, 384 → 512 before M3-02 — the owner's agreed ceiling, since M3-02 adds an affine-floor
  shader, a pseudo-3D stage, a CRT filter and new bosses and M3-03 adds CJK font atlases). Spend the headroom, do
  not treat it as a target: prefer lean shipped code, lazy content or trimming first. Raising it **above 512 KB**
  needs the owner's say-so. The real constraint is the ≤ 10 s launch, guarded by M2-18's boot-time check.
- **M3-03 has parts no agent can finish:** TV Seller Office submission and alpha test, Steamworks with a real Steam
  app id, LG webOS on real hardware, and Steam Deck verification. The agents should build the code, adapters with
  fakes, scripts and docs, and leave the account- and hardware-only actions as a checklist for the user. That is
  not `blocked`.
- **Follow-up (not must-fix):** the render-pixi debug-overlay allocation guard
  (`packages/render-pixi/test/debug/debug-alloc.test.ts`) cycles its camera over 64 positions, so a value-keyed
  cache there goes unseen. It is debug-only code and not in release bundles.
- **CI warning:** `pnpm/action-setup@v4` targets the deprecated Node 20 runtime (GitHub forces it onto Node 24).
  Harmless for now.
- **Input probe: done 2026-09-15** on both monitors (plan §8.2) — results in
  [`docs/dev/input-probe-results.md`](docs/dev/input-probe-results.md), raw logs in `tools/input-probe/results/`.
  Headlines: the remote sends one key at a time (no diagonals, no move + OK), repeats are flagless keydowns with no
  fake pairs, Back / Play/Pause arrive only on release, Home is only a `blur`, rAF jitters enough to double-step the
  loop. **All of it was applied in M3-02b** (the game pauses on `blur` now, the loop is vsync-locked, the profile
  debounces 0 and carries `singleKey`, nothing asks for a held Back), and the probe itself was fixed (handler-clock
  timings, a raw delta histogram, `NO — not delivered` verdicts). The GitHub board has no issue for M3-02b … M3-02e
  yet.
- **Manual, on the M7 monitors** (no agent can do these): the M1 checks (§8.4), the M2 checks (§8.5) and store
  readiness (§8.6), from the Windows desktop per [`docs/client/install-on-tv.md`](docs/client/install-on-tv.md);
  still open from the probe: a 240-fps latency video and two gamepads at once. M3-02b added its own §8.4 block: the
  overlay's `TPF` counters while flying on both monitors, Home during play, the INPUT TEST's Back ×3 exit, the 240 fps
  videos, and optionally a re-run of the fixed probe. M3-02c added the render review's §4 table **M1–M8** — baseline
  FPS / TICK / RENDER / DRAW / `REB` / `RT` on the title, zone A and a boss, the CRT A/B, the Mode-7 and layer-effect
  entry hitches, the first-dense-pattern hitch, the WebGL1-vs-2 A/B, memory, and whether the app stops rendering under
  the Home overlay; the recipe is
  [`docs/dev/rendering-and-shell.md` § Measuring on the TV](docs/dev/rendering-and-shell.md#measuring-on-the-tv) and
  the numbers land in [`docs/dev/input-probe-results.md`](docs/dev/input-probe-results.md) §11. **Nothing is blocked
  on it** — M3-02d and M3-02e are gated by the headless bench.

### Batch history

| Batch | Steps | Result |
| --- | --- | --- |
| `wf_ad4fced7-5d7` | M1-01 … M1-09 | done (stopped by request after M1-09, once for a reboot) |
| `wf_a877d723-7ec` | M1-10 … M1-19 | done — M1 complete |
| `wf_c7021ba8-423` | M2-01 … M2-14 | done (stopped by request after M2-14 for the test-concurrency work) |
| `wf_b0f423dd-f75` | M2-15 … M2-18 | done — M2 complete, `1.0.0-rc.1` |
| `wf_7ef7476a-6cf` | M3-01 | done (stopped by request after M3-01 to hand over to a new session) |

## Steps

| Step | Title | Status | Review rounds | Tests | Commits | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| M1-01 | Engine foundations: RNG, trig tables, event queue, pools | done | 1 | 871 | 760df29, dc94228, 85c1566 (+ this row fix) | `scripts/gen-trig-tables.mjs` computes both tables with BigInt fixed-point maths (no `Math.*`) and Prettier-formats them, so regeneration is byte-identical on any engine; `--check` / `--out FILE`. The angle constants live in the generated `math/trig-table.ts`; `math` also exports `wrapAngle` and **drops** the planned 16.16 helpers (IEEE `+ − × ÷` and `Math.sqrt` are already deterministic — reason in the docblock). `SimEventKind` became numeric codes + `SIM_EVENT_KIND_NAMES`; `EventQueue` gained `capacity`/`dropped`. `SoaPool<S>` is schema-generic and adds `pendingFreeCount`/`clear()`, with field arrays in sorted name order. `rng.setState` also takes a `Uint32Array`, `RNG_STATE_WORDS` is exported, `callCount` is diagnostic. ESLint: the `**` ban forced re-listing the Chrome-69 `no-restricted-syntax` entries in the core block, and the Node-built-in `no-restricted-imports` moved from `patterns` to `paths` (gitignore-style matching made `events` match the core's own module). **Bug found by the TEST agent:** `drain()` released the whole pending block before visiting it, so a visitor that overflowed the ring had its own pushes visited as pending (and again next drain); slots are now released one at a time, overflow drops the unvisited originals and ends the drain, and `clear()` mid-drain ends it too (regression tests in `events-edge.test.ts`). |
| M1-02 | Content schemas, loader & content module | done | 3 | 194 | 709ee78, 1f843e5, 1fd8b69, 7528d3e, 5c7f5a6 (+ this row fix) | `Schema.parse(value, path, issues, refs?)` takes an optional 4th argument (reference-site collector); `s.object` records a site per `s.ref` field and `loadContent` writes the resolved index into the sibling `<field>Id` (`-1` for null / absent / unresolved), so `s.array(s.ref())` (and, after the tests, `s.record(s.ref())`) throws a `TypeError` at construction — references are wrapped in objects. Added `s.record(value, keyPattern?)` beyond the listed combinators (weapon `params`). `s.object` reports unknown fields and uses `NoInfer` for the optional-key set. Ref kinds: `ship`/`weapon`/`enemy`/`stage` must resolve; `sprite`/`script` are interned (sorted, numbered), `script` checked against `knownScripts` when given (M1-08); `sfx`/`music` resolve against `SFX_CUES`/`MUSIC_CUES`. `db`: `sprites`/`scripts` are `StringTable { names, index }`; lists `ships`/`weapons`/`weaponPresets`/`enemies`/`stages` with `…Index` maps; issue paths `<file>:<json path>`; duplicate ids across files are issues. `CONTENT_MIGRATIONS` ships `0 → 1` for weapons/enemies/stage; the new `player` kind has none (format-0 player → "no migration"); `loadContent` takes a `migrations` override. `pnpm content:check` validates the shipped files and the `example.*.json` samples as two independent sets (+ README JSONC samples); added `content/player/example.player.json` + README and an `example-warden` enemy. Stage events validated: spawn, boss, midboss, warning, music, scroll, checkpoint (`formation`/`branch` in M1-07). `shmupContent()` defaults its root to the repo `content/`, exports `CONTENT_MODULE_ID` and `readContentFiles(root?)`; `types/virtual-modules.d.ts` is also in the root tsconfig for ESLint. Apps register the plugin; the shell imports the module in M1-04. **Review fixes:** duplicate stage ids were reported as `<file>.id` instead of `<file>:id` (round 1); `turbo.json` did not hash `content/**` / `types/**`, so a content-only edit could replay a stale cached `dist/` (round 2 — now in `globalDependencies`). **Bugs found by the TEST agent:** `s.record` dropped a `"__proto__"` key (or replaced the result's prototype for object values) — now an invalid-key issue; its key message read `(must match undefined)` without a pattern; `g`/`y` patterns in `s.str`/`s.record` depended on the previous call's `lastIndex`; `s.record(s.ref())` recorded no ref sites (now a `TypeError`); an absent optional reference got no `<field>Id` although `sfxId`/`mainId` are documented as `-1`; the dev-server watcher reloaded for sibling folders such as `content-old/` (`startsWith` → containment check). Docs: new `docs/dev/content-data.md`. |
| M1-03 | Placeholder asset pipeline (sprites, font, atlas) | done | 2 | 374 | 5ee2fbc, 6838ba0, 91ea178, 52484b4 (+ this row fix) | Frame names are `<sprite>#<index>`; a `hitFlash` sprite gets a whole **sibling sprite** `<sprite>@flash` (same frames and anchor) instead of one `<frame>@flash` per frame, so the renderer swaps a sprite id. Sprite names are the path below `assets/source/sprites/` (`ships/kestrel`, the names content already uses) and a `*.sprite.json`'s `name` must match it. Manifest keys beyond the plan: `sprites` (frame names in index order + `flash`), `animations` per sprite (`animations[sprite][tag]`), fonts carry `sprite`/`cellWidth`/`cellHeight`, glyphs keyed by decimal code point; pages `main.png`, `main-1.png`, … (power-of-two; today one 512×256 page, 55 sprites, 272 frames). `scripts/assets/` has four extra helpers (`image.mjs`, `rng.mjs`, `manifest.mjs`, `pipeline.mjs`); generators use only exactly rounded maths (no `Math.sin`/`cos`). KESTREL = `ships/kestrel` (3 frames) + separate 2-frame `ships/kestrel-thruster`; bullets `bullets/{round,oval,needle}-{pink,red,purple}` (oval/needle 8 direction frames); the 17 terrain tiles are also one-frame animations; the missing-name sprite is `ui/missing`, not `@missing`. `shmupAssets()` runs the cached pipeline in `buildStart` (CI runs tests before `pnpm build`); both apps register it now (the shell consumes it in M1-04). `tsconfig.tooling.json` gained `allowJs`; `pngjs` is used untyped (no `@types/pngjs`); `assets/source/` is Prettier-ignored. `turbo.json`: `//#assets` before `build`/`dev`/a reserved `test:e2e`; `assets/source/**` and `scripts/assets/**` in `globalDependencies`. `check-bundle.mjs` rule 6: every non-widget file must be under `dist/assets/`. `pnpm content:check` checks only the shipped content's sprite names. **Review fix (round 1):** editing a pipeline script while `pnpm dev` ran left a stale atlas that every later run took for current — the input hash now snapshots the pipeline code when the module loads (`CODE_HASH`), and the dev watcher no longer regenerates in-process for `scripts/assets/` edits (Vite restarts the server; a warning when no restart is coming). **Bugs found by the TEST agent:** thin enemy bullets (needle sides, 22.5° ovals) showed body colour on the silhouette edge instead of the §12 dark rim (every edge pixel is now rim-coloured); an Aseprite sidecar with no frames produced a zero-frame sprite and `buildAtlas()` crashed with a `TypeError` (now a source issue); a real-art frame too large for a page escaped the packer as a bare `RangeError` stack trace (now an `AssetSourceError` naming file and frame); a malformed percent-encoded atlas URL made the dev middleware answer 500 (now 404); docs claimed the asset RNG matched `core/rng` (same sfc32 step, different seeding) and the terrain slope-height comment was wrong. Docs: new `docs/dev/asset-pipeline.md`. |
| M1-04 | Rendering foundations & shared browser shell | done | 2 | 144 | 8b17719, 1864e5c, e9420fd, 9554d12 (+ this row fix) | `LayerId` is a numeric const object (`BgFar` … `Debug`, codes 0–13) with `LAYER_COUNT` / `LAYER_NAMES` (§3.4 spellings); `SpriteFlag` = `FlipX`, `FlipY`, `Hidden` (blink), `Flash` (draw the `<sprite>@flash` sibling). `SpriteBatchView` also carries `capacity` and types its arrays `ArrayLike<number>` (SoA pools implement it directly); `createSpriteBatch` / `pushSprite` build mirror views. `ParallaxView` / `TerrainView` are minimal shapes M1-07 draws. `DrawList` stores commands column-wise (`op, x, y, w, h, color, alpha, ref, frame, flags, value`) with `dropped` and `revision` counters (the renderer skips unchanged lists). `TextMetrics` moved from the placeholder core `ui` into `presentation` (`ui` re-exports it). The D20 constants `HUD_BAR_HEIGHT`, `PLAYFIELD_Y`, `PLAYFIELD_W`, `PLAYFIELD_H` were added to core `config` now (listed under M1-06) because the sprite binding needs `PLAYFIELD_Y`; `Game` gained `events: EventQueue` and `renderFrame()` returns the full frame. Sprite ids index a sprite name table set with `renderer.setSpriteNames()`; the atlas numbers each sprite's frames consecutively, so frame = `table[spriteId] + frame`, checked against `framesLeft`; unknown names and out-of-range frames draw `ui/missing` (not `@missing`); a flash table serves `SpriteFlag.Flash`; a stale page (image size ≠ manifest) is rejected. `sprites` also exports an ordered `QuadPool`: the HUD and UI layers each own one of 1024 quads (the planned glyph pool) and the `ui` `DrawListView` draws every command type into it in order; `text` exposes `drawText` / `drawNumber` into a `GlyphSink` (numbers capped at `MAX_SAFE_INTEGER`). The renderer gained `atlas`, `testPattern`, `font`, `glyphCapacity`, `setSpriteNames()` and `bindWorld()` (bindings created when a new `WorldView` object appears; the shell pre-binds its scene at load); the test pattern shows only with `?scene=calibration`. The shell adds `frame-loop` (moved out of both apps, which each had a copy) and `showcase` (the default scene) to the planned modules; `bootShell`'s `platform` option is a factory `(renderer) => Platform`; extra options `scene`, `audioUnlock`, `preferWebGLVersion`, `contentOwners`, `createImage`, `overlay`. The progress bar and boot error screen are drawn on a separate 2D **overlay canvas** (a canvas that had a 2D context can never get WebGL, and the error screen must work when WebGL failed); the game canvas carries `data-shmup-state`; foreign content kinds with no owner are issues. `bootWebApp` / `bootTizenApp` take `(canvas, resources, win)` with the virtual modules from `main.ts`; the Tizen Back watcher is installed before boot, so Back also exits the boot error screen. `check-bundle.mjs` rule 7: at least one page under `dist/assets/atlas/`. `pnpm test:e2e` runs `turbo run build` for web and tizen, then Playwright (`@playwright/test` root devDependency; the reserved turbo `test:e2e` task stays unused); Chromium runs with `--allow-file-access-from-files` and without `DISPLAY`; `test-results/` and `playwright-report/` are git- and Prettier-ignored; new CI job `e2e`. **Review fix (round 1):** `renderer.render()` built two pass-option literals per frame — both are now created once, and `resetPass()` restores the fields Pixi writes into them (`target`, `clear`, `clearColor`, cached `transform`) before every call (mutation-checked test). **Bugs found by the TEST agent:** a draw-list `sprite` command flagged `SpriteFlag.Hidden` was still drawn (the quad pool ignores `Hidden`) — `createDrawListView` now skips it like world bindings do; the quad pool assigned `sprite.tint` on every pass and Pixi's tint setter allocates before comparing, so a HUD redrawn every frame allocated ~1 KB per frame — `setTint()` only writes a changed tint. Docs: new `docs/dev/rendering-and-shell.md`; `docs/client/preview-build.md` rewritten for the showcase and the boot error screen. |
| M1-05 | Remote-first input profiles | done | 1 | 166 | ab7bd56, e12a6d2, ac0b4a0 (+ this row fix) | The profiles file is `content/input/remote.input-profiles.json` (not `remote-profiles.json`: `pnpm content:check` enforces `<folder>/<name>.<kind>.json`), with a folder README and `example.input-profiles.json`; labels are upper-case for the bitmap font. Bindings: remote game OK = PowerUp, Back = Pause (D12), Play/Pause = Pause, Ch+ = Special, Ch− = Speed; remote menu OK = Confirm, Back = Back, Play/Pause = Pause. Remote profiles bind by `keyCode` only. `keyboard-remote-emulation` is `device: 'remote'`, debounce 2, `lastWins` for diagonals **and** SOCD, PgUp/PgDn = Ch±. Profiles register only D14's three keys; the colour keys stay in the no-profile fallback `REMOTE_KEYS_TO_REGISTER`. Validation beyond the schema: every `game` table binds the directions + Pause, every `menu` table the directions + Confirm + Back; gamepad profiles bind buttons only with debounce 0; only remote profiles register keys, never `Exit`/volume (`SYSTEM_REMOTE_KEYS`, also filtered by Tizen `registerRemoteKeys`); ids unique lower-case kebab; a bad profile is dropped, the others kept. API beyond the plan: `loadInputProfiles(files)`, `createInputProfileRegistry()` (its `load` is the content owner the apps pass to the shell), `chooseInputProfile`, `overrideInputTuning`, `loadInputProfileChoice` / `saveInputProfileChoice` (storage key `input.profile`); compiled tables list keys bound only in the other context with mask `0` (tracked and `preventDefault()`-ed in both contexts) and `keymap.findKeyActions` returns `-1` for unbound keys; the placeholder types `RemoteTuning` / `DeviceBindings` were replaced by `InputTuning` and the profile types. Debounce: a key released between polls N and N+1 stays held through poll N+ticks and is released on poll N+ticks+1; the keyboard source tracks up to 32 keys in fixed slots and `advance()` runs first in `WebInput.poll()`. A key or pad button held across a profile or context switch keeps only the actions common to both tables until released (`GamepadReadState` gained optional `pressedButtons` / `staleButtons`, `readGamepadActions` a `previousButtons` parameter). `ShellInput.setContext` is required; the shell loader exports `DEFAULT_CONTENT_OWNERS` (`input-profiles` → input-web), merged under `contentOwners`, so `@shmup/shell` now depends on `@shmup/input-web` (§3.1 allows it). Apps apply profiles in the platform factory after content validation; `createTizenPlatform` takes `registerKeys`; a saved choice is applied once storage answers (on Tizen its keys are registered again); the web app exports `inputOverridesFromSearch` and an unknown `?profile=` logs `console.warn` and falls back to the default; both apps expose `app.profiles`, `WebInput` exposes `context` / `keyProfile` / `gamepadProfile`. **Bugs found by the TEST agent:** `keymap.findKeyActions` — when a profile bound a key by code in one context, the other context's `0` placeholder for that code hid its own keyCode binding of the same key (Enter by code in the game and keyCode 13 in menus left OK / Enter dead in menus); a `0` code entry now falls through to the keyCode table. `remote.createReleaseDebouncer` — a `NaN` capacity produced a zero-slot debouncer reporting capacity `NaN` (`Math.max(1, NaN)` is `NaN`); it now gets one slot. Docs: new `docs/dev/input-profiles.md`; `docs/client/controls.md` rewritten for the game / menu tables, the control profiles and `?profile=keyboard-remote-emulation`. |
| M1-06 | Sim world, tick pipeline, player ship & collision | done | 1 | 145 | fdffe6b, 089831f, 5fae090 (+ this row fix) | `createSpatialGrid(width, height, cellSize = 32, capacity = 256)` instead of the plan's leading default; per tick `begin(originX, originY)` → `insert` → `build()` → `query`. Grid queries test the stored boxes exactly (closed), so they *equal* brute force rather than returning cell candidates; boxes outside the area clamp into the border cells and boxes spanning more than 9 cells go to an overflow list every query scans (no silent misses, no allocation). All shape tests are closed (touching = hit); extra exports `pointSegmentDistanceSq`, `COLLISION_MASKS`, `layersInteract`. `PlayerShip` adds `slot`, `active` (P2 inactive until M2-06), `lives` and `moving` (the D26 Option-trail flag); phase 1 writes a `PlayerIntent` per player (`world.intents`, masks + `moveX`/`moveY`, opposites cancel); extra API `createPlayer(slot, lives)`, `createPlayerIntent`, `spawnPlayer`, `setPlayerState`, `readPlayerIntent`, `playerBankFrame`, `resolvePlayerShip` (`kestrel` › first ship › `DEFAULT_PLAYER_SHIP`, the built-in fallback for an empty content DB, not drawn). Fly-in: camera-relative x −24 → 64 at mid-playfield, cubic ease-out over the content's `enterTicks`; `respawning` flies in the same way; banking moves one step per tick; only `ships/kestrel` is drawn (the thruster sprite is not referenced by content). `WORLD_PHASES` is a frozen array of `{ phase, name, runsDuringHitStop, run }` (`WorldPhase` 0–8, `WORLD_PHASE_NAMES`); only `input` and `fx` run during hit-stop. The camera has a scroll velocity `vx`/`vy` (0 = static) applied in phase 3 — the hook for M1-07's stage runner; the World also carries `grid` (camera view + `GRID_MARGIN` 64 px), `playerBatch`, `ship`, `config`, `content`; `WorldStatus` = `playing \| bossWarning \| stageClear \| gameOver`; the pool registry has `register`, `flushAll`, `clearAll` (field arrays in sorted name order for the hash). `hashWorld` also covers camera velocity, status, hit-stop and each player's `active` / `lives` / `moving`; one small allocation remains (the returned 32-bit value boxed into a 16-byte heap number when it is not a small integer) — call it every few ticks, not per entity; `createDebugFlags()` added. The allocation guard `measureHeapGrowth(fn, iterations, warmup?)` uses V8's `GCProfiler` and adds back the bytes collected during the loop, so it measures all allocation, not only retained growth; `defineShmupProject` gained an `execArgv` option (`--expose-gc` for core and shell). Shell: new `flight` module is the default scene (the World over a drifting starfield + D20 HUD), the showcase moved to `?scene=showcase`, the calibration scene renders the game frame without the World; `Shell.flight` added. The D20 playfield constants already existed from M1-04. **Bugs found by the TEST agent** (all per-tick / per-frame allocations, each with a regression test): world phase 6 passed the fractional camera position to `grid.begin()` and V8 boxed each fractional argument (16 B per scrolling axis per tick, ~32 B/tick with two-axis scroll — over the 256 KB / 10,000-tick budget once M1-07 scrolls); the grid origin is now floored (it only picks cells, queries stay exact) — `world-edge.test.ts`. `updatePlayer`'s diagonal-vs-straight speed ternary merged a tagged array element with a computed double, boxing a heap number on every diagonal tick with the frozen `DEFAULT_PLAYER_SHIP` table; now always `speeds[level] * (diagonal ? DIAGONAL_SCALE : 1)` (× 1 is exact, bit-identical) — `player-edge.test.ts`, `world-edge.test.ts`. `createFixedStepLoop` (found through `game.frame`) kept the accumulator and last timestamp in closure variables, re-boxed on every fractional assignment (~32 B per frame on a real rAF clock); they now live in a `Float64Array`, behaviour unchanged — `loop-edge.test.ts`, `game-world.test.ts`. Docs: new `docs/dev/sim-world.md`; `docs/client/preview-build.md` rewritten for free flight (how the ship should respond, what to check on the monitors). |
| M1-07 | Stage runtime: camera, timeline, checkpoints, terrain, parallax | done | 2 | 92 | dbff387, 512395e, 09b2d05, d5dfa7f (+ this row fix) | Stage schema: `music: { stage, boss }` cue refs; `lock` is a boolean; the first camera key must be at x 0, keys / checkpoints strictly sorted, events non-decreasing (ties in file order), nothing past `length`, `yTicks` needs `yTo` — all load issues. Event types are exactly the M1 list: the stub's `midboss` (back in M2-09), `scroll` (→ `speed` with an optional `ramp`) and inline `checkpoint` (→ the `checkpoints` array) were removed; `spawn` lost `formation` / `count` (the `formation` event has `count` + `interval`); `flag` names are numbered per stage (`flagNames` sorted, `flagId` = bit, ≤ 32). The identity 0 → 1 stage migration stays (format-0 stubs now report schema issues). Camera semantics: a key applies at the start of the tick the camera reaches it, events fire on that tick itself (so a key overrides a `speed` event at the same x, except at x 0); linear speed ramps, `inOutQuad` pans, lock keys stop the camera **exactly** at their x until `unlock()`, then it resumes at the key's speed; never past `length`. New content kind `tileset` (one per file; tiles carry `name`, `frame`, `type` empty / solid / hazard, `anchor`, 8-column `mask`; tile id = index + 1; `tileSize` fixed at 8) — the per-tile `frame` replaces the plan's "tile n draws frame n − 1". `tilemap.generator` is an object `{ type: 'heightfield', segments }`; expansion is a third `loadContent` pass into `StageSpec.terrain` (map width `ceil((length + 384) / 8)`); the generator picks the tiles named `solid` / `floor` / `ceiling` and matches slopes by anchor + mask; floor wins over ceiling; RLE rows (`<count>*<id>` tokens) may be combined with the generator and overwrite it. Terrain queries take the map first; added `terrainAt` (type code) and `terrainRectHit` (whole pixels, used by the World to avoid V8 argument boxing); `boxHitsTerrain` returns the highest `TerrainType` (0 = none); terrain tests are pixel-exact and half-open; the placeholder `TerrainQuery` interface was replaced by `TerrainMap` + functions. Runner API `createStageRunner(stage, hooks, camera?)`, hooks `event(code, event, index)` + `clear()`, `restartAt(checkpointIndex)` (-1 = start) re-derives speed / pan / flags from the data; a class with the timeline compiled into typed arrays and all state in one hashed `Float64Array` (`STAGE_STATE_SLOTS` 19) — per-instance closures and megamorphic content-object loads had kept `tick()` unoptimised and allocating. New sim option `GameConfig.stage: string \| null` (default `null` = free flight); `createWorld` throws `RangeError` for unknown ids (`resolveWorldStage`); `World` gained `stage`, `terrain` (private copy), `parallax`; hooks: `music` → `SimEventKind.Music` (the stage theme is queued at creation), `end` → status `stageClear`, `clear` → `pools.clearAll()`. The camera became a class instance (`createStageCamera()`) because V8 shared a literal hidden class with the new camera-key schema literal and boxed its fields. `playerHit(ship, cause, tick, debugFlags)` lives in `core/player` with numeric `PlayerHitCause`; it records `hitCause` / `hitTick` / `hits` (hashed) and ignores inactive, not-alive, invulnerable and god-mode ships (no death until M1-12). Render contract: `ParallaxView` gained `spacing` (`y = baseY − camera.y·factor`, `offsetX = camera.x·factor mod spacing`), bands only on `BG_FAR` / `BG_MID`, ≤ 8, horizontal repeat only; `TerrainView` gained `tileFrame`. Renderer terrain grid is **49 × 26** (one extra row for vertical pans, capped at the map's rows), ring-buffered; `TerrainBinding.sync(view, camera)` takes the camera object; `PixiRenderer.terrain` / `.parallax` added. Shell flight scene uses the stage's parallax / terrain instead of its starfield and shows the stage name; `apps/web` `?stage=<id>` via `stageFromSearch` / `contentStageIds` (unknown ids warn); Tizen has no stage parameter. `test-range` has no spawn events yet (no shipped enemies until M1-08); the example stage was rewritten to the new format. **Review fixes (round 1):** `restartAt` applied a camera key and an event at the same x in the opposite order from live play, so the restart speed was wrong (live 3 vs restart 1) — the re-derivation now replays live order (event before key on a tie) and applies the runner part of the events at exactly the checkpoint x as live play did on arrival; they re-fire next tick for the hooks only (new `StageSlot.Replay`, slots 18 → 19); documented approximation: a key less than one tick's movement before a speed event is replayed key → event. A lock key was overshot when a non-lock key lay within one tick's movement before it (camera 102 instead of 101) — `compileStage` builds `keyNextLock` and step 3 clamps against the first pending lock key. **Bugs found by the TEST agent:** `restartAt` re-derived a tie between the first camera key and a `speed` event at x 0 as event → key, but live play applies the key at 0 first (tick step 1) and fires the events at 0 afterwards (step 4), so a restart at any later checkpoint used the first key's speed instead of the event's (live 0.5 px/tick vs restart 1) — the key at 0 now goes first in the merge (regression test, randomised live-vs-restart property over 60 stages). Test infrastructure: render-pixi `measureAllocation` now collects garbage before the warm-up too and takes the steadiest of 3 windows — the M1-07 layer allocation guards (and a renderer one) failed about 1 run in 8 from engine tier-up noise although the steady state allocates nothing. Docs: new `docs/dev/stage-runtime.md`; `docs/client/preview-build.md` gained the scrolling test stage (`?stage=test-range`) section. |
| M1-08 | Enemies, behaviour scripts & movement | done | 1 | 131 | 7963753, ef92c75, 1de307a, ce8e012 (+ this row fix) | New content kind `paths` (`content/paths/*.paths.json`, 2–64 points, ≤ 16,384 px): a **centripetal** Catmull-Rom spline (no cusps between close points) baked at load by `core/data` `bakePath` into a 1-px arc-length table relative to its first point; the `path` mover translates it to where it starts and continues along the end tangent past the last point. The enemy spec gained two fields the plan lacked — `params` (behaviour tunables by name) and `child` (the enemy a spawner releases) — and its optional fields get defaults at load (`anim` 1 frame, `mover` null, `ground` null, `settleTicks` 30, `explosion` small, `megaCrashImmune` false); `drop` is now `"capsule" \| null`; stage `path` is a `paths` ref (`pathId`); `spawn` / `formation` gained `screenX` (default 400 = 16 px past the right edge; negative = behind), `formation` gained `drop` (default capsule) and `bonus` (default 0); `homing.turnRate` is whole binary units. Weapon and enemy behaviours share one script table, so `KNOWN_SCRIPT_IDS` = the eight enemy behaviours ∪ `WEAPON_SCRIPT_IDS` (the four M1-10 ids, kept in `behaviors` because a placeholder module may not export runtime values — M1-10 moves them to `weapons`); the shell's `loadGameContent` passes it by default and appends `checkEnemyBehaviors` (unknown tunables, spawners without a child), as does `pnpm content:check`. New module `behaviors` (`defineBehavior`, `createBehaviorRegistry`, `DEFAULT_BEHAVIORS`, the M1 roster `drifter.sine`, `fan.loop`, `carrier.straight`, `turret.floor`, `walker.floor`, `hatch.spawner`, `rammer.aimed`, `orbiter.loop`); `createWorld(config, content, { behaviors })` takes an optional registry (tests). The 64 enemies are `Enemy` class instances in fixed slots taken lowest-free-first (states Free / Live / Removed, freed in phase 8) instead of `createPool`, which has no deterministic iteration order. Flying enemies ride the camera from the camera position they last saw (`camX` / `camY`), so an enemy spawned in phases 3–4 after the tick's camera move is not moved twice; ground enemies are world-anchored and snap to the floor below / ceiling above their spawn point. No separate pending-spawn ring: the 32-slot formation table stores each formation's next spawn tick; a member that cannot spawn counts as escaped. Followers replay the leader's recorded 256-entry `FollowTrack`; a leader killed or escaped while members are still out becomes an invisible, intangible **ghost** that keeps recording until the formation resolves (or is 128 px outside the view). Despawn: seen enemies escape 32 px outside the view, never-seen ones are removed 128 px outside or after 600 ticks. New `SimEventKind.FormationBonus` (7) and a new `FX_CUES` registry (explosion particles); each tick's kills, drops and bonus points are recorded in `EnemySystem.outcomes` for M1-11 / M1-12. Hurtboxes go into the World grid with whole-pixel bounds, contact is an exact circle-vs-box test → `playerHit(Contact)`, at most one accepted hit per ship and tick; `hashWorld` covers every enemy slot and the formation table. Allocation: D29's generators still allocate one object per spawn and V8's `{ value, done }` result per wake (≈ 40 B); with sleeping scripts the 64-enemy guard measures ≈ 5 KB over 10,000 ticks (budget 64 KB) after several hot-path fixes (no fractional arguments across non-inlined calls, `atan2B` on scaled integers, `\| 0` on `Math.ceil` bounds against −0, inlined path sampling / sprite push / track record). An `{ x: <schema>, y: <schema> }` object literal made every other `{ x, y }` literal allocate on fractional writes (the render-pixi terrain guard went over budget), so the path point shape is built without a literal. Content: the `test-range` roster (10 enemies, all eight behaviours), three paths and a timeline using them; new pixel-map sprite `enemies/hatch`. The e2e stage test compares frames 30 apart instead of 60 (with enemies drawn and parallel e2e workers the loop ran up to 4 ticks per frame and the scroll left the 250-px search window); existing tests updated for the enemy batches, the new event kind, path refs and the hatch sprite; the long 64-enemy tests got explicit timeouts after a CI timeout. Docs: new guide `docs/dev/enemies-and-behaviors.md`; client guide to the test stage's enemies in `docs/client/preview-build.md`. **Bugs found by the TEST agent:** `patterns` `moveWaypoint` — a waypoint with `hold: 0` paused one tick like `hold: 1` (arrival always entered the hold phase); it now leaves on the tick after arriving. `enemies` `spawnEnemy` — `EnemySystem.spawn` / `ScriptApi.spawn` with a fractional spec index passed the range check, read `undefined` from the spec tables and threw a `TypeError` instead of returning `null`; non-whole indices are now rejected (regression tests in `patterns-edge.test.ts` / `enemies-edge.test.ts`). |
| M1-09 | Enemy bullets, lasers & attack patterns | done | 1 | 107 | 1a5fd50, 0ce875f, 236a832 (+ this row fix) | Bullet kinds are a built-in core table (`BULLET_KINDS`: round / oval / needle × pink / red / purple over M1-03's art, hit radius 2 / 2 / 1.5, 8 directional frames for ovals and needles), not content — the M2-02 pattern DSL brings content. Their sprites and the laser beam are **engine sprites** (`BULLET_SPRITES` = core `world` `ENGINE_SPRITES`), interned through a new `loadContent` option `extraSprites`; the shell's `loadGameContent` passes them by default and `pnpm content:check` verifies them against the atlas (without them bullets simulate but are hidden). `World.bullets` (`createBulletSystem(world)`) registers two SoA pools, `enemyBullets` (512) and `enemyLasers` (16) — flushed in phase 8, hashed; the bullet pool **is** the `ENEMY_BULLETS` sprite batch and the laser pool the new render-contract `LaserView` (`WorldView.lasers`, optional). Pool fields: the plan's `anim` became `frame` + `kind`; added `turnRate` / `homing` (capped homing), `draw` (sprite flags) and internal `AimOnLaunch` / `Dead` flag bits (a removed bullet stays in `[0, count)` until phase 8). Bullets and fixed lasers **ride the camera** (`x += camera.dx`) like flying enemies, so patterns keep their shape and aimed shots stay aimed while the stage scrolls; culling is exact at view ± 16 px, terrain one `terrainAt` lookup. `spawnBullet` / `fireLaser` / `cancelAllBullets` take anything with a `.bullets` system (the World); `fireLaser(owner, src, angle, length, telegraph 40, grow 8, active 60, width 6, fade 8)` takes positional timings instead of the plan's options object (no per-call allocation); the source is a `LaserSource` `{ slot, x, y }` (an `Enemy` works; slot -1 = fixed); attached lasers detach when their enemy is removed or turns ghost (warning / growing ones vanish, active ones fade). Fire primitives are functions in `core/patterns` taking `(bullets, BulletOrigin, …)` — `fireAimed`, `fireNWay` (`step` = the gap between neighbours), `fireRing`, `fireSpiral` (returns the next angle — the script-held state), `fireStack`, `fireSpray` (two gameplay-RNG draws per bullet), `fireHoming`, `fireDelayed` — plus `rankedWait`; the enemy `ScriptApi` wraps them (`aimed`, `nWay`, `ring`, `spiral`, `stack`, `spray`, `homing`, `delayed`, `laser`, `fireWait`, `bullets`) and applies the fire rule itself (off screen / not settled / ghost / not live fires nothing). New sim option `GameConfig.aimDirections` (default 32, a power of two 4–1024) quantises aimed shots. Lasers hit with `PlayerHitCause.Laser` (the plan said `playerHit('bullet')`); at most one accepted bullet hit and one laser hit per ship and tick; an accepted bullet is removed. `CancelMode` has only `Sparkle` (points: M2-02); cancel also removes cancelable lasers; new `FX_CUES.BulletCancel` (3), at most 64 sparkle events per call, evenly spread. Rank (`core/rank` partial): `DIFFICULTY_RANK_BASE` easy 0 / normal 2 / hard 4 / arcade 6; `RankCurve { perRank, perRankSq }` is exactly 1 at Normal, so content speeds and intervals are Normal values; `World.rank` (hashed) sets `bullets.speedScale` / `fireScale`. Render: render-pixi `createLaserBinding` uses two sprites per slot (a warning line tinted once, a beam) and picks the beam frame of the rounded width, because Pixi's tint and fractional-scale writes allocate; new procedural art `lasers/beam-<colour>` (8 frames of 4×8 px, frame k a band k + 1 px tall); the flight scene passes the laser view through. Roster tunables: `turret.floor` `fireTicks` 90 / `bulletSpeed` 1.5 (counted in `aimTicks` steps); `walker.floor` `spread` 48 / `bulletSpeed` 1.25; `orbiter.loop` `ringTicks` 120 / `ringCount` 8 / `bulletSpeed` 1; no shipped content fires a laser yet. Allocation fixes found with V8 tracing: the collision code copies the ship position into class fields and caches the hurt radius (loads through `ship` / `host.ship` allocated); the bullet and laser loops share one `update()` method (a small wrapper stayed in Maglev and inlined the laser loop, which allocated); the allocation guards (512 live bullets + 16 lasers, pool churn, delays / changes / detach / cancel) moved to their own test files with a 20k-tick warm-up. Docs: new guide `docs/dev/bullets-and-patterns.md`; `docs/client/preview-build.md` gained "Enemy bullets" (who fires what, what good looks like, troubleshooting). **Bug found by the TEST agent:** `core/bullets` — a bullet whose position became NaN (NaN speed or acceleration) was never removed and counted as a hit on every ship it was checked against, keeping its pool slot forever; a laser with a NaN origin hit every tick. Fixed by rewriting the off-screen check and both contact checks so NaN counts as outside / as a miss (same results for finite numbers); regression tests in `bullets-edge.test.ts`. |
| M1-10 | Player weapons (Type A) & Options | done | 3 | 65 | ce5d00e, 9529d69, 8f45ebd, 35e5811, 2578812 (+ this row fix) | `type-a.weapons.json` lost `refireTicks`: new sim options `GameConfig.autofireInterval` (4) / `missileInterval` (10, both 1–60, replay-recorded) pace Type A and a weapon's `refireTicks` overrides them; `shot.double` draws `shots/double` (the forward shot of the pair uses the main shot's sprite, offset and hitbox). Behaviour tunables live in content `params` with defaults (`WEAPON_BEHAVIOR_PARAMS`: `ox`/`oy`, `hw`/`hh`, `angle`, `maxLength`, `hitCooldownTicks`, `slideSpeed`, `frames`); new `checkWeaponBehaviors` (non-weapon behaviour, unknown params, wrong slot) runs in the shell's loader and `pnpm content:check`; `WEAPON_SCRIPT_IDS` moved to `core/weapons` (`behaviors` re-exports it). Roles come from preset `type-a` (else the first preset, else the first weapon per slot); content without weapons fires nothing (no built-in arsenal). New `GameConfig.loadout: 'default' \| 'full'` applied at world creation (`applyLoadoutPreset`; full = speed level 2, Missile, Laser, four Options); `Loadout` is a class `{ main, missile, options, shield }`, the speed level stays on `PlayerShip`; firing needs no button when `autofire \|\| remoteMode`; `apps/web` reads `?loadout=full` (`loadoutFromSearch`). Shooter id = `player × 5 + k` (k 0 = ship, 1–4 = Options); caps per shooter and role recounted each phase 2; per-shooter autofire timers restart only when something fired; shots ride the camera. Laser: head `x` + `length`, follows its shooter's `y`, terrain stops the head (then it shrinks away), whole tail→head hitbox, drawn as 8-px `shots/laser` segments in a 192-slot `PlayerShots` mirror batch. Missile: lands on bottom-pixel terrain contact, slides screen-relative re-snapping with `findFloor` (step `ceil(slideSpeed) + 1`), dies at walls, falls again over cliffs. Hits found in phase 6 (non-piercing: lowest overlapping enemy slot; piercing: every one off cooldown, armour always) and applied in phase 7 (a hit on an enemy killed earlier that tick is skipped, the shot flies on); per-shot cooldown tables are a pool of `PIERCE_TABLES` (32) × 64-entry `Uint8Array` tables (shot field `table` = index + 1) instead of one per shot slot — a piercing shot with no free table is not fired. `EnemySystem.damage` / `kill` gained `by` → `EnemyOutcomes.killBy`; new `SFX_CUES.Clink` (21); SFX rate-limited per cue and pushed at whole pixels (fractional event arguments were boxed — the allocation guard caught it). Options (`core/options` `OptionGroup` class: 49-entry screen-space ring, `head`, `count`, positions): the trail also records every fly-in tick including the one it ends, is reset on the fly-in's first tick (for `enterTicks` ≤ 1 on the first `alive` tick), Options hide while the ship is not `alive`, drawn from their own batch on `LayerId.Player` before the ships; `options/orb` joined `ENGINE_SPRITES`. World batches are now ground, air, player shots, Options, ships, enemy bullets. `hashWorld` covers loadouts, option groups, autofire timers and live piercing shots' cooldown tables. Test infrastructure: `measureHeapGrowth` gained `attempts` (default 3, the steadiest window) and `settled` (32 KiB early stop) because the stage-runner guards were already flaky on master; the measured loop must stay in the warm-up's function (OSR inlining); `enemies-runtime` "nobody shoots" tests now disable autofire; new `weapons-runtime` integration test and `test/e2e/weapons.spec.ts`. **Review fixes:** round 1 — the terrain-scan allocation guard failed ~1 run in 4 (69–96 KB of 64 KiB): it now warms up 20,000 calls, and every guard keeps the steadiest of three windows with the `settled` early stop (core suite 13.5 s instead of 20.5 s without it; new `alloc.test.ts` self-tests); round 2 — with a fly-in of `enterTicks` ≤ 1 the Option trail was never reset, so Options sat at the view's top-left corner: an `alive` ship with `stateTicks` 0 now resets it (`WeaponHost` gained `ship`), and the fly-in's last step is recorded like the others (before, Options lagged 11 px behind with `enterTicks` 2). **Bug found by the TEST agent:** a falling missile at a non-finite position (NaN / infinite `spawnShot`) had its terrain pixel read as row 0 through `\| 0`, so rock on the map's top row "landed" it and it slid on at y −2 inside the cull margin instead of being culled; the landing test now runs only for finite positions (regression test in `weapons-edge.test.ts`). Docs: new `docs/dev/weapons-and-options.md`; `docs/client/preview-build.md` gained "Your weapons" (autofire, two-shot cap, hits and flashes, the `?loadout=full` laser / missiles / Options) and troubleshooting rows; `controls.md` explains that Shot / Sub need no press. |
| M1-11 | Power meter, capsules, Force Field & Mega Crash | done | 1 | 70 | f36982e, ead0a0f, 369fe68 (+ this row fix) | The slot **names** (`speed missile double laser option shield mega`) live in `core/config` as `MeterSlotName` / `METER_SLOT_NAMES` (+ `DEFAULT_AUTO_POWER_UP_ORDER`, `MAX_AUTO_POWER_UP_ORDER` 32), so `config` never imports `powerups`; `powerups` numbers them as `MeterSlot` 0–6 — the placeholder's `special` became `mega`. `DEFAULT_GAME_CONFIG.powerUpMode` is now `'meter'`; `resolveGameConfig` rejects `'direct'` ("not implemented until M2-05") and any other string, validates `autoPowerUpOrder` and stores a frozen copy. The plan's `canEquip(slot)` became `canEquipSlot(slot, ship, loadout, maxSpeedLevel)` / `equipSlot` (the rules need the state), plus `PowerUpSystem.canEquip(player, slot)` and `equippable(player)` (a bit mask of equippable slots for the M1-16 HUD) — **the meter is not drawn until M1-16**; the Speed cap is the ship spec's `speeds.length − 1` (5). The PowerUp press is read in phase 2 between `updatePlayer` and the weapons (a new weapon fires that tick), for every active ship that is not `dying` / `dead`. `pickupMagnet` defaults to **true** (D33): an item within 16 px of an alive ship's pickup box is pulled towards the nearest such ship at 2 px/tick. Items use a built-in kind table (`ITEM_KINDS`: the capsule, engine sprite `items/capsule`, 300 points) instead of content; capsules stay in world space and are culled 32 px outside the view; pickups (item radius 5 vs the closed pickup box, lowest player slot wins a tie, brute force — not the grid) are found in phase 6 and applied in phase 7; `PowerUpSystem.outcomes` lists them with their points for M1-12. Capsules from the enemy outcomes' drops (carriers, completed formations) spawn at the end of phase 7; drops of kills made between ticks (tools) at the next phase 3 (`beginTick`, `dropsTaken` hashed). Auto Power-Up's next wanted slot is the first order entry the loadout does not satisfy yet (a slot listed n times wants level n, capped at its maximum; a Double / Laser entry is also satisfied by a later Double / Laser entry; `mega` never), re-evaluated at every pickup. The shield lives on the ship (`PlayerShip.shield: ShieldState`) and `Loadout.shield` was removed; `GameConfig.loadout: 'full'` now includes a Force Field. `playerHit` hands every hit to `absorbShieldHit` first: absorbed hits count as accepted (the bullet is used up) but are not recorded on the ship; bullets, lasers and contact are absorbed, terrain never; the 8 shield-hit i-frames also cover the bare ship right after the break (not terrain) and count down in phase 7 skipping the hit's own tick, so a hit on tick t blocks t+1 … t+8. The M1-03 sprite has **4** wear frames (fresh at 5 and 4 hits, then worn, damaged, critical), not three; the shield blinks during its i-frames and is drawn from a `LayerId.Player` batch after the ships. Mega Crash is armed by the press in phase 2 and detonates in phase 7 of the same tick through the new `EnemySystem.megaCrash(by)` (compiled `megaCrashImmune` table; armour does not protect), so its kills score and drop capsules; it also cancels every cancelable bullet and laser with sparkles. New events: `SimEventKind.PowerUp` (8; id = slot, param = player), `SFX_CUES.PowerUpDenied` (22), `FX_CUES.ShieldBreak` (4); a pickup plays `SFX MeterAdvance`, Mega Crash `SFX MegaCrash` + a 12-tick `SimEventKind.Flash`. The shield and item batches are appended after the enemy bullets (earlier batch indices unchanged); `ENGINE_SPRITES` gained `items/capsule` and `shields/force-field`; `hashWorld` covers the meters, pending Mega Crashes, every ship's shield and `dropsTaken`. Rare events (a press, a pickup) run in cold code and cost a few bytes each: the allocation guards measure ≈ 20 KB over 10,000 busy ticks (budget 64 KB). **Bug found by the TEST agent:** `core/powerups` `advanceMeter` — a `NaN` cursor stayed `NaN` forever (every capsule kept it `NaN`, every PowerUp press was denied); any cursor that is not a slot or -1 now wraps to Speed (regression tests in `powerups-edge.test.ts`). Docs: new `docs/dev/powerups-and-shields.md`; `docs/client/preview-build.md` gained "Power-ups" (capsule sources, the magnet, the capsule-count table while the meter is not drawn, OK rules, the Force Field's wear, Mega Crash, what good looks like, troubleshooting) and TV check 9 (OK while holding an arrow — the plan's optional manual check); `controls.md` explains PowerUp. |
| M1-12 | Death, respawn, checkpoints, lives & score | done | 1 | 71 | 84297d6, 0d7f7b3, db4b912 (+ this row fix) | `playerHit` still only records the hit (every phase-6 system sees the same ships); phase 7 turns a ship hit on this tick (`hitTick === tick`) into the death sequence (`killShip`) **after** the shots' hits, the power-ups and the tick's score: `killPlayer` (`dying`, lives − 1, never below 0), `SFX PlayerDeath`, `FX ExplosionLarge` + new `FX_CUES.Debris` (5), `Rumble`, new `SimEventKind.MusicDuck` (9, param 120), `requestHitStop(8)`, `requestShake(Medium, 20)`, `cancelAll(Sparkle)` (lasers too), then the penalty; terrain passes the Force Field, so a terrain death takes the shield with it. `dying` lasts `PLAYER_DYING_TICKS` **24** (the plan gave no length; counted after the 8-tick hit-stop), `dead` `PLAYER_DEAD_TICKS` 60; the respawn decision runs in phase 2 right after the ships' timers (`lifecycleSystem`). `respawnInvulnTicks` raised to **150** in `kestrel` / example player content and `DEFAULT_PLAYER_SHIP` (was 120): the blink covers the fly-in (`invulnTicks` = `enterTicks` + 150) and exactly 150 invulnerable ticks start when control returns; the invulnerable ship fires. Death penalties live in `core/powerups` (`applyDeathPenalty`, `loseOneLevel` — `player` cannot import the weapon values without a cycle) and apply at the death: every preset loses the shield; `classic` one level (Option → Double / Laser → Missile → Speed), cursor kept; `arcade` everything and cursor −1, and at the **respawn** `stage.restartAt(stage.checkpoint)` (the stage `clear` hook is now `clearSession`: pools, enemies, weapons, power-ups, score counters); free flight gets the same clear with no camera move; other co-op ships in play fly in again without a penalty. Every respawn flies in from the left edge of the current view. `deathPenalty` strings are not validated at runtime (unknown → casual behaviour). Game over is set once every active ship is out (`playerOut`: dead, dead time over, no lives) — after the last explosion and dead time — and only from `playing` / `bossWarning` (never over `stageClear`); the World keeps simulating. Scoring: `ScoreBoard` (`PlayerScore { score, displayDirty }`, session `hiScore` + `hiScoreDirty`, `setHiScore` for M1-17's save) inside a `ScoringSystem` (`world.scoring`) credits enemy outcomes in phase 7 and in phase 3 (kills made between ticks), each exactly once (`killsScored` / `bonusesScored`, hashed); `EnemyOutcomes` gained `bonusCount` / `bonusScore` / `bonusBy` so a formation bonus goes to the killer of its last member; the hi-score is **not** hashed. fx: `FxState` (`world.fx`) keeps the shake / flash timers (hashed); hit-stop now counts down only on ticks that started frozen (`fx.frozen`, set by `stepWorld`), so a request during tick t freezes exactly t+1 … t+n (before, phase 9 of the request's tick took one off); `Shake` events carry the duration in `id`, `Flash` events the `FlashKind`; Mega Crash's flash goes through `requestFlash` (same event). Allocation fixes: the fly-in writes the `outCubic` easing inline (the call boxed values, ~1.7 KB per respawn); cancel sparkles are pushed at whole pixels. New guards `world-death-alloc` / `world-death-arcade-alloc` (~10 KB / 10,000 ticks). The co-op power-up guard now re-grants shields and uses the `casual` penalty, because `classic` deaths left a `core/weapons` grid visitor deoptimised by V8 (a tiering quirk to watch in M1-19's bench). Tests that parked ships in terrain or ran stages unattended now use god mode, top up lives, or run the bullet phases alone. Shell flight HUD shows the score, `HI` and the hi-score, `lives − 1` stock and `GAME OVER`. **Bug found by the test agent:** `ScoreBoard.setHiScore` (`packages/core/src/scoring/index.ts`) marked the HUD dirty even when the hi-score did not change (a value flooring to the current one, 10.5 over 10, or a second value above `MAX_SCORE` once at the cap) — fixed to flag only a real increase, regression tests in `scoring-edge.test.ts`. Docs: new `docs/dev/death-and-scoring.md`; client `preview-build.md` "Lives, losing your ship and the score". |
| M1-13 | Bosses & the WARNING sequence | done | 1 | 54 | 6dc00e2, e67242a, 25d9dd7 (+ this row fix) | Content: a boss is an `enemies` entry with only an `id` and a `boss` section; the loader fills its regular fields (`hp` = the cores' total, `score` = `boss.score`, empty `script` / `sprite` with ids -1, a 1-px hurtbox, `megaCrashImmune`; `boss: null` on regular enemies). A regular enemy's required fields (`hp`, `score`, `hurtbox`, `script`, `sprite`, `drop`) are now checked by the loader instead of the schema; a bad entry (boss section included) still fails its whole file. The boss section adds to the plan: part `name`s; `x` / `y` offsets (the plan's `localX` / `localY`); `vulnerable: 'never'` (armour); **`core`** parts (the plan did not say how a boss dies: when every core is destroyed); **`gun`** parts (what the generic behaviours fire from); per-part `score` and `explosion`; per-phase `params`; `until.count` and `until.ticks`. Every phase but the last needs `until`, the last must not have one. A fourth loader pass (after reference resolution) reports a `spawn` / `formation` event or a `child` naming a boss, and a `warning` / `boss` event naming a regular enemy. `Boss` / `BossPart` are classes with numeric codes (`BossState` None / Warning / Intro / Fight / Dying / Dead, `BossVulnerable`) instead of string states; one boss per World, 16 part slots. The boss moves by its own motion (`hold`, `track`, eased `moveTo`) rather than the enemy movers (keeps `updateMover` monomorphic) and rides the camera. Parts use hit / grid / laser-source ids `BOSS_PART_ID_BASE` (64) + index (`MAX_HIT_TARGETS` 80); `core/weapons` applies part hits through `BossSystem.damagePart` (a `BossHit`: None / Clink / Damaged / Destroyed); piercing shots keep a second table, `WeaponSystem.partCooldowns` (enemy tables unchanged); `BulletHost` gained an optional `laserSources` (the World: enemies, then parts). Destroying a part destroys the parts attached to it, each paying its score; the whole boss clinks during its intro; parts touch the ships during the intro and the fight. New `StageRunner.brake(ticks)`: the speed ramps to 0, then the camera locks; keys and `speed` events met on the way only record the resume speed; `unlock()` ramps back up; `STAGE_STATE_SLOTS` 19 → 22. New `SimEventKind.Dim` (10) and `BossDefeated` (11), `SfxPriority` hints (the siren is `Critical`), `FX_CUES.BossChain` (6) / `BossBlast` (7), `FlashKind.Warning` (8 ticks) / `BossBlast` (24 ticks); the render contract gained `WarningView` (`WorldView.warning`), drawn by the shell's flight scene as a translucent band. WARNING text template `'WARNING!!\nGIANT HOSTILE "{name}"\nCLOSING IN - CODE {code}'` — D10's em dash became `-` because the pixel font is ASCII-only. The death sequence counts simulated ticks (the hit-stop pauses it): chain explosions until tick 120, final blast at 120, tally at 121 (after the hit-stop), status `stageClear` and lock released at 180 — 185 World ticks after the kill. Boss behaviours are a second roster in `core/behaviors` (`boss.hover`, `boss.lanes`; `BossBehaviorDef`, `defineBossBehavior`, `createBossBehaviorRegistry`, `DEFAULT_BOSS_BEHAVIORS`, `BOSS_BEHAVIOR_IDS` added to `KNOWN_SCRIPT_IDS`); `checkEnemyBehaviors` checks boss phases; new `WorldOptions.bossBehaviors`. A checkpoint clear removes the boss, turns `bossWarning` back into `playing` and re-queues the stage theme if the boss had changed the music; the boss batch is appended last to the World's `view.batches`. Content: `test-boss.enemies.json` (TRIAL WARDEN, TW-00) and `test-boss.stage.json` (BOSS RANGE, `?stage=test-boss`); the example warden became a boss and the samples lost their redundant `boss` / `music` events. Allocation: the guard that runs a whole boss sequence every ~500 ticks has its own 128 KB budget (measured ≈ 70 KB — its once-per-boss code runs partly in V8's lower tiers, which box numbers); the per-tick guards (a long fight ≈ 39 KB / 10,000 ticks) stay within 64 KB; `boss.hover` keeps its "never" timer a small integer. **Bug found by the test agent:** `core/data` did not report a part named twice in a boss phase's `until.partsDestroyed` — the repeat added nothing to the watched parts' mask but still counted toward the number of destroyed parts the phase waits for (the default `count`), so that phase could never end; now an issue (`packages/core/src/data/index.ts`, regression test in `bosses-data-edge.test.ts`). Docs: new `docs/dev/bosses-and-warning.md`; client `preview-build.md` "The boss range and the WARNING". |
| M1-14 | FX & game feel | done | 1 | 117 | 8716dd5, 7d9d059, 8a16106 (+ this row fix) | Content: the file is `content/fx/particles.fx.json` (the `<folder>/<name>.<kind>.json` rule), not the plan's `particles.json`; presets are bound to events through a `triggers` list (`{ event: 'fx' \| 'sfx', cue, preset, dx?, dy? }`, ≤ 4 per cue): `fx` = every `FX_CUES` cue, `sfx` = the sounds that imply a visual (`EnemyHit` → `spark`, `Clink` → `clink`, `MeterAdvance` / `CapsulePickup` → `pickup`, `PlayerShot` → `muzzle` 9 px ahead), so the sim needed no new particle events; an extra preset `shield.break` serves `FX_CUES.ShieldBreak`. Validated by render-pixi `loadFxContent` (the `fx` owner, in the shell's `DEFAULT_CONTENT_OWNERS`; `bootShell` keeps the parsed content as `Shell.fx`); `pnpm content:check` checks the preset sprites against the atlas — the root `package.json` links `@shmup/render-pixi` as a workspace dev dependency for it (lockfile +3 lines). Two new procedural sprites, `fx/sparkle` (cancel twinkle) and `fx/ring` (pickup ring). Particles live in **world pixels** and the camera is applied when they are drawn (not at spawn), so explosions stay on scrolling terrain; they advance by simulated ticks (the renderer steps them by the `frame.tick` delta — frozen while paused, cleared when the tick goes back, ≤ 60 a frame); a particle emitted between ticks first appears on the next tick. The presentation RNG is the core's sfc32 seeded from the game seed (`^ 0x2545f491`) but stepped on an `Int32Array` state with 16-bit draws (closure words allocated on every draw). Normal and additive particles have separate preallocated sprite sets (changing a blend mode rebuilds Pixi's render group). The renderer owns `particles`, `popups` and `effects` (new `setFxContent`, options `effects` / `fxSeed` / `particleCapacity`); the shell's `connectFxEvents` (dispatch module) wires `Particles`, `Sfx`, `Shake`, `Flash`, `Dim`, `Score`, `FormationBonus`, `BossDefeated` — in free flight only. Hit flash and the invulnerability blink were already sim-side (M1-10 / M1-12) and drawn — nothing changed. The shake mirrors the sim's `shakeAmount` tick for tick along a fixed 8-step pattern (`EffectSettings.screenShake` = off switch); flashes have a look per `FlashKind` (Mega Crash white 0.85, WARNING red 0.35, boss blast white 1.0) behind a ≤ 3-starts-per-60-ticks limiter (1 and alpha ≤ 0.25 with `reduceFlashing`). The WARNING's `Dim` event darkens only the **playfield** through a new overlay in the world group (under the flash and the HUD); `frame.screen.dim` stays the menu dim. Score popups need points and a place, so the core gained `SimEventKind.Score` (12: `id` = player, `x`/`y` = the kill, `param` = points), pushed by `core/scoring` for credited kills and by `core/bosses` for destroyed parts — presentation only, not hashed; formation bonuses and the boss tally pop up (gold) from their own events; capsule pickups get no popup (it would cover the ship). New shell module `fx-gallery` (`?scene=fx-gallery`: every preset, then the shakes, flashes, dim and popups, one station a second; the World's events not connected there); new e2e `fx-gallery.spec.ts` (web + Tizen builds). `effects` stays `partial` (raster / palette effects M2-08, CRT M3-02), `particles` is `implemented`. **Bugs found by the test agent:** (1) render-pixi `effects` — score popups shared one ordered quad pool, so a popup blinking, appearing or expiring moved later glyphs onto other quads; with a gold bonus popup among white ones those quads were re-tinted on such frames and Pixi's tint setter allocated (~1 MB per 10,000 frames) — each popup slot now has its own 8-quad pool, re-tinted only when a popup of another colour takes the slot; (2) render-pixi `particles` — `emit()` with a fractional preset index (e.g. 0.5) returned NaN (it read `pCount[0.5]`) — it now spawns nothing and returns 0. Docs: new `docs/dev/fx-and-game-feel.md`; client `preview-build.md` "Explosions, sparks, shake and flashes" and the effects gallery. |
| M1-15 | Audio engine & procedural placeholder SFX/music | done | 2 | 114 | 61c45a2, 6dd8929, 540d0cb, 0184374 (+ this row fix) | Content follows the `<folder>/<name>.<kind>.json` rule: the bank is `content/audio/main.sfx.json` (kind `sfx`) and the songs `content/audio/music/<id>.music.json` (kind `music`), not `sfx.json` / `*.song.json`; the content naming test takes the basename and allows the kinds `sfx` and `music` in `audio/`. `SFX_CUES` has **23** cues (the whole §19 list), all bound, instead of ≈30 — no cue was added, so the sim is unchanged; the WARNING siren is a 0.92-s square wail swept by a 1.1 Hz modulation. Music binding: a track declares the `MUSIC_CUES` name it answers (`cue`) and optionally `stages`; `resolveMusicCues(content, stageId)` picks the track bound to the running stage over the cue's default. Original songs: `zone-a` (AZURE VERGE, 6.4 s intro + 44.8 s loop), `boss` (BULWARK ASSAULT), `title` (prepared by M1-16's scene flow, nothing plays it yet), `stage-clear` and `game-over` jingles. Synth: sines from the core's `SIN_TABLE_Q16`, pitch ratios from 13 literals, noise / randomness from the core's sfc32 — bit-identical on every engine, pinned with `pcmHash`; the SFX parameter set adds `decay`, `sustainVolume`, `pitchJumpTime`, `modulationDepth`, `tremoloRate`, `duty`, `seed`; songs use whitespace-separated text tracks (`C4:2 . - = @inst`), an instrument `sweep`, a default song volume of 0.4; rows are whole samples, the loop region holds the loop's steady state (seam = unrolled render), `renderSong` also returns `sampleRate` (loop points −1 for a one-shot). New audio-web module `engine` (`createAudioEngine`: `loadSfx`, `prepareMusic`, `attach`, `playSfx`, `playMusic`, `duckMusic`, `endFrame`) composes loader, sfx and music for the shell; `web-audio` gained the structural Web Audio types (`PlaybackContextLike`, `isPlaybackContext` …); the core `IAudio` was **not** grown — playback is event-driven through the shell. "One track resident" is the music player's rule; the engine keeps the stage's music **set** prepared so the boss theme can start mid-stage without rendering; a cue outside the set is ignored (`missedMusic`). `Silence` fades out over the event's ticks, `MusicDuck` ducks to 0.35 (4-tick fall, held for half the ticks). Voice manager: voices are freed by the context clock (no `onended`); a cue at its instance cap restarts its oldest copy even when `critical`; a voice of higher tier is never stolen (the new sound is dropped); the event's `SfxPriority` hint overrides the cue's tier; dedupe is **per drained frame** (`endFrame()`), because events carry no tick number; cue volume is baked into the samples; `bus: 'ui'` is unpanned, `pan: false` keeps whole-screen sounds centred; pan ±0.6 at the playfield edges. Loader: SFX are rendered at boot before a context exists — prepared sounds keep their `Float32Array` until `attach()` copies it into an `AudioBuffer`; the OGG path (XHR `arraybuffer`, status 0 for `file://`, `OfflineAudioContext(2, 1, 32000)` callback decode, loop points scaled from the file's `sampleRate`) is fake-tested, no audio file ships. Shell: owns `sfx` / `music` (also in `DEFAULT_CONTENT_OWNERS`), renders the bank and prepares the booted stage's music set behind the bar (`LOADING SOUND` / `LOADING MUSIC`), new boot error `AUDIO FAILED TO LOAD`, attaches the engine right after `audio.unlock()` (which creates the context synchronously) and when it resolves, `connectAudioEvents` (free flight) maps `Sfx` / `Music` / `MusicDuck`, `engine.endFrame()` after each drain, engine destroyed on `stop()`; `ShellOptions.audio` is `IAudio & Partial<AudioGraphLike>`, new `ShellOptions.audioLoader` and `Shell.audioEngine`. Open-space free flight prepares no music, so the Tizen app plays SFX but **no music** yet; in a browser nothing plays before the first key / pointer gesture. Volumes from options wait for M1-17. Tooling: root dev dependency link to `@shmup/audio-web` (for `content:check`) and a shell dependency on it; `scripts/audio-preview.mjs` (`pnpm audio:preview`, WAVs via Vite `ssrLoadModule`, a looping song as intro + loop + loop, `--out` / `--only` / `--quiet`); the apps' boot tests mock `@shmup/audio-web` partially; shell tests that used `sfx` as an unowned kind use `campaign`; new e2e `audio.spec.ts`. `boss.spec.ts` now polls for the boss and needs two band-free captures in a row (a load flake also seen on the pre-change build); `stage.spec.ts`'s load flake (the terrain scroll exceeding its 250-px search window) is documented and left unchanged. **Review fix (round 1):** `bootShell` prepared a fixed set (`STAGE_MUSIC_CUES`: Stage, Boss, StageClear, GameOver), but a stage's data names its own theme / boss cues and can switch through `music` events, so e.g. a `FinalBoss` theme or a mid-stage `ZoneMap` event validated cleanly but played silence — new `stageMusicCues(stage)` (theme, boss — the generic `Boss` when none is resolved —, each `music` event's cue, StageClear, GameOver; deduplicated, no `Silence`), prepared by the shell; `STAGE_MUSIC_CUES` is now only `prepareMusic`'s default; `content:check` asserts every cue a shipped stage references is in its set and has a track (one unreproducible `//#test:integration` failure during that round). **Bugs found by the TEST agent:** (1) audio-web `engine` + `sfx` — every positional SFX request allocated ≈ 16 B (a freshly computed fractional pan boxed on the call into `sfx.play()`, dropped and deduped requests included; 324 KB per 10,000 frames in the new guard) — new `SfxPlayer.playAt(cue, x, priority)` with `panField` / `panWidth` computes the pan only once a voice starts, and the engine plays positional cues through it; (2) audio-web `music` — `stop(fadeOutTicks)` during a running fade-out called `source.stop()` a second time unguarded, so on engines that throw on a second stop two `Silence` events in a row threw out of the event dispatch — now caught like `dropSource()`; (3) audio-web `sfx` — `play()` with a fractional cue id or a hole in the bank threw a `TypeError` (`cues[1.5]` is `undefined`, not `null`) — such ids are now dropped like any unknown cue (regression tests in the `*-edge` suites). Docs: new `docs/dev/audio.md`; client `preview-build.md` "Sound and music" (what you hear and when, the first key press in a browser, the loop point, TV check 11 — SFX latency, the plan's optional manual check), `controls.md` sound notes. |
| M1-16 | Scene flow, canvas UI kit & HUD | done | 1 | 114 | 03a74d6, 4d6312c, 3dc62e4 (+ this row fix) | Two ways to run a game: `createGame(platform, overrides, content, options)` gained `GameOptions.scenes` (`'boot' \| 'title' \| 'game' \| null`); without it the session is bare gameplay exactly as before (one World, no scenes, nothing reacting to its status) — every earlier test, the tools and the shell's dev scenes use it. With it `game.world` is the game scene's World — **a fresh one per game start and per RETRY STAGE** (a scene transition, never a tick), plus a placeholder World before the first start (its queued stage theme dropped: the flow clears the event queue); `game.scenes` is the `SceneFlow`, `game.inputContext` the top scene's. Every World of a session pushes into one event queue (new `WorldOptions.events`), with the menus' sounds and the scenes' music. `RenderFrame.tick` is the World's tick while the game shows (frozen under the pause menu and the end screens, back to 0 for a new World — so the renderer's particles, starfield and HUD flash freeze and a new World clears them), otherwise the flow's own tick; `game.pause()` stays a host-level freeze (the pause menu is a scene). Stack: `SceneStack` depth 8 with `push` / `pop` / `replace` / `reset` and `enter` / `exit` / `cover` / `uncover`; requests during a tick are queued (≤ 8) and applied in order at its end (also those hooks request; a runaway chain > 64 throws), outside a tick at once; the lookup is `sceneAt(i)`, not `at(i)` — the Chrome-69 lint rule rejects any `.at(` call. `BootScene` waits for the host's `finishBoot()`; the shell calls it after loading, so the boot scene is only up for one tick (pre-renderer loading stays on the 2D overlay bar). OPTIONS is disabled in the title and pause menus until M1-17; EXIT only exists when `platform.exit` does — in a browser Back on the title goes from the menu back to `PRESS OK`. In the game Back pauses as well as Pause; stage clear / game over open their screens 90 / 30 World ticks after the status changes; the game-over screen ignores OK for 30 ticks, then returns to the title on OK / Back or after 600 ticks; stage clear: tally 240 ticks → `TO BE CONTINUED` 240 → title, OK skips. RETRY STAGE restarts without a confirmation; QUIT TO TITLE asks. Menus answer to any player (`mergeMenuInput`). A platform resume with the game on top pushes the pause menu. The session hi-score carries into every new World and the title (`SceneFlow.setHiScore` for M1-17's save). UI composition: the frame has **one** UI draw list — each visible scene (the topmost non-overlay one and the overlays above it) draws its part bottom to top through its own string-slot range, and the list is rebuilt only when the visible set or a scene's `uiRevision` changed; so the confirm dialog sits over the still-visible pause menu, with an opaque panel. The boss WARNING band moved from the shell's flight scene into `GameScene.drawUi` (same look). UI kit: `ListMenu` items are actions, `Slider`s or `Toggle`s; `menuTick` / `confirmTick` return a numeric `MenuResult`; held-duration repeat on the press, after 18 ticks, then every 6 (the last pressed direction; a latched tap acts once). **Confirm buffer:** a press refills a 4-tick buffer; a menu that just opened holds back activation for 2 ticks (`lockTicks`) but the focus still moves and Back still works, and a buffered Confirm fires when the hold ends if it is at most 3 ticks old. A Confirm on a disabled item plays `MenuBack` (UI bus) instead of `PowerUpDenied`. `MenuLayout`s passed to `drawMenu` are frozen constants (a literal per redraw would allocate). HUD: `buildHud(world, list, sprites)` + `Hud.update` (change detection over the scores' / hi-score's dirty flags, lives, player 2, the meter cursor, the equippable mask, the flash phase while a slot is highlighted, the shield); the meter uses the **atlas labels** (`SPEED MISSILE DOUBLE LASER OPTION ? !`), not the plan's abbreviations; layout `1P` at x 8, `HI` at 156, `2P` at 292 (8 digits 16 px after, `------` while player 2 is out), up to 5 stock icons (more: one icon and the count), the seven 40-px `hud/meter-slot` boxes from x 58 (frame 1 lit on the "on" half of the 8-tick flash, frame 2 when not equippable), Force Field pips from x 344; rectangles when the UI sprites are missing. The UI sprites (`UI_SPRITES`: `hud/life`, `hud/meter-slot`, `hud/meter-labels`, `ui/logo`) joined `ENGINE_SPRITES`. New procedural sprite `ui/logo` (`scripts/assets/procedural/ui.mjs`: original 5×7 block letters ×3, gradient, outline, shadow — 165×27); the atlas page grew to 512×512. Shell: the default scene is now `'game'` (the scene flow; `?scene=` missing or unknown); `?scene=flight` keeps free flight with its old dev HUD, and the gameplay e2e specs use it; new module `scene-view` (`createSceneView`: starfield backdrop behind the title, an open-space wrapper view per World, the followed camera for the audio pan, `worldChanges` → particles / popups cleared), the title theme (and the stage-clear / game-over jingles in open space) prepared at boot, `finishBoot()` after loading, the `data-shmup-scene` canvas attribute (`SCENE_ATTRIBUTE`), `Shell.sceneView`. Tizen: the Back watcher is removed once the shell is running, so Back exits directly only from the loading and boot error screens; afterwards the stack owns Back (title → exit confirmation → `platform.exit()` after YES). render-pixi `ui` marked implemented (code unchanged). Bugs found by the test agent (fixed in 4d6312c): (1) core/ui `buildHud` — with more than five stock and no `hud/life` sprite the count was drawn with no icon; it now gets the same rectangle as the other stock icons when the sprite is missing; (2) core/scenes `SceneFlow.setHiScore` — the hi-score was not capped at `MAX_SCORE` like the scoreboard's, so a huge value made the title show a different number from the game; now capped (floored as before). Docs: new `docs/dev/scenes-and-ui.md`; API reference (`scenes`, `ui`, `GameOptions`, `WorldOptions.events`, shell `scene-view` / `SCENE_ATTRIBUTE`); client guides for the title, menus, HUD, pause and end screens and the TV's exit confirmation. |
| M1-17 | Saves, audio options & platform integration | done | 1 | 112 | 00a5e4e, c8f3364, 6f0d5d8 (+ this row fix) | Save format (`core/save`, implemented): one JSON document `SaveData v1 { version, options, hiScores, stats: { gamesStarted, gameOvers, stagesCleared } }` under the storage key `save.v1` (`shmup-cup:save.v1` in `localStorage`). **The key stays `save.v1` for the whole format family**; the document's `version` field drives the migrations (`SAVE_MIGRATIONS[n]`: n → n + 1; no `version` = 0). The acceptance's "v0" is defined as the pre-release layout of the skeleton's placeholder `SaveData` (flat `hiScores` list, float volumes 0–1, `profile`, `unlocks`) — migrated to the `meter-normal` table, levels 0–10 and `input.profileId`; **unlocks are dropped** in v1 (fixture `packages/core/test/save/fixtures/save-v0.json`). Loading never rejects: JSON → migrations → a field-by-field sanitiser; unparsable / non-object text is `corrupt`, a newer or malformed version `unreadable` — both give defaults and copy the text to `save.corrupt`; the main key is replaced by the next flush. Hi-score rows reuse the existing `core/scoring` `HiScoreEntry` (name `---`, score, `reached`, mode `1p`, difficulty) instead of a second type; the table key is `hiScoreModeKey(config)` = `<powerUpMode>-<difficulty>` (`meter-normal`); top 10, ties go below older rows, a score of 0 never enters. **Stage clear also records a score** (M1's run ends there), game over too; QUIT TO TITLE and RETRY STAGE record nothing; every start and retry counts `gamesStarted`. "Write only on change" is `SaveStore.flush()` (compares the canonical text with the text last written; never rejects); `writeSave` itself writes unconditionally. The flow flushes when the Options screen closes and when a game ends. User options (`core/config`): `UserOptions { audio { master, music, sfx } (levels 0–10), input { profileId \| null }, display {} }`, defaults 10/10/10 (the current mix), `resolveUserOptions`, `volumeGain(level) = (level/10)²`, `InputProfileChoice`. **The SFX slider drives both the `sfx` and the `ui` bus** (menu sounds follow SFX). Changes reach the shell through the new `SimEventKind.UserOption` (13, appended) + `UserOptionKind` (Master/Music/Sfx volume = level, InputProfile = the choice's **index** in the flow's profile choices), applied by the shell's `connectOptionEvents`; `applyAudioOptions` sets the saved volumes at boot. Options screen: `OptionsScene` overlay from the title and the pause menu (both OPTIONS items enabled; existing menu tests updated); new UI-kit widget **`Choice`** (`MenuItemKind.Choice` = 3, `createChoice`) for CONTROLS — `menuStringSlots` adds one slot per choice item; BACK / Back store the options (the profile id only when CONTROLS changed) and flush; CONTROLS disabled (`DEFAULT`) without profiles; the game-over screen shows `NEW HI-SCORE` below its panel for a new best. **The profile choice moved into the save** (`options.input.profileId`): the apps no longer read input-web's `input.profile` key; `loadInputProfileChoice` / `saveInputProfileChoice` stay exported but unused. New input-web `selectableKeyProfiles` / `inputProfileChoices`: only profiles whose menu table the host's key space can drive are offered (web `code`: keyboard profiles; TV `keyCode`: `tizen-remote-*`), and a saved id outside that set is ignored. Profile labels renamed `SAFE 4-WAY` / `FAST 8-WAY` / `KEYBOARD AS REMOTE`; the default gets ` (DEFAULT)`. The web `?profile=` override wins over the saved choice (not over a pick) and is also listed in CONTROLS; the TV registers the new profile's keys on a switch. New shell API: `ShellOptions.inputProfiles` (`choices` / `active` / `apply(id, 'save' \| 'options')`) and `ShellOptions.now`; `Shell.loadedSave`, `Shell.save`, `Shell.bootTiming` (`startMs`, `readyMs`, `bootMs`) and the canvas attribute `data-shmup-boot-ms` (`BOOT_MS_ATTRIBUTE` = launch-to-ready ms); the save is loaded after the platform is created (it provides the storage) and before `createGame`, in dev scenes too (volumes, profile); a window `blur` listener clears held input. `GameOptions` gained `save` and `inputProfiles` (`InputProfileSetup`); `SceneFlow` gained `options`, `save`, `modeKey`, `inputProfiles`, `activeInputProfile`. Bugs found by the test agent (fixed in c8f3364): (1) core/save — a save with several hi-score tables was rewritten after every reload although nothing changed: the text followed the order the tables were added in while loading sorts them; `serializeSave` is now canonical (tables by name, rows field by field); (2) core/save `SaveStore.flush` — when two overlapping writes both failed, the store believed the first one had been stored, so a later flush of that same content was skipped; a failed write now makes the store forget what storage holds and the next flush always writes; (3) core/config `resolveUserOptions` and core/save's counters returned `-0` (volume levels in (−0.5, 0], a stored `-0` counter) — V8 boxes `-0` like a fraction; both now return 0. No golden replays exist yet (M1-19), so nothing was re-blessed. Docs: new `docs/dev/saves-and-options.md`; API reference (`save` section, `config` user options, `UserOption` events, `Choice`, `OptionsScene`, input-web choices, shell save / timing / `connectOptionEvents`); scenes, shell, audio, input-profile and architecture guides; conventions (Chrome 69's unstable sort, `-0` from `Math.round`); client guides for the Options screen, what the game remembers, CONTROLS and the TV checks 16–18. |
| M1-18 | Zone A content, boss & 4-way playtest bot | done | 1 | 79 | a5e0a0c, e05d0fd, 9ecb210 (+ this row fix) | Stage `content/stages/zone-a.stage.json` (AZURE VERGE): 9,000 px, camera keys 0.75 / 0.8 / 0.6 / **1.5** / 0.75 px/tick at 0 / 1,500 / 3,500 / 6,000 / 8,000, checkpoints 0 / 3,500 / 6,000, `warning` 8,600, `end` 9,000; heightfield terrain (a low floor, the floor + ceiling corridor 3,440–6,400 with ≥ 92 px open, a low floor under the high-speed section); **28** capsule sources before the boss (5 / 3 / 4 within 900 px after the checkpoints, 2 in the calm); ≈ 3 min of scroll to the WARNING, ≈ 3.5 min (210 s) to the stage clear with the bot. New far planet band `bg/azure-verge` from the new procedural generator `scripts/assets/procedural/backdrops.mjs`, drawn last on `mid`. Enemies: **8 types on the existing M1 roster behaviours — no new enemy behaviour was needed** (`skeet` / `skeet-chain`, `vane`, `tender`, `lancer`, `picket` / `picket-ceiling`, `strider`, `burrow` + `burrow-mite`, `gyre`); new sprites `enemies/vane`, `enemies/gyre` (the rest reuse M1-03 art); new `content/paths/zone-a.paths.json`. Boss HB-01 `halcyon-bulwark` (tally 30,000): armour `hull` / `wing-top` / `wing-bottom` and the two emitters (`vulnerable: never` — **the emitters are the guns**, so the lanes cannot be shot off), core reuses `bosses/core` (hp 40, `afterParts` of `plate-1…4`, hp 12, stacked along the core's lane); new sprites `bosses/bulwark-hull`, `-wing-top`, `-wing-bottom`, `-emitter`, `-plate`; new boss behaviour **`boss.bulwark`** (slow `track`, lane lasers from the guns in turn **attached** to the emitter so a lane sweeps with the boss, optional aimed `ways`-ways); 3 phases: lasers only → (2 plates) + 3-ways of needles at 1.5 → (4 plates) lanes every 55 ticks, overlapping, 42 px apart edge to edge. Songs: the M1-15 `zone-a` / `boss` songs are zone A's — **no new song was written**. The 4-way content test lives in `test/integration/content.test.ts` (so `pnpm content:check` runs it): static `bulletSpeed` ≤ 2.0 plus a dynamic HB-01 fight with the bot (lanes = telegraph / grow / active lasers widened by the hurt radius, overlapping lanes merge), shared checks in `test/playtest/rules.ts`; the corridor check of `stage-runtime.test.ts` now covers every stage with terrain. Debug skip flag (for the e2e smoke; M1-19's controls build on it): new sim option `GameConfig.stageSkip: 'none' \| 'boss'` (validated, replay-recorded), `StageRunner.jumpTo(x)`, `core/debug` `skipToBoss(world)` + `BOSS_SKIP_LEAD` (96 px), applied by `createWorld`; web `?skip=boss` (`stageSkipFromSearch`); none on the TV. **The apps play zone A by default** in the scene flow: `@shmup/shell` `DEFAULT_STAGE_ID` / `defaultStageId(files)`; the dev scenes (`?scene=flight` …) keep open space for the gameplay e2e specs, on the web and the TV; `enemies-runtime.test.ts` scoped to test-range's own enemies. Playtest API: `runStage(stageId, bot, { godMode, seed, stageSkip, config, maxTicks, observe })` → report with the recorded per-tick input; `replayStage` replays it; `fourWayBot()` scans 12 lanes × 16 px in 2-tick slots (40 ticks ahead); the no-god-mode run is printed (today: stage clear, no death). Bugs found by the test agent (test tooling): `rules.ts` `laserLaneGaps` counted a beam wholly above / below the playfield as a lane at row 0 / 200 (a false narrow gap and an open band wider than the playfield) — now skipped; `four-way-bot.ts` rated crossing its own lane as badly as staying, so it froze inside an active beam or waited for a bullet about to hit — a threat at the ship's own spot no longer penalises leaving (regression tests; zone A runs unchanged). Docs: new `docs/dev/zone-a-and-playtest.md`; client `preview-build.md` section "The first zone: AZURE VERGE" and TV checks 19–24. |
| M1-19 | Debug tools, replays, golden tests & M1 release check | done | 2 | 129 | eae92ee, 0e1c4ca, b9e1842, 6f82c5d (+ this row fix) | Debug switches live on the **`Game`** (`game.debug`) and are shared with every World of the session through the new `WorldOptions.debugFlags` (god mode / outlines survive a new game); `DebugFlags` gained `showGrid` and `overlay`, `slowMo` is `SlowMo` (`1 \| 2 \| 4`, `SLOW_MO_STEPS`). **Frame advance and slow motion are in `Game.frame`**, not the loop: frame advance runs only the ticks queued with the new `Game.requestStep(n)` (all in the next frame), slow motion feeds the loop a clock slowed 2× / 4× in whole ms; switching resets the accumulator (no burst); `Game.step` ignores both. `createDebugControls(game).run(DebugCommand)`; new core helpers `jumpToCheckpoint`, `jumpToNextCheckpoint`, `collectDebugCounters` (hash every `DEBUG_HASH_INTERVAL` = 60 ticks); the stage jumps act only while the game scene is on top and the World is `playing` / `bossWarning`. Keys: F1 overlay, F2 god mode, F3 outlines (off → hitboxes → + grid), F4 frame advance, F5 step (the only repeating key), F6 slow-mo, F7 next checkpoint, F8 skip to boss. **TV:** Pause (10252, or 19) then Ch+ ×3 within 3 s unlocks the tools and shows the overlay; the Tizen app's `onUnlock` then registers the number keys and 1–8 act as F1–F8 (`tizenDebugTools`, `DEBUG_REMOTE_KEYS`); the sequence keys still reach the game (Pause opens the pause menu, where Ch+ is unbound). **New `@shmup/shell` module `debug`** (not named in the plan — keys and timing are host work): `debugToolsFactory` / `createDebugTools`, passed as the factory `ShellOptions.debugTools` so release bundles leave the module out; `Shell.debug`; `window.__shmupDebug` (`ShmupDebugApi`: `sceneId`, ticks, flags, counters, stats, `unlocked`, `buildId`, `run`, `game`); the frame loop times ticks and render (smoothed) and rebuilds the overlay before `renderer.render`. Overlay (render-pixi `debug`, implemented): core `DrawList`s through the `ui` quad pools, **one colour per list** (Pixi's `tint` setter allocates) — nine outline lists, seven panel lists — plus a 60-frame graph; draw calls from the new renderer option `countDrawCalls` → `PixiRenderer.drawCalls` (−1 when off). Builds: `shmupBuildInfo()` defines `__SHMUP_DEV__` (dev server, `--mode development \| test`) and `__SHMUP_BUILD__` (short git SHA, `+` when dirty, `SHMUP_BUILD_ID` overrides; types in `types/build-info.d.ts`); new app scripts **`build:test`** (what `pnpm test:e2e` builds; Turborepo task) and **`build:dev`** (on-device debug build); the release bundle carries no debug code (asserted). Release `app.js` 228.6 KB gzip, test build 234.3 KB. Replays (core `replay`, implemented): `assisted` = god mode for the whole run; hashes every 600 ticks **plus a final hash**; the input device is not recorded; the build lock is **reported, not enforced** (`DesyncReport.buildMatches`); replays cover **bare-gameplay sessions only** (recording the scene flow is later work, M2-15 / M3-01); extra helpers `createReplayGame`, `playReplay`, `encodeReplay` / `decodeReplay` (RLE of LEB128 varints, own base64). Golden replays: 4 scenarios (`zone-a-god`, `-arcade`, `-deaths`, `-boss`); the 4-way bot never dies on zone A, so the deaths scenario uses a careless **weaving pilot**; files store description + expected outcome, use the fixed build id `golden`, are excluded from Prettier; `pnpm golden:update` = `scripts/golden-update.mjs` (spawns Vitest with `SHMUP_GOLDEN_UPDATE=1`). Bench: own config `test/bench/vitest.config.ts` (`*.perf.ts`, not part of `pnpm test`), CI step after the build; heap growth = retained heap after forced GCs; the pool top-up is timed with the ticks; median ≈ 0.12 ms/tick. check-bundle: exported budget constants + `pngSize`; atlas pages must be readable PNGs. e2e smoke holds → then ↑ for 2.5 s each and also checks the debug keys and the TV unlock. Version 0.1.0 also in apps/electron and the root `package.json`; a test keeps `config.xml` equal to the Tizen package version; the **`v0.1.0` tag is left for the step's final commit**. Review round 1 (major, fixed): `pnpm test:e2e` was flaky under load — specs compared captures a set number of rAF frames apart while the loop runs 1–4 ticks per frame; new `test/e2e/frame-advance.ts` (`freezeSim`, `stepTo` through `__shmupDebug` / `requestStep`), `stage.spec.ts` / `enemies.spec.ts` step exact ticks, Playwright workers capped at half the cores (≤ 8). **Bugs found by the TEST agent:** core `replay` — a zero-tick replay never compared its final hash (the starting state), so a tampered header (another seed) played back as ok; `createPlayback.check()` before the first tick now compares it and `playReplay` makes that call. render-pixi `debug` — `buildDebugOutlines` drew a 1-px "hurtbox" for boss parts without a hurtbox; such parts are now skipped. The render-pixi overlay allocation guard was flaky (~25 % under parallel test processes, V8 hidden-class state of the first overlay instance); it now measures a second overlay after a throwaway one. Docs: new `docs/dev/debug-and-replays.md`; new client `docs/client/debug-tools.md` (the tools, reading the panel, the M1 release check); `preview-build.md`, `controls.md`, `install-on-tv.md`, API reference, build/test/deploy, architecture, conventions (golden re-bless and debug-only code rules) updated. |
| M2-01 | Rank, difficulty presets, extends & continues | done | 1 | 158 | b31fac5, 7769bfd, 49b5ca8 (+ this row fix) | The table is **`content/rules/difficulty.rules.json`** (not `difficulty.json` — the `<folder>/<name>.<kind>.json` naming rule), kind `rules` owned by `core/data` (`ContentDb.difficulty`; all four presets required, `aimDirections` a power of two, one file only); `core/config` keeps the same values as `DEFAULT_DIFFICULTY_TABLE` for sessions without content, and `pnpm content:check` keeps the two equal. Values: rank base 0/2/4/6, growth 0.5/1/1/1, lives 5/3/3/2, continues 5/3/2/0, death penalty casual/classic/classic/arcade, aim directions 16/32/32/32, bullet speed ×0.85/1/1/1, extends 20,000 then every 70,000. `GameConfig` gained `rankBase`, `rankGrowth`, `extendFirst`, `extendEvery`, `continues`, `bulletSpeedMul`; `resolveGameConfig(overrides, table)` fills the chosen preset's fields **under** explicit overrides — **behaviour change:** `{ difficulty: 'arcade' }` now also means 2 lives and the arcade penalty; `deathPenalty` is now validated; `withDifficulty` switches a config to another preset; replay headers record every resolved value (format version unchanged — an M1 header decodes to its preset's values). `rankGrowth` multiplies the growth terms (floored): `rank = base + floor(growth × (8·(loop−1) + (stage−1) + power + special))`, 0–31, ≤ 16 on loop 1; `updateWorldRank` at the end of phase 3 (power of the strongest active ship, dying / dead ones with what the penalty left); `world.rankInputs.loop` / `stage` stay 1 until the campaign (M2-10); the Reduce term (+2) is defined for M2-04. Enemy rank modifiers reuse the enemy schema's existing (unread) `rank: { fireRate, bulletSpeed }` fields, now sensitivity multipliers `1 + k·(scale−1)` applied through `BulletSystem.setShooterRank` / `clearShooterRank` only while that enemy's script runs. Revenge bullets: `revenge: { minRank, pattern, speed? }` with built-in patterns `aimed` / `spread3` / `ring8`, only on on-screen kills credited to a player, never on a Mega Crash; zone A's `vane` fans fire them from rank 12. Extends in `core/scoring` (`checkExtends` after every crediting, cap `MAX_LIVES` 9, `ExtraLife` at `SfxPriority.Critical`, waiting while the game is over). A continue keeps the score and writes the continue count into its **last digit** (`markContinue`); `continueWorld` also re-queues the stage music (the countdown fades it out). The continue is decided by the scene flow between World ticks, so bare-gameplay replays (which end at the game over) do not record it. The difficulty menu is an overlay scene (`DifficultyScene`) pushed by START, so **starting a game takes one more OK** — every flow test and e2e spec was updated; the choice lasts for the session only (saved with the options of M2-16); each difficulty keeps its own hi-score table (`meter-<difficulty>`). `ContinueScene`: 10 s, OK after a 30-tick lock, Back / timeout → game over. The boss-fight allocation guard now runs at a constant rank (`rankGrowth: 0`): at rank 14 the boss fires ~40 % more often — more coroutine wakes (D29), not a new per-tick allocation (measured: fractional bullet speeds alone barely change the bytes). Golden replays re-blessed on purpose (reason in `b31fac5`); all four keep their outcome (3 stage clears, 1 game over). **Bugs found by the TEST agent:** core `config` `resolveGameConfig` — an explicit `difficulty: undefined` override filled Normal's fields but left `difficulty` undefined in the frozen, validated config (the explicit key won the spread), so its hi-score key read `meter-undefined`; the resolved preset is now always written (key order unchanged, no sim change; regression test `config-difficulty-edge.test.ts`). core `world` `updateWorldRank` docblock said dying / dead ships do not count; the code counts them with what the penalty left — doc corrected, behaviour pinned in `world-rank-edge.test.ts`. Docs: new `docs/dev/difficulty-and-rank.md`; `preview-build.md` (difficulty, extra ships, continues; TV checks 25–28), `controls.md`, `debug-tools.md` (`RANK`), API reference, death-and-scoring, bullets-and-patterns, scenes-and-ui, saves-and-options, sim-world, enemies, content-data, architecture, zone A, debug-and-replays (golden outcomes) updated; `CHANGELOG.md` gained an Unreleased section. |
| M2-02 | Pattern DSL, bending lasers, bullet cancel & readability | done | 2 | 115 | 3f69cf1, 898a4a6, 38bb514, 5bc61f0 (+ this row fix) | The format is `content/patterns/*.patterns.json` with named **`actions`** (`{ id, body }` — a pattern an enemy runs *is* an action) and **`bullets`** (`{ id, kind?, direction?, speed?, actions? }`), ids global; `bulletRef` is a **field of `fire`** (with `params`), not a node of its own, and `actionRef` takes `params` (`$1` … `$9`). `accel` is the engine's speed-along-heading acceleration (`accel` / `min` / `max` / `term`, px/tick²) rather than BulletML's horizontal / vertical pair (bullets are polar); a `sequence` value in `changeSpeed` / `changeDirection` is a per-tick change for `term` ticks, the other types reach their target evenly and **land on it exactly** (new bullet pool fields `accelTerm` / `termSpeed`, `turnTerm` / `termAngle`); `wait` has a `ranked` flag (÷ the rank's fire rate, like `fireWait`). Directions are binary angle units; an enemy's `relative` measures from a start heading (left, `pattern.loop`'s `heading` tunable) that its `changeDirection` sets; an enemy's relative speed is 0; expressions add `floor` / `round` / `abs` / `min` / `max` / `sin` / `cos` (the committed table). The compiler (`core/patterns/dsl.ts`, run by `loadContent` after collecting, **before** references resolve — so the enemy `pattern` ref kind resolves against compiled ids) parses expressions with a recursive-descent parser (no `eval`), folds constants with the interpreter's own arithmetic and **inlines** `actionRef` / `bulletRef` (no call stack at run time); recursion is an issue except a bullet firing itself without params (shared program); `repeat` nests ≤ 4 after inlining; bank ≤ 262,144 numbers; a bad expression fails its file's schema (the whole file contributes nothing); an action with any issue — its own, an inlined action's or a launched bullet program's — gets entry 0. Bullet kind names live in a leaf `bullets/kinds.ts` so `core/data` does not import the bullet system. Interpreter `PatternVm` (`world.patterns`): 64 emitter slots + 512 bullet-program slots in typed arrays (a bullet stores `runner + 1`; slots from a rotating hint), stepped by the new **`pattern.loop`** behaviour through `ScriptApi.startPattern` / `stepPattern` (the pattern's `wait` is the coroutine's `yield`) and inside `BulletSystem.update` through an injected `BulletProgramRunner` (no module cycle); ≤ 1,024 instructions per run; fires obey the enemy fire rule (the pattern advances, nothing launches); `$rand` draws through the new `Rng.nextFloatInto` (a returned fraction was boxed); `hashWorld` mixes the runners in use (locals included) and the bending lasers. Boss behaviours and revenge bullets do not run DSL patterns yet (M2-09); no DSL node fires lasers. Shipped: `common.patterns.json` (5 patterns) and a test enemy `sentry` in its own file `content/enemies/test-sentry.enemies.json` that no stage spawns — zone A is unchanged. **Bending lasers**: 8 stable slots (`BendingLaserTable`, not a packed pool — each keeps its 64-node ring), fired with `fireBendingLaser` / `ScriptApi.bendingLaser` (not attached; fire rule and rank scale in the latter), hitbox circles of diameter `width` on every `floor(width / 2 / speed)`-th node, drawn as un-rotated round segment sprites per node via the render contract's new `BendingLaserView` and render-pixi `createBendingLaserBinding`; no shipped content fires one. **Cancel into points**: `CancelMode.Points` for a boss's death (→ its killer) and a Mega Crash (→ the bomber); the player's death still only sparkles; point items (pool `cancelPoints`, `ITEMS` batch, `items/point`) drift 12 ticks, then fly to the credited player's HUD score slot and add `bulletCancel` points (`content/rules/scoring.rules.json`, rules section `scoring`, default `DEFAULT_SCORING_RULES` = 10) — after 180 ticks at the latest; a full pool credits at once; no `Score` popup. **Palettes**: core `BULLET_PALETTES` and `UserOptions.display.bulletPalette` (save stays version 1 — an older `display: {}` resolves to `standard`); a new **BULLETS** item in the Options screen (`OptionsItem.Back` is now **5**; panel 288×128) and `UserOptionKind.BulletPalette` = 4; the new generator `palettes.mjs` draws every bullet / beam / bend again as `<sprite>@<palette>` (45 variants, recoloured and shape-coded: pink solid, red ring, purple dot); render-pixi `resolveBulletPaletteTable` / `renderer.setBulletPalette` swap the sprite tables (the shell applies the saved choice at boot and the event live); real-art PNG overrides need their own variants. Golden replays re-blessed on purpose in `3f69cf1` (new bullet pool fields and the `cancelPoints` pool change every hash; cancel points add +130 to the two full-stage clears and +180 to the boss run); outcomes unchanged; the review fixes and tests changed none. **Review round 1** (2 major findings, both fixed in `898a4a6`): a shared bullet program was not linked to fire sites compiled after its job was drained (bullets silently lost their actions) — jobs now record their entry, failures spread to callers, shared programs are keyed by strict / loose context; reference params were pasted in as unevaluated expressions (`$i` / `$rand` read where used) — params are now **values** (constants folded, others evaluated once into runner locals: `SetLocal [9, slot, expr]`, `Fire` args `[4, dirType, speedType, kind, bulletEntry, argCount, …args, dir, speed]`, `ExprOp.Local` / `Arg`; ≤ 16 live locals; locals zeroed on reset and hashed). **Bug found by the TEST agent:** `core/patterns` `dsl.ts` silently dropped `params` on a `fire` with an inline `bullet` instead of reporting `params need a bulletRef` (as documented) — now reported and the action gets entry 0 (regression test in `patterns-dsl-edge.test.ts`). Docs: new `docs/dev/pattern-dsl.md`; bullets-and-patterns (bending lasers, cancel into points, program fields), rendering-and-shell (bending binding, the colour-blind palettes, e2e `bullet-palette.spec.ts`), asset-pipeline (`palettes`, `lasers/bend-*`, `items/point`), saves-and-options (BULLETS, `display.bulletPalette`), content-data, enemies-and-behaviors (`pattern.loop`, `ScriptApi` additions), sim-world, engine-foundations (`nextFloatInto`), debug-and-replays (golden outcomes), death-and-scoring, bosses-and-warning, powerups-and-shields, fx-and-game-feel, difficulty-and-rank, architecture, conventions, API reference; client `preview-build.md` (BULLETS, gold point diamonds; TV checks 29–30), `controls.md`; TSDoc completed (a wrong `compileExpression` example length fixed, a duplicated bullets-module paragraph removed, the collision module's stale circle-chain plan corrected). |
| M2-03 | Meter arsenal: loadouts B–D, Weapon Edit, parking & weapon select | done | 2 | 77 | 06e47aa, 6f655b3, 1bc676e, da71588 (+ this row fix) | `GameConfig` gained `weaponPreset` (a `content/weapons/` preset id, default `type-a`; a content without it falls back to its first preset), `weaponEdit` (`null` or `{ missile, double, laser }` weapon ids — Weapon Edit; `resolveGameConfig` checks the shape and keeps a frozen copy, `createWorld` / `core/weapons` `resolveArsenal` throws `RangeError` for an unknown id or a weapon of another slot, in meter order), `megaChoice` (`megaCrash` / `normal` / `speedDown` / `lifeOption` / `fullBarrier`) and `shieldChoice` (`forceField` only until M2-04); `ArsenalChoice`, `withArsenal` (a field left `undefined` keeps the config's, `weaponEdit: null` clears) and `arsenalMatches` (the flow keeps the config object when nothing changes); replay headers record the four fields with the format version unchanged (a header without them decodes to the defaults). The loadout is session-wide (both players) and session-only (saved with the M2-16 options). Content: `content/weapons/types-b-d.weapons.json` (named to load after `type-a` so the select lists A → D) with nine weapons and presets `type-b` / `type-c` / `type-d`; weapons gained an optional `name` (upper case, ≤ 16 — the select's label); the Twin Laser ships **non-piercing** (two beams × two pairs × five shooters would outrun the 32 hit-cooldown tables). Behaviours: `ShotKind` 4–9 appended (`SpreadBomb`, `TwoWay`, `Torpedo`, `FreeWay`, `Ripple`, `Twin`); Tail Gun / Vertical are `Double`-kind pairs (`angle` 512 / 256) and the Cyclone a thicker `Laser` (hh 4, 80 px, 4 swirl frames); the Spread Bomb arcs (`angle` 64, `gravity` 0.12), bursts on terrain or its first target (no damage of its own) into a world-anchored piercing blast (new `ShotFlag.Blast`; `blastRadius` 14, `blastTicks` 12, cooldown 6 → at most two hits; its cooldown table reserved at launch; armour clinks without putting it out); the Photon Torpedo's "pierces small enemies" = it flies on through every enemy its hit destroys (not boss parts); the Free Way's second shot follows the player's last 8-way direction held while alive (`WeaponSystem.freeWayHeading`, hashed; up-forward before any); the **Ripple's ring (4 px, `RIPPLE_RING_WIDTH`) is its hitbox instead of its box** — with the box the lowest-slot rule gave every ring to HALCYON BULWARK's fringe armour and Type B could not hurt the boss; Twin beams keep their lane in `vy` and the beam follow is now `shooter y + oy + lane` (0 for Type A). `WeaponSystem.setArsenal` recompiles the role tables in place (the preview); `roleWeapons` is no longer frozen; also `resolveArsenal`, `weaponsOfSlot`, `weaponLabel`, `WEAPON_BEHAVIOR_LABELS`, `WEAPON_SPRITES` (`shots/blast` joined `ENGINE_SPRITES`). Power-ups: `MeterChoices` from the config (`meterChoicesOf`, `PowerUpSystem.choices`); `canEquipSlot` / `equipSlot` / `equippableSlots` take it optionally (default Type A's) and a `MeterShip` (`lives` for LIFE OPTION); NORMAL greyed on the basic shot, SPEED DOWN at level 0, LIFE OPTION = `min(lives − 1, 4 − options)` (greyed at 0), FULL BARRIER grants a fresh `?` shield also over a worn one (greyed at full strength); only Mega Crash arms `megaPending`; `?` (and a `'full'` loadout, on a continue too) grants `shieldSpecOf(shieldChoice)`; Auto Power-Up parks on a greyed `!`. HUD: `hud/meter-labels` grew to 16 frames (new micro glyphs); `core/ui` `meterLabelFrame` names MISSILE / DOUBLE / LASER after the arsenal (`SPREAD`, `TAIL`, `RIPPLE` …); `?` and `!` keep their symbols. Scenes: the difficulty menu's OK now pushes `WeaponSelectScene` (`weaponSelect`, a full screen: TYPE A–D / EDIT, MISSILE / DOUBLE / LASER disabled unless EDIT, `? SLOT`, `! SLOT`, AUTO, ORDER, START), opened on START — **one more OK per game start** (every flow test and e2e spec updated); `AutoOrderScene` (`autoOrder`, overlay) edits the first 12 order entries; START → `chooseArsenal` (every difficulty's config) and the game, kept for RETRY and later games (`SceneFlow.arsenal`); UI string slots 96 → 160; parking needed nothing new (AUTO off keeps the cursor). Live preview = a private silent World (own event queue cleared every tick, own god-mode flags) on the new `content/stages/weapon-range.stage.json` + `content/enemies/weapon-range.enemies.json`, drawn full screen behind the left panel (not a smaller viewport), the ship held at x 232 and weaving, its main weapon following the focused row; `@shmup/shell` `scene-view` now wraps whichever World view the frame shows (it assumed `game.world`). The range's spawns create their coroutines (D29), so the preview's allocation guard flies the range without targets. Assets: pixel maps `shots/{bomb,two-way,torpedo,tail,vertical,free,twin}`, generator `procedural/weapons.mjs` (`shots/blast`, `shots/ripple`, `shots/cyclone`). Golden replays re-blessed in `06e47aa` (the new content shifts sprite ids / enemy spec indices, the Free Way direction is hashed — outcomes unchanged); new scenarios `zone-a-type-b`, `zone-a-edit` and, in the test round (no re-bless), `zone-a-type-c`, `zone-a-type-d` — every Types B–D weapon in a golden run. `terrain-edge`'s CPU-bound linear-scan test got a 30 s timeout (it timed out under machine load). **Review round 1** (1 major, fixed in `6f655b3`): `RIPPLE_RING_WIDTH` was public API of `core/weapons` but missing from the package entry — re-exported, and `packages/core/test/index.test.ts` now fails whenever any runtime export of a `src/<module>/index.ts` (other than `moduleInfo`) is missing from `src/index.ts` or differs there (`Object.is`, for bullets' `NaN` sentinel); a scan found no other miss. **Bugs found by the TEST agent:** `core/weapons` — a Spread Bomb's blast ignored its hit cooldown on armour and armoured boss parts (the armour bypass of the piercing cooldown applied to every piercing shot), so it hit armour every tick and clinked 3 times in its 12 ticks instead of at most once per `hitCooldownTicks` (2 clinks); the bypass now applies only to shots that die on armour (golden hashes unchanged). `core/scenes` — closing the Auto Power-Up order editor, even with Back and no edit, dropped every order entry past its 12 rows; they are now kept after the edited rows. Docs: new `docs/dev/meter-arsenal.md`; weapons-and-options, powerups-and-shields, scenes-and-ui, difficulty-and-rank, debug-and-replays (golden table), rendering-and-shell (scene view, `weapon-select.spec.ts`), content-data, asset-pipeline, sim-world, bosses-and-warning, death-and-scoring, zone-a-and-playtest, architecture, conventions (M2-03 hot-path rules), repo-layout, build-test-deploy, API reference; client `preview-build.md` (Choosing your weapons, TV checks 31–35, troubleshooting), `controls.md`, client README; TSDoc completed (preview docblock corrected — drawn full screen, not beside the panel —, `@throws` for a bad Weapon Edit on `createWorld` / `createWeaponSystem`, `setArsenal` remarks and example, `equipHighlighted` arming Mega Crash only for that choice, config `specRefs` §7 / §16). |
| M2-04 | Option & shield variants + Option Hunter | done | 1 | 136 | 1434577, 60b1328, 6eb60b8 (+ this row fix) | `GameConfig.optionChoice` (`OPTION_CHOICES`: trail default, snake, formation, rotate) and `ShieldChoice` grown to forceField / shield / freeShield / rotateShield / reduce, both in `ArsenalChoice` / `arsenalMatches` and replay headers (format unchanged: a missing key resolves to the default). **Spread / extend deviates from the plan's "hold PowerUp on the Option slot"**: `PowerUp` held ≥ `OPTION_HOLD_TICKS` (15) extends Formation / Rotate **wherever the meter cursor is** — equipping happens on the press edge, so a hold never conflicts and a quick tap never moves the Options — and **Special** (remote Ch+, keyboard V, pad Y; bound since M1-05 but unused) toggles them out / in (`OptionGroup.steer`, phase 2, 12 whole steps). Snake = a screen-space chain whose 16-px links are only pulled (swings out opposite to the motion, keeps its shape when the ship stops); Formation = a `>` spreading into a `V`; Rotate = an even orbit, radius 20 → 40 extended; the trail keeps recording under every type. `OptionGroup.follow` keeps only the trail inline — the new types' placement is the private `place()`: with everything in one method V8 stopped inlining it and boxed a fractional argument (the allocation guard caught it). Shields: two families — fields (Force Field, Reduce) through `playerHit`; pods (front Shield, Free Shield, Rotate Shield) that **never cover the ship itself** and stop only the enemy bullets (used up) and bodies (fly on) touching them, not lasers, boss parts or terrain; 14 hits and own i-frames per pod, the state's hits = the pods' sum. Free Shield: each `?` attaches a pair at the player's last 8-way direction (`WeaponSystem.freeWayHeading`), up to 4 pods; `?` stays equippable while a pair fits or one is worn, then replaces the most worn pair. Reduce: 2 hits, `ShieldState.hurtScale` ⅓ → ⅔ → 1 scales every hurt-circle test (bullets, lasers, bending lasers, enemy and boss contact, the debug overlay), the terrain box untouched; rank counts Reduce +2 instead of a shield's +4. FULL BARRIER = `refillShield` (every pod slot back in place). Option Hunter = the enemy **data flag** `optionHunter` + behaviour `hunter.option` (variants rear / front / dive; line up via a re-aimed `Waypoint` mover, wind up, charge): the enemy system refuses a hunter's spawn while no ship has an Option, pushes the new `SFX OptionHunter` (23), makes it armoured and gives it no contact damage; `ScriptApi.camera` added for the view conversion. Stealing is `EnemySystem.huntOptions` in phase 7, **before the power-ups** (a Mega Crash in the same tick frees what was just taken): the first Option touched and every one behind it in the chain, up to 8 carried, `SFX OptionStolen` (24); carried Options are drawn grey in the new `EnemySystem.carriedBatch`, the view's last batch; a killed hunter drops `DropKind.FreeOption` per Option → `ItemKind.FreeOption` items that drift, can be re-collected by anyone and expire after 600 ticks; a hunter that escapes keeps them. Blue capsule: `ENEMY_DROPS` gained `blueCapsule` (enemy and formation drops), `DropKind.BlueCapsule` / `ItemKind.BlueCapsule`; `EnemySystem.clearOnScreen` kills only on-screen enemies that are not `megaCrashImmune` — no bullet cancelled, the meter not advanced. **Zone A content unchanged** (its 4-way rules and bot budgets were tuned without hunters): hunters in `content/enemies/option-hunters.enemies.json`, `carrier-blue` in `test-range.enemies.json`, the dev stage `content/stages/hunter-range.stage.json`. Weapon select: new OPTION row (`WeaponSelectItem.Option` 4 — `?` is now 5 … START 9), `OPTION_CHOICE_LABELS`, five `SHIELD_CHOICE_LABELS`; the preview toggles the spread every `PREVIEW_SPREAD_TICKS` (90) while OPTION is focused. Golden replays re-blessed in `1434577` (new hashed state; the new enemies file shifts zone A's enemy spec indices; all eight outcomes unchanged) with new scenarios zone-a-rotate and zone-a-reduce; the test round added zone-a-snake and zone-a-free-shield (no re-bless). **Bug fixed by the test agent:** `core/shields` `absorbPodHit` accepted a fractional pod slot (0.5 passed the range check and read `undefined` hits), costing the shield a hit and reporting Broke without touching any pod, and `podWearFrame` returned NaN for such a slot — both now treat a slot that is not a whole number as not in use (found by shields-variants-edge; golden hashes unchanged). Docs: new `docs/dev/options-shields-hunter.md`; weapons-and-options, powerups-and-shields, enemies-and-behaviors, meter-arsenal, scenes-and-ui, difficulty-and-rank, debug-and-replays (golden table), sim-world, bullets-and-patterns, bosses-and-warning, audio, content-data, asset-pipeline, rendering-and-shell, stage-runtime, zone-a-and-playtest, architecture, conventions (M2-04 hot-path rule), repo-layout, build-test-deploy, engine-foundations, API reference; client `preview-build.md` (OPTION and `?` choices, the other shields, the blue capsule, the new Option Hunter range section, TV checks 36–39, troubleshooting), `controls.md` (Special / held PowerUp), `debug-tools.md`, client README; TSDoc completed (`EnemySystem.kill` / `sync` / `huntOptions` / `clearOnScreen`, `buildDebugOutlines` and Reduce, overlong doc lines reflowed). |
| M2-05 | Direct mode & ship select | done | 1 | 114 | f68cead, 0767e27, 3e7b658 (+ this row fix) | `GameConfig.shipId` (default `DEFAULT_SHIP_ID` `kestrel`; `createWorld` flies that ship, else the content's first) and `powerUpMode: 'direct'` accepted (`POWER_UP_MODES`); `ShipChoice` / `withShip` / `shipMatches`; both in replay headers (format unchanged: a missing `shipId` resolves to the KESTREL). Content: ship `mode` (meter default / direct) and `startSpeedLevel` (default 0, must index `speeds`) — `manta.player.json` 1.75 / 2.25 / 2.75 starting at 2.25, D3's "fixed 2.25 with a 3-step toggle" as data; **`startSpeedLevel` applies only in Direct mode** (the KESTREL's Speed Ups still start at 0). `weapons` files' `families` (`WeaponFamilySpec`: label ≤ 5, slot main / sub, 1–9 levels of 1–8 emitters `{ weapon, angle?, ox?, oy? }` with optional `refireTicks` / `volleys`) → `ContentDb.weaponFamilies`, a fifth loader pass reports a family weapon of another slot; stage `directItems` (1–256 colours, empty = `DEFAULT_DIRECT_ITEM_PLAN`). `EnemyDrop` gained `powerup` = `DropKind.PowerUp` 3 — **`DropKind.FreeOption` moved from 3 to 4** so the content drops keep their numbers (freed Options are never saved). **A `capsule` drop also becomes the next planned item in Direct mode**, like `powerup`, so zone A gained only a 27-entry `directItems` plan and **its events are unchanged**; the plan cycles and **never rewinds** at checkpoints (`planCursor`, hashed). The "coloured lead enemy" is a carrier with `drop: "powerup"` (`lead-carrier`; zone A's red `tender` carriers act the same way for the MANTA); the pincer wave is `cube.pincer` in a `formation` of six and reuses the existing "every member killed → the drop at the last kill" rule; dev stage `direct-range`. Items (`ItemKind.DirectRed` … `DirectOctagon` 3–8, 300 points) drift with the view like freed Options (−0.35 px/tick, ±0.3, bouncing), live 600 ticks, blink the last 120; effects in `PowerUpSystem.collectDirect`: red / green a level up to the family's top, blue `collectArm`, orange +1 life up to 9, **yellow = Mega Crash's screen clear with no boss damage** ("heavy damage to mid-bosses" waits for the mid-bosses of M2-09), octagon = the next main family keeping the level. Weapons: every weapon a family fires gets a **direct role after the four meter ones, so `liveCounts` uses a stride of 36 (`WEAPON_ROLE_SLOTS`) instead of 4**; a weapon group of `n` shots fires all or nothing while `live + n ≤ volleys × n`; **main and sub volleys reuse the ship's main and missile autofire timers**; behaviours `direct.bolt` (heading, still `frame` or the octant frame with `turn`) and `direct.bomb`; `Loadout.shot` / `sub` / `family` (hashed), `applyDirectLoadout` (`'full'` = both levels 8 + the gold Hyper Arm). Arm = `ShieldKind.Arm` 6 with `tier` / `charge` (hashed): 1 / 4 / 9 blue items → 3 / 4 / 5 hits, every blue item repairs, absorbs terrain with the 8-tick i-frames, a break or a death starts the count over. Speed press (remote Ch−) cycles the speed level in phase 2 in Direct mode (the PowerUp press is ignored there, the Speed press in meter mode); `applyDirectDeathPenalty` (every preset takes the Arm; classic one shot level else one sub level; arcade both levels and the family; the speed stays); `directPowerRank` = `floor((shot + sub) / 2)` + 2 / 3 / 4 by tier (12 at full power, like the meter ship). HUD: tier pips SHOT / SUB / ARM / SPD + the family label; **the HUD list grew to 64 commands / 9 strings** (`HUD_COMMAND_COUNT`, `HUD_STRING_COUNT`; meter HUDs use strings 0–3 only). **Ship select** (`ShipSelectScene`, overlay) between the difficulty menu and the weapon select, **skipped when the content has a single ship**; OK on the MANTA starts the game at once; **Back from the weapon select now returns to the ship select**; session hi-scores kept **per power-up mode and difficulty** (the save's `direct-*` tables); **UI string slots 160 → 192**; every flow-driving integration / shell / app / e2e spec presses one more OK (KESTREL). Assets: `ships/manta`, `enemies/cube`, generator `procedural/direct.mjs`; the atlas stays 512×512. **The Direct-mode allocation guard excludes enemy spawns** — each spawn creates its behaviour coroutine (D29), ~70–80 KB on the direct range alone, the same as a meter ship flying it. **The ESLint rules integration test now warms ESLint up once in `beforeAll` (120 s hook timeout)** — its first check timed out under the full `pnpm test` load. Golden replays re-blessed in `f68cead` (new hashed state and content — all twelve outcomes unchanged) with new scenarios zone-a-manta and zone-a-manta-boss; the test round added zone-a-manta-deaths (no re-bless). **Bugs fixed by the test agent:** `core/powerups` `collectDirect` pushed the `CapsulePickup` cue for an item index outside `DIRECT_ITEMS` (−1, 6, 0.5, NaN) before returning false — a bad player or item now does nothing; `core/ui` `buildHud` threw a TypeError in Direct mode (`families[-1].label`) for a negative `Loadout.family` although `core/weapons` fires the first family for it — the HUD now reads the index like the weapons; and for the same negative index `collectDirect`'s red item capped at 0 and the octagon switched to the family already firing — both now read it as the first family (found by powerups-direct-edge / ui-hud-direct-edge; golden hashes unchanged). Docs: new `docs/dev/direct-mode.md`; api-reference (config, data, world, player, debug, enemies, behaviors, rank, weapons, powerups, shields, ui, scenes, save, planned list, web boot, asset generators, golden), powerups-and-shields, weapons-and-options, options-shields-hunter, meter-arsenal, scenes-and-ui, difficulty-and-rank, death-and-scoring, debug-and-replays (fifteen golden runs), sim-world, enemies-and-behaviors, content-data, asset-pipeline, stage-runtime, zone-a-and-playtest, saves-and-options, audio, rendering-and-shell, build-test-deploy, architecture, conventions (M2-05 hot-path rule, the ESLint warm-up), repo-layout, docs indexes; client `preview-build.md` (new sections Choosing your ship, The MANTA, The Direct range; TV checks 40–43; sounds; troubleshooting), `controls.md` (Speed on Ch− / Left Shift / LB-RB, OK inert for the MANTA, the ship select), client README; READMEs (root, core, web, tizen, assets), CHANGELOG; TSDoc completed (`resolvePlayerShip` and the player module's ships, `hiScoreModeKey` / save tables, `PowerUpSystem.resolve` / `clear`, `Hud.update` and other overlong doc lines reflowed, `moduleInfo.specRefs` of behaviors / ui / rank gained §6). |
| M2-06 | Two-player simultaneous co-op | done | 2 | 66 | 2ebb4a4, 4f912fd, ea8f0d7, dd4ca13 (+ this row fix) | `GameConfig.coop` (default `false`) and **`coopExtra` in the config** (default `DEFAULT_COOP_EXTRA` 0.5, 0–`MAX_COOP_EXTRA` 4), not in a rules file — sim-affecting, so replay headers record both (format unchanged: a missing key decodes to a one-player game); `withCoop`. The **title menu is 1 PLAYER / 2 PLAYERS / OPTIONS / EXIT** (`TitleItem.Start` 0, `TwoPlayers` 1, `Options` 2, `Exit` 3 — tests and e2e specs that walked down to OPTIONS press Down once more); both go through the difficulty, ship and weapon select, `SceneFlow.choosePlayers` folds `withCoop` into every difficulty's armed config; ship and loadout are session-wide. **Drop-in join**: `JOIN_ACTIONS` = Confirm \| Pause, read in World **phase 1** (also during hit-stop) for a slot that `playerCanJoin` (co-op World, `playing` / `bossWarning`, inactive or out with continues left); `joinPlayer` covers both a fresh join (startingLives, the creation loadout, score 0) and a **mid-game per-player continue** (lives, power reset + starting loadout, continue digit, **no stage restart**), a blinking `respawnPlayer` fly-in and the new `SFX_CUES.PlayerJoin` (25, synthesized preset); the game scene no longer pauses on a joinable player's START (any other Pause / Back — read per player now — still pauses). **Continues are per player**: `continuesLeft` = `config.continues` − the player's own `PlayerScore.continues`; `canContinue` needs one active player with continues left; `continueWorld(world, who)` takes a player mask and the co-op countdown passes the players who pressed OK (one-player: any controller); `World.continuesUsed` counts continue events. **'Leave' = running out of lives** (the other plays on; game over when every active player is out); there is **no host-driven leave** — a disconnected pad's ship stays in play — because only recorded input may change the sim. **Device routing by seats** (`input-web` `web-input` → `implemented`): `WebInput.setSeats` ← `Game.inputSeats` (2 while a co-op game or its continue countdown is on top; the shell forwards it before the frame's ticks, `ShellInput.setSeats?` optional). With one seat **every device, pads included, drives player 1** (pad slot 1 is no longer always P2); with two the keyboard / remote is P1's and an unassigned pad drives P1 until its first join press (a gamepad-profile **menu**-table Confirm / Pause button: A, START) takes P2's seat and is forwarded as a latched Confirm; seats persist until disconnect; `PAD_SEAT_NONE` / `PAD_SEAT_P2`, `padSeat`. **Split keyboard** = a keyboard profile's new optional `split` section (validated like `context`, keyboard profiles only, no key in both halves → `splitTables`, a second keyboard source `splitKeyboard`): `keyboard-split` WASD + F / G + Esc / Q vs arrows + K / L + Enter, offered in Options → CONTROLS on the web only. **Aim tie-break and item ownership were already correct** (nearest living ship / first ship to touch, P1 on ties) — tested for co-op, no code change. `coopExtra`: while two ships are in play each capsule / power-up drop adds to a hashed `Float64Array` credit (`PowerUpSystem.coopCredit`, never reset) and each whole credit drops one more item `COOP_EXTRA_OFFSET` (12) px below (a capsule, or the plan's next item in Direct mode; blue capsule / freed Options not scaled). **P2 palette swap**: `scripts/assets/coop.mjs` derives `<name>@p2` (red ↔ blue) for every `ships/*` sprite and `hud/life`; `loadContent` interns `<ship>@p2` (`P2_SPRITE_SUFFIX`, `PlayerShipSpec.spriteP2Id`, -1 for the built-in ship); `UI_SPRITES` gained `hud/life@p2` (`UiSprites.lifeP2`); atlas stays 512×512. **HUD**: `HudPlayerState` / `hudPlayerState` (Playing / Join / Continue / Out / Absent — **repeats the World's join rule** because `core/world` imports `core/ui`); a blinking `PRESS START` (`HUD_PROMPT_BLINK_TICKS` 32) for a joinable slot; the bottom bar splits into two 192-px compact halves (`METER_SHORT_LABELS`, compact Direct pips) **only while both ships are active**, an out player's half `PRESS START` / `GAME OVER`; **`HUD_STRING_COUNT` 9 → 22, `HUD_COMMAND_COUNT` 64 → 96**; co-op strings written only when drawn, so 4-slot one-player lists still work; `Hud.update` compares two `Int32Array`s of 12 values per player. End screens show both scores, co-op rows recorded with the hi-score mode `2p` (same tables per power-up mode and difficulty; `NEW HI-SCORE` follows player 1's rank). **Golden replays re-blessed** (the hashed co-op credit and the sprite table's new `@p2` names change every hash — all 15 outcomes unchanged); new scenarios `zone-a-coop` (two 4-way bots, P2 from tick 300, stage clear) and `zone-a-coop-deaths` (a weaving P2 that dies seven times and continues twice with START while P1 plays on) — `GoldenScenario.p2` / `GoldenOutcome.p2`; `fourWayBot(player)` flies any slot. 30 s timeouts on four load-sensitive tests (the zone A playtests, the scene-flow allocation guard, the alloc helper's reclaimed-garbage test, the trig-table CLI test). **Review round 1:** `PAD_SEAT_NONE` / `PAD_SEAT_P2` were missing from the `@shmup/input-web` package entry (fixed in `4f912fd`, with an export test and the API reference rows). **Bugs found by the TEST agent (fixed in `ea8f0d7`):** (1) a seated pad that dropped off a **shorter** `getGamepads()` list kept player 2's seat forever (`padSeats` was only reset for indices < `pads.length`), so no other pad could join — a vanished pad (null / disconnected / unlisted) now gives the seat up before the same poll's seat-free check; (2) a seat change turned held buttons into new presses on the other player's slot — player 2's pad START opened the pause menu and the same held START then resumed it as player 1's fresh Pause press (and resuming re-paused on player 2's slot) — sources that change players (a seated pad, the split keyboard's right half) now carry their held actions without a press edge on the next poll; (3) docs only: the `SFX_CUES.PlayerJoin` TSDoc named a nonexistent `core/world` `continuePlayer`. Docs: new `docs/dev/coop.md`; client `preview-build.md` (Two players, TV checks 44–47) and `controls.md` (Two players, the split keyboard). |
| M2-07 | Advanced stage systems & Tiled import | done | 3 | 167 | 75470d0, 81e43ae, fd029ab, fc0f676, 478b0d7 (+ this row fix) | **Destructible tiles are tileset data, not a new collision type**: a colliding tile gets `hp` (1–255), `regen` (ticks, needs `hp`) and `score` and still collides as its `type` (`TilesetTables.hp` / `regen` / `score`); `terrain-a` gained `brick` (hp 4), `cube` (hp 2) and `tissue` (hp 3, regen 240), ids 18–20, with generated frames. `core/collision` `DestructibleTerrain` (one per World over its private map): a sparse table of **512** damaged / regrowing cells — a hit on a new cell while it is full is ignored unless it breaks the tile at once; regrowth waits while a keep-out rectangle (the ships' terrain boxes) overlaps the cell; a 64-entry change ring (`count` / `resets` / `cells`) is the new render-contract `TerrainChanges` (`TerrainView.changes`) that render-pixi's terrain binding follows. **The checkpoint rollback always restores the stage's original tiles** (broken back, placed gone) on every restart / jump / continue — no snapshot when a checkpoint is passed. **Moving blocks live inside the terrain queries** (`TerrainMap.blocks` = `TerrainBlocks`, ≤ 16, tested by `terrainAt` / `terrainRectHit` / `findFloor` / `findCeiling`); a `block` event's left edge is **world** x `event.x + screenX` (default 400), not camera-relative; a block needs a tilemap; drawn tile by tile in a `LayerId.Terrain` batch (≤ 256 tiles), back at age 0 after a restart. **Falling rocks are a mover** (`MoverKind.Ballistic` 9, content `ballistic`: `vx`, `vy`, `gravity`, `maxFall`, `trigger`, `land` pass / stop / shatter) with the proximity trigger built in; `MoverBody` gained `hw`; a landed `shatter` body is removed with `EnemySystem.destroy` (no score / drop, a formation member counts as escaped), any other wakes its script next tick. **Branches** = a stage table `{ id, flag, value }`; every event may name a `branch` and is skipped entirely (no runner part, no hooks) when not taken. **Region triggers** (world rectangle, `until`, ≤ 32 per stage) are probed by the living ships in tick phase 3 (`StageRunner.probe(ship)`); a restart applies the flag of a trigger behind it that had fired at the trigger's place in the timeline (an approximation of the tick it really fired on). `STAGE_STATE_SLOTS` **22 → 28** (hold, hold key, diagonal pan, trigger masks); `StageEventCode.Trigger` 8, `Block` 9. `hold` keys stop the camera exactly at their x like `lock` (not together with lock); `yOver` pans are linear in scroll x (not with `yTicks`); high-speed sections need no new data (speed keys / events up to 16 px/tick). **Gimmicks** (`core/behaviors`): `rock.fall`, `bubble.split` (new `BehaviorDef.death` / `EnemyBehavior.death` callback — never on a Mega Crash or the blue capsule), `volcano.lob` (`ScriptApi.setMoverOf`), `field.suction`, `tentacle.grab` (a lunge with a short pull field and a drawn chain — **no 'held ship' state**), `cube.stack` (seeded cube rush → the tileset's `cube` tile via `ScriptApi.placeTile` / `tileId`); World side `core/stage` `stage/systems.ts` `StageGimmicks` (`world.gimmicks`: destructible terrain, `MovingBlockSystem`, ≤ 8 pull fields, ≤ 8 chains of ≤ 16 links with the new engine sprite `gimmicks/chain-link`) in phases 2 / 3 / 5 / 9; the view appends the chain and block batches after the existing ones. **Tiled importer** `scripts/content/tiled-import.mjs` (`pnpm content:tiled`): objects read by `class` (or `type` before Tiled 1.9), spawns fire 400 px before their object with `y` made camera-relative through the imported keys (`cameraYAt`; a spawn during a timed pan is warned about on stderr), gids map to content tile ids by tileset order, flipped tiles and compressed / base64 layers refused; fixture `test/scripts/content/fixtures/tiled-sample.tmj` + expected JSON. Dev stage `gimmick-range` (`?stage=gimmick-range`, e2e `gimmicks.spec.ts`); zone A unchanged. **Goldens re-blessed** once for the new hash layout (runner slots, `mixGimmicks`) and the sprite ids shifted by the new engine sprite — verified against the old layout first (zone A's behaviour unchanged); three `gimmick-range` goldens added by the test step (the older 17 byte-identical). **Review fixes:** keep-out boxes were only set when the tileset had an `hp` tile, so a rush cube could be placed onto a ship (round 1); the importer wrote spawn `y` as a world y though the game reads it camera-relative (round 1); `resetTo` restarted overlapping diagonal pans at the wrong camera y (round 2). **Bug found by the TEST agent:** a checkpoint restart at x = a region trigger's `until` did not re-arm the unfired trigger although live play keeps it armed while `camera.x ≤ until` (a hold or lock on that x let live play fire it but not the restarted run); `restoreTrigger` / `applyEvent` now arm while `until ≥ camera.x`. Docs: new `docs/dev/advanced-stages.md`, tester guide's *The Gimmick range*. |
| M2-08 | Presentation polish: raster effects, palettes, visual options | done | 1 | 119 | 0b7bea2, 7a62546, cf869f5 (+ this row fix) | **Stage data, presentation only**: optional `raster` (≤ 8: `layer` far / mid / terrain, `kind` `wave` / `haze` / `lines`, playfield rows `top`…`bottom`, `amplitude` / `wavelength` / `period`, `factorTop` / `factorBottom` / `wrap`, camera-x `from` / `to`) and `cycles` (≤ 8: far / mid / terrain / ground / air, 2–8 exact `#rrggbb` colours, `ticks`; ≤ 8 cycled colours per layer) validated by `core/data`, handed to the renderer as the new render-contract `WorldView.effects` (`createStageEffectsView`) — never read by the sim, not hashed. A `lines` effect may list **`bands`** (strip heights) so each strip scrolls as one piece: plain per-row speeds on a uniform pattern turned into noise after a few hundred pixels of scroll. **One GLSL ES 1.0 filter per layer for both effects** (raster offset + palette cycle), attached only while an effect is in camera range (the `filters` list swapped only at range edges); the table is a **1×216 RGBA8 texture** (R, G = whole-pixel offset + 32768; B, A = wrap period), decoded exactly under `mediump`. Draw calls 2 plain / 5 / 7, budget 12; `EffectSettings.rasterEffects` turns them off. **Palette cycling matches the exact ramp colours in the existing RGBA art** — no indexed-colour sprites. The **GLSL ES 1.0 check** is a small test-side tokenising checker (`render-pixi/test/effects/glsl-es100.ts`, no new dependency) plus a real WebGL1 compile + link in the e2e test. The **Mega Crash flash is additive white at 0.7** on a second, `add`-blended overlay sprite (was 0.85 over the picture). **Display options** (`scaleMode` integer / fit / stretch, `screenShake`, `reduceFlashing`, `showHitbox`) join `UserOptions.display` with **no save migration**; four new Options rows (**`OptionsItem.Back` is now 9**), `UserOptionKind` 5–8, applied at boot (`applyDisplayOptions`; `ShellOptions.effects` still wins) and live (`connectOptionEvents`' `display` target). **Show hitbox** uses a new render-contract mirror `WorldView.hitboxes` / `HitboxBatch` (the World's `hitboxBatch`), drawn on `HITBOX` with the atlas's white pixel — no new sprite. **Render interpolation lives in the renderer** (`setInterpolation`), switched by a shell refresh probe using the **interquartile mean of the last 31 rAF deltas** (70 Hz threshold) — a median picked one extreme of alternating jitter; the blend travels as an object (`RenderBlend`) so fractional arguments are not boxed; a slot blends only when it kept its sprite and moved ≤ 24 px. `window.__shmupDebug` now exposes the **renderer** for the e2e tests. **Golden replays re-blessed** because the two new content sprites (`bg/sea-swell`, `bg/checker-floor`, generator `raster-bands`) shift sprite ids; the simulation is unchanged (verified by removing the new stage file). Dev stage `raster-range`; zone A untouched. The test round added `raster-range-god`, also played back with `raster` / `cycles` stripped (every hash matches) — the older files byte-identical. **Bugs found by the TEST agent:** (1) render-pixi renderer / layers: with interpolation on, the hitbox markers were drawn at the current tick under the interpolated camera — a tick of motion ahead of the interpolated ship; added `HitboxBinding.syncInterpolated` and a history reset when the markers were not drawn the previous frame. (2) shell frame-loop: reading `RefreshMonitor.hz` (a getter) boxed ~16 B on every per-frame read in `'auto'` mode; the monitor is now a class with plain `ready` / `hz` fields, and `createRefreshMonitor(NaN)` no longer builds a monitor that never becomes ready. (3) core data: a raster effect's or cycle's `to` was only checked against an explicit `from` (`to: 0` alone — an effect never on — was accepted); cycle colours at most 1 apart per channel on one layer are now rejected (the shader cannot tell them apart). (4) render-pixi palette: `colorCycleStep` returned NaN for a ramp length in (0, 1) and for NaN / infinite ticks. |
| M2-09 | Advanced bosses: mid-bosses, raids, multi-bosses | done | 2 | 90 | 8cdc9eb, 8d273b1, aba9a05, ef0564e (+ this row fix) | **Four boss slots** (`MAX_BOSSES` 4, 16 parts each; slot 0 is still `bosses.boss`): a part's hit id is `64 + slot × 16 + index` (`BossPart.global` = part slot), so `MAX_HIT_TARGETS` is **128**, the weapons' part cooldown tables are 32 × 64 and the World's `laserSources` list every slot's parts; `damagePart` / `isArmoured` take the **part slot** (= the part index for slot 0), `WeaponHost.bosses` reads `bosses.parts`. Stage bosses (role `boss`) are still one main encounter at a time; the last stage boss to end releases the lock and clears the stage (or brings the next rush boss); a boss entering during `update()` from another slot's clock counts its intro from the next tick (`Boss.enterTick`). **Sprites are not rotated** (Pixi's transform setters allocate): a turned part (`angle` / `spin`, binary units; children placed by the parent's world angle from a module-level copy of the sine table — a zero turn keeps the M1 translation exactly) shows its heading with **heading frames** (`turn`, `turnedFrame`) and is hit as a **circle** (`radius`, exclusive with `hurtbox`; the loader refuses `turn` on a box); `aimPart` takes the heading from the bullet system's `aimFrom`, so turrets are quantised like aimed shots. **Captains** are a boss-section role (`role: 'captain'`, `BossRole.Captain`): any free slot, `boss` event only (a `warning` naming one is a content issue), no lock, music or `bossWarning`, a short death (`CAPTAIN_CHAIN_TICKS` 48, medium shake, no flash / hit-stop, tally at 49, `Dead` at 72) that never clears the stage; archetypes `captain.ram`, `captain.launcher` (the boss's `minion` via `BossScriptApi.launch`), `captain.circler` (new `BossMotion.Orbit`), `captain.crab`. **Raids** (`raid` segments relative to the boss's origin): the boss is anchored where it entered (a `boss` event stops the camera at once) and from its fight drives the new **`StageRunner.follow(target)`** (`StageCameraTarget`, the system's `RaidCamera`; in free flight the camera's velocity); while following, the runner's timeline (keys, events, checkpoints, trigger disarms) stays at the x where the follow began; at death / escape the camera eases back exactly there (`RAID_RETURN_TICKS` / `BOSS_ESCAPE_TICKS`) and is handed back the tick after; raid parts fire only on screen (`BossPart.inView`, `boss.raid`). **Double bosses** (`partner`, `alternate`, `enrage`): the resting half moves to `BOSS_REST_X`, is drawn from the new `bosses.backBatch` (`LayerId.GroundEnemies`), is out of the grid, and its script and phase clock wait (its motion resumes on the way back); the survivor of a death enrages (fire waits × `fireRate`, track / orbit × `speed`, a jump to `enrage.phase`); the pair's link is cut both ways when either slot ends. **Boss inside a boss** (`inner`): revealed at the outer's final blast from its first core. **Timers** add `BossState.Escape` (6), `SimEventKind.BossEscaped` (14), `World.endingFlags` with `EndingFlag.BossEscaped` (hashed; M2-10 carries it). **HP bar**: model `BossHpBar` (`bosses.hpBar`: the cores and the parts a core requires of the stage bosses in play — else of the captains; full × progress in the intro, 0 while dying); view `core/ui` `bossHpBarFill` / `BOSS_HP_BAR_WIDTH`, `buildHud(…, bossHp)`, `Hud.showBossHp` (`BOSS` + a 64-px bar in place of the hi-score; `HUD_STRING_COUNT` 23, `HUD_COMMAND_COUNT` 100); behind the new display option `UserOptions.display.bossHpBar` (default **off**, save v1 without migration), the Options row BOSS HP (`OptionsItem.BossHp` 9, **Back 10**, panel 288×192) and `UserOptionKind.BossHpBar` (9) — **no host applies that event**: the scene flow copies the saved value into `Hud.showBossHp` every displayed frame. **Boss rush**: stage `type: 'bossRush'` + `rush` (`enemy`, `delay` 60, `warning`), run by the boss system (`rushIndex` / `rushDelay`, hashed); such a stage may not have an `end` event; a checkpoint restart brings the current entry again. `createBossSystem(host, behaviors, stage?)`; `BossHost` gained `enemies.spawn`, `endingFlags`, `stage.follow` and the camera's `vx` / `vy` (`BossCamera`). Content: `advanced-bosses.enemies.json` (four captains, IRON LEVIATHAN with LEVIATHAN HEART inside, the EMBER / FROST twins), the dev stages `captain-range`, `raid-range`, `twin-range`, `gauntlet-range`, the procedural generator `bosses` (`bosses/turret` with 16 heading frames, `orb`, `raid-hull`, `captain-shell` — the atlas page grew to 1024×512); zone A untouched. Not delivered here: boss / revenge DSL patterns and the yellow item's damage to mid-bosses (moved to the zones' steps). **Golden replays re-blessed** (`8cdc9eb`): the hash layout changed (four slots and their new fields, the 64-slot part tables, the rush state, the ending flags) and the new content shifts the sorted sprite and script ids — every replay's inputs, tick count and outcome identical; the stage-runtime integration test now defeats a fighting boss in any slot. The test round added `captain-range-god`, `raid-range-god`, `raid-range-escape` (the escape and `EndingFlag.BossEscaped`) and `twin-range-god` (the older files byte-identical). **Review fixes** (round 1, all confirmed): (1) core/stage: a raid's camera following fired stage timeline events up to the moved camera x — a pan over the stage's `end` cleared the stage mid-fight; the timeline now waits at the follow's start x (and the raid's return lands exactly on it). (2) core/bosses: a stale double-boss partner link hit whatever boss later took the dead partner's slot (enrage, escape, turns); links are cut both ways (`unlink`) and checked both ways (`mateOf`). (3) core/bosses: a timed inner boss 'escaped' to its on-screen reveal point; it now flies to its entry's off-screen intro start. **Bug found by the TEST agent:** core/bosses `turn()`: an alternating double boss whose partner's intro outlasted the first turn never turned again (the leader's clock was not re-armed, so the partner withdrew after its intro and rested for good); the leader now keeps its turn and retries after another `alternate` (shipped content and goldens unaffected). |
| M2-10 | Zone map, campaign flow, transitions & bonus stages | done | 2 | 155 | 10b5fea, ff1109e, 06474f7, 263b0d8 (+ this row fix) | **Campaign file** is `content/campaign/main.campaign.json` (the `<folder>/<name>.<kind>.json` rule; the plan said `campaign.json`), kind `campaign` owned by `core/data` (`data/campaign.ts`, `ContentDb.campaign`, one per content set): zones (`id`, 1–2-letter `label`, `name`, `stage`, ≤ 3 `preview` lines), `edges`, `endings` with run-flag conditions `all` / `none` over `RUN_FLAG_NAMES` (`bossEscaped`, `noDeath`, `noContinue`, `bonus`). Graph validation: unique ids, known zones, no self-loops / duplicates, ≤ 4 exits, every zone reachable and **every edge exactly one depth deeper** — so no cycles and every route ends in a final zone —, an unconditional ending per final zone, no bonus stage as a zone. Derived depth / row / exits / final / routes (16); `selectCampaignEnding` is the ending hook. **A flow game is a campaign run only when the host config's stage is the start zone's stage** (the shipped game — zone A); `?stage=` dev stages and open space keep the M1 single-stage flow and stage-clear screen unchanged. Run state `core/scenes` `run.ts` (`RunState`, `SceneFlow.run`): each zone a fresh World (`runWorldConfig` + `prepareRunWorld`), the players carried (`captureCarry` / `applyCarry`: score with the continue digit, lives, loadout, speed, meter cursor, shield; down → respawns blinking, out → stays out), `rankInputs.stage` = zones cleared + 1, RETRY STAGE restarts the zone from its entry state. **Launch intro** = the existing fly-in plus a zone title card (`ZONE_CARD_TICKS` 150); the **fly-out** is a new `PlayerState` `leaving` (appended, code 5; `flyOutPlayer`), from the tick after the status turns `stageClear` — unhittable, not firing, Options trailing. **Zone tally** (`StageClearScene.zoneMode`, 300 ticks): kill rate from the new hashed `EnemySystem.stats` (`EnemyStats`) × 100 points, plus 100 points per whole second a **defeated** (not escaped) stage boss's fight stayed under 90 s; paid to every player in play, extends checked. The run is recorded only at the final zone's clear (or a game over); practice records nothing. **Next zone "prepared on the map"** = new `SimEventKind.PrepareStage` (15, id = stage index); `@shmup/shell` `connectStagePreparation` prepares that stage's music set plus the title theme. Tilesets need nothing (one resident atlas — texture unloading is M2-17). The map fades the music out (no `ZoneMap` track yet). `MapScene`: one column per depth, Up / Down among the exits, OK → `LAUNCH` 60 ticks, Back → quit to title?; the UI list grew to **384 commands / 224 string slots**. **Bonus framework**: stage type `bonus`, the `bonus` event (`gap` / `ground` / `digit`, defaulted `until`, ≤ 8 per stage, `StageEventCode.Bonus` 10), `core/stage` `bonus.ts` `BonusEntrances` (`World.bonus`, phase 3, hashed, re-armed by a restart); warp after 40 ticks; a death in a bonus stage always fails it (back after 60 ticks at the entrance, entrances locked); a bonus stage's clear is the zone's clear (boss skipped, run flag `bonus`). Drops `oneUp` / `bonusCapsule` (`DropKind` 4 / 5 — **`FreeOption` is now 6**), `ItemKind` 9 / 10, sprites `items/1up` / `items/capsule-bonus`. Dev stages `bonus-range` → `bonus-vault`. **No campaign zone has an entrance yet** (M2-11 puts one in zone B). **Ending hook** = `EndingScene` placeholder showing the ending `selectCampaignEnding` picked (route, score, flag lines). **Practice** = `SceneFlow.startPractice(zone, checkpoint)`; records nothing, returns to the title (the select screen is M2-15). **Stub zones B–I** (≈ 45–70 s) reuse zone A's roster and existing bosses (HALCYON BULWARK in B / D / F / H, the twins in C / E / G, IRON LEVIATHAN in I); the zone-A-only design-rule suite does not cover them. **Playtests**: `test/playtest/campaign.ts` + `campaign-routes-b` / `-c.test.ts` fly all 16 routes with the 4-way bot in god mode; the bot now faces the first **fighting** boss slot (`mainBoss` — it never found a double boss's survivor in slot 1). **Goldens re-blessed** (`10b5fea`: hash layout — bonus entrances, enemy totals —, two new engine sprites and `bonus.enemies.json` shift ids; zone A inputs, ticks and outcomes unchanged); `captain-range-god`, `raid-range-god`, `twin-range-god` re-recorded with the improved bot (same outcomes). Shell tests use `strings` instead of `campaign` as the example of an unowned foreign kind. **Review fix** (round 1, confirmed): only the zone map sent `PrepareStage`, so the engine kept the last zone's music set and run 2's zone A (and a practice zone) played the upper zone's track; `FlowControl.prepareStage` now dedupes against `preparedStage` (the host config's stage at boot), `TitleScene.enter` prepares the next run's start stage, `beginRun` / `startPractice` prepare their own (no sim or golden change). Not covered: a bonus stage's own `stages`-scoped track (the warp sends no `PrepareStage`). **Bugs found by the TEST agent:** (1) core/data `campaign.ts`: a self-loop edge was reported twice, the second time as a contradictory depth error — the layer check skips self-loops; (2) core/scenes: the largest maps the validation accepts overflowed the 384-command UI list (a 32-zone map dropped 603 commands, losing nodes and the preview panel) — `MapScene.edgeDots` fits the edge dots to `MAP_EDGE_DOT_BUDGET` (the shipped map keeps 7 per edge); (3) shell dispatch: `connectStagePreparation` `unshift`ed the title theme into the cue picker's own array, so a memoised picker grew one Title entry per event — it builds a new list, the title first and once. Docs: [`docs/dev/campaign-and-bonus-stages.md`](docs/dev/campaign-and-bonus-stages.md), the tester guide's zone map and hidden bonus stages sections, API reference and the touched guides. |
| M2-11 | Zones B & C | done | 2 | 43 | 254491e, c428e1d, 9c9fa71, 1a20f6d (+ this row fix) | **Zone B, BRINE NEBULA** (`zone-b.stage.json`, 9,800 px, tileset `terrain-reef`, four checkpoints, a 1.4 px/tick riptide, `wave` rasters on the palette-cycled `bg/brine-sea` and the nebula band) and **zone C, DUNE EXPANSE** (`zone-c.stage.json`, 9,600 px, `terrain-dune`, four checkpoints, a 1.3 px/tick sandstorm run, `haze` rasters) replace their stubs, each with a roster file (`zone-b` / `zone-c.enemies.json`, sorting after zone A's so no earlier spec index moved), a 26-entry `directItems` plan, stage-scoped songs (`zone-b` / `boss-b`, `zone-c` / `boss-c`) and procedural art. **As-built deviations:** zone B's mid-boss **SPUME HERALD** (SH-02) is a captain on the existing `captain.launcher` with a 30-s time limit; zone C has **no mid-boss** (the plan names one only for B). GALVANIC MAW's **homing rockets are shootable minion enemies** (`maw-rocket`, `rocket.homing`) launched from its pods, not homing bullets — `BossScriptApi` has no homing-bullet call and the minion route needed no engine change. The **mouth weak point** is a `whenOpen` core; a new `gape` tunable on `boss.maw` moves the parts attached to the core (the jaws) apart while it is open. "Enemies inside bubbles" is **content only**: a `bubble.split` bubble whose `child` is one fish. The content test counts new enemy types as **distinct sprites** (a floor and a ceiling variant are one type): B places six, C five; the capsule carriers reuse zone A's `tender`. New behaviours `rocket.homing`, `worm.burst` (a formation is one worm: a `Ballistic` leader with a proximity trigger passing through the terrain, the members `Follow`), `boss.maw`, `boss.widow` (random steps in a box, spreads from the head behind its two fangs, spider drones, detached silk-line lasers one at a time). The 4-way bot does not predict ground enemies' vertical motion, so the worms trigger at 190 px (the arc comes down in front of the ship); without god mode the bot clears B with no death and C with one (reported, not asserted); with god mode B 225 s (GALVANIC MAW 24 s), C 243 s (SANDGRAVE WIDOW 51 s). The playtest harness takes its boss statistics from the **stage boss in any slot**, never a captain (zone B's captain arrives first in slot 0). The recovery-rule check moved into a shared helper, `test/playtest/recovery.ts`, used by zone A's recovery test and the new `zone-bc-recovery.test.ts` (all eight checkpoints). All new sprites are drawn by the two new generators `brine.mjs` / `dune.mjs` (no pixel maps), with shape helpers `fillEllipse` / `drawLine` in `common.mjs` and recoloured tilesets from `terrain.mjs`'s `TERRAIN_PALETTES`. The bonus stage is `brine-grotto` (**PEARL GROTTO**, type `bonus`, 1,800 px): it uses the zone's music, reuses the bonus vault's carriers and is entered through a `gap` at world x 5,616–5,680, y 0–24 marked by two static reef blocks. **Goldens:** all 28 existing files re-blessed in `254491e` (the new sprites and scripts shift ids in the state hash — inputs, tick counts and outcomes identical); new `zone-b-god`, `zone-c-god`, `brine-grotto-god`, and from the test round `zone-b-deaths`, `zone-c-bot` (33 in all). The Tizen `app.js` is **307.5 KB gzip of its 350 KB budget** (the zone content adds ≈ 6 KB gzip — zones D–I need the same care). **Review fix** (round 1, confirmed, `c428e1d`): `boss.maw`'s jaws drifted 1 px when two phases used different `gape` values (GALVANIC MAW 4, 4, 5: in phase 3 the jaws sat at ±8 shut / ±13 open instead of ±9 / ±14) — a phase start undid the new phase's gape while the jaws had been moved by the previous one. Fixed in the engine: `BossPart.restX` / `restY` (the boss data's offsets, set on activation) and `setJaws(api, apart)` placing each jaw absolutely at rest ± apart; every phase starts shut and at rest. Only `zone-b-god` re-blessed (desync at tick 13,200; still clears with no deaths, now 13,538 ticks / 70,480 points, was 13,523 / 70,450). **Bugs found by the TEST agent:** none (43 tests added: behaviour edges, allocation guards for rockets and worms, procedural edges, Direct mode, the browser spec, two goldens). Docs: [`docs/dev/zones-b-and-c.md`](docs/dev/zones-b-and-c.md), the tester guide's zone B / C sections (PEARL GROTTO, TV checks 57–60), API reference and the touched guides. |
| M2-12 | Zones D & E | done | 1 | 69 | 022d0b6, e0d7cdd, ad11862 (+ this row fix) | **Zone D, MAGMA DEEP** (`zone-d.stage.json`, 9,600 px, tileset `terrain-magma`, a **400-px-tall map** — `rowsTall` 50 —, four checkpoints, a 1.3 px/tick lava river) and **zone E, TEMPEST RIDGE** (`zone-e.stage.json`, 9,800 px, `terrain-ridge`, four checkpoints, a 1.4 px/tick gale run, six parallax bands — two palette-cycled storm-cloud bands with a `wave`, a mountain band, three rows of rain) replace their stubs, each with a roster file (`zone-d` / `zone-e.enemies.json`: six and five new types, counted against every zone flown before), a 26-entry `directItems` plan, stage-scoped songs (`zone-d` / `boss-d` BASTION OF CINDERS, `zone-e` / `boss-e` STEED OF THE SQUALL), paths (`zone-d.paths.json` bat swoops, `zone-e.paths.json` rear-entry kite loops), the DSL pattern `tempest.bolt` and procedural art (`magma.mjs` 13 sprites, `tempest.mjs` 17; `TERRAIN_PALETTES` `tiles/terrain-magma` / `-ridge`). **As-built deviations:** **no new engine code** — CINDER BASTION's **rotating shield arms are data**: a `hub` part attached to the core (no hurtbox, no sprite) plus four armoured circle-hurtbox arm parts on it; `boss.bastion` only sets the hub's `spin` per phase (the arms keep their angle, so a phase change never makes them jump) and reverses it every `reverseTicks`. SQUALL STEED's homing minis are **minions** (`steed-foal`, `rocket.homing`) launched from the `whenOpen` chest, following M2-11's approach. New enemy behaviour **`rear.swoop`** for the jumpers that enter from behind (one `Waypoint` mover, one wake); the gale kites are `fan.loop` on new rear-entry paths, spawned at a negative `screenX`. **The dive** is a scroll stop that pans the camera 200 px down (camera key at 3,560: `hold` 150, `yTo` 200) on the 50-row map; the lava band sits at `y` 256 so it rises into view with the dive. **The destructible maze** is seven `brick` walls (hp 4) written as `rle` rows over the generated caves (world x 4,800–5,776); **every wall has a 48-px gap** — a gapless wall killed the 4-way bot (its lane-centred ship straddles two tile rows) and 48 px is the stage-runtime open-space rule; shooting through a wall is the shortcut. **Balance from bot runs:** turrets and crawlers moved out of the maze (a turret passing under the ship fires up its column), SQUALL STEED's bob cut from 40 px to 14–18 px (the 40-px bob made the fight last three minutes), CINDER BASTION's core raised to 80 hp (60 fell in 10 s with an Option). The seahorse's orbit speed is the tunable **`bobSpeed`**, because the content test reads a boss phase's `speed` as px/tick. The content test's zones block now covers **B–E**: new types counted against every zone a run can have flown before (`earlier`), ground-enemy placement checked at the camera y each event fires at, the maximum number of simultaneous laser lanes set per boss (`lanes`: the bastion 2 — never under the 16-px gap —, the seahorse 0). `stage-runtime.test.ts`'s open-space check now measures the rows the camera shows (zone D's caves are map rows 200–399). Measured with the 4-way bot and god mode: D 228 s (CINDER BASTION 14 s), E 246 s (SQUALL STEED 51 s); without god mode both clear with one death. **Golden replays re-blessed** in `022d0b6` (only hashes changed in all 33 older files — inputs, ticks, headers, outcomes identical); new `zone-d-god` (13,698 ticks) and `zone-e-god` (14,736 ticks), and from the test round `zone-d-bot`, `zone-d-boss`, `zone-e-bot` (38 in all; no existing golden changed in the test round). `playGolden` gained an optional per-tick observer. The Tizen `app.js` is **313.5 KB gzip of its 350 KB budget** (307.5 after M2-11). **Gotcha:** `StageRunner.jumpTo` moves the camera, not the ships — a test jumping past the dive calls `spawnPlayer(ship, camera)` again. Review: 1 round, clean. **Bugs found by the TEST agent:** (1) core/behaviors `rear.swoop`: a jumper spawned right of its `turnX` never fired — its shot was timed as if it were already at the turn point and fell inside the enemy's startup (settle) delay; the approach is now timed from the distance either way (shipped jumpers all spawn behind the view, so no golden changed). (2) `scripts/assets/procedural/tempest.mjs`: the squall jumper's two animation frames were identical (the flame was drawn under the body and wings), so its jet never flickered; the flame now differs behind the tail. (3) `boss.steed` docblock wrongly said a phase change never moves it; a new bob height moves it up to the change of the radii — 2 px between the shipped phases (docs corrected, limit pinned in a test). Docs: [`docs/dev/zones-d-and-e.md`](docs/dev/zones-d-and-e.md), the tester guide's zone D / E sections (TV checks 61–64), API reference and the touched guides. |
| M2-13 | Zones F & G | done | 2 | 65 | 31f35db, db2e5b0, 6390711, 15845d1 (+ this row fix) | **Zone F, CELL VAULT** (`zone-f.stage.json`, 9,600 px, tileset `terrain-vault`, four checkpoints, a 1.3 px/tick pulse run, a far band of living cells — `bg/vault-membrane`, painted only in its palette cycle's four colours — pulsing and breathing with a `wave`) and **zone G, PRISM LABYRINTH** (`zone-g.stage.json`, 9,800 px, `terrain-prism`, four checkpoints, a 1.3 px/tick refraction run, glinting facets and a `haze` over the spires) replace their stubs, each with a roster file (`zone-f` / `zone-g.enemies.json`: six new types each, counted against A–E), a 26-entry `directItems` plan, stage-scoped songs (`zone-f` / `boss-f` REGENT OF THE VAULT, `zone-g` / `boss-g` THRONE OF FACETS), the DSL patterns `vault.spores` / `prism.fan` and procedural art (`vault.mjs`, `prism.mjs`, 14 sprites each; `TERRAIN_PALETTES` `tiles/terrain-vault` / `-prism`). New behaviours **`cell.chase`** (chasing cells; also the mitosis cell's `bubble.split` child and MANTLE REGENT's minion), **`boss.squid`** (MANTLE REGENT MR-06: two tentacles of a breakable root, armoured segments and a gun tip curl in front of the eye in a cycle; breaking one ends the first phase — `partsDestroyed` with `count` 1) and **`boss.facet`** (FACET MONARCH FM-07: the core `afterParts` of two crystals, two armoured arms waving like claws, detached lanes in the last phase) on one shared **curling-arm** rule (`isArm`, `armSide`, `curlArms`, `setArmCurl`, `armCurl`: chains of circle-hit parts turned alike relative to their parents, mirrored by the root's `restY`, the curl carried across phases); **no engine change** (M2-09's `spinPart` / `setPartAngle` / circle hurtboxes). **CELL VAULT's regenerating walls** are seven `tissue` walls (hp 3, `regen` 240, never grown into a ship) written as `rle` rows at world x 2,896–3,872, each with a 56-px gap; `vault-claw`s are M2-07's `tentacle.grab` on floor and ceiling. **PRISM LABYRINTH**: seven solid crystal walls (world x 3,392–4,272, hanging from the ceiling and rising from the floor in turn, drawn with the tileset's wall-edge, floor and ceiling tiles), four seeded `cube.stack` rushes stacking onto 22 short crystal pillars, and the **second hidden bonus stage** `glimmer-cache.stage.json` (GLIMMER CACHE, type `bonus`, 1,800 px: bonus-capsule carriers, a 1UP carrier, a cube rush, two `cube` block walls; zone G's resident songs) behind a **`ground` entrance** (the prism gallery's four facet turrets, window x 2,300–3,100) — B's is a `gap`, so the two open differently. **As-built deviations:** FACET MONARCH's first armour hitbox (on the housing) covered the core so every core hit clinked — the armour is now a separate hull part without a sprite behind the core, the housing sprite decoration only; no new path file (the halo crystals reuse zone A's `gyre-orbit-*` loops); zone G's map preview now reads `A RUSH OF CUBES THAT BUILD WALLS.` (the cubes charge, they do not fall); the labyrinth walls use the tileset's wall-edge, floor and ceiling tiles and the prism rock is lighter than the backdrop (the first colours let the walls vanish into it); the commits carry a `Claude-Session` line above the required `Co-Authored-By` last line. **Balance from bot runs:** turrets passing over / under the ship fire straight along its column, which a lane-only dodger cannot escape — fewer turrets, slower fire (170 / 180 ticks at 1.2 px/tick), the labyrinth's first wall moved to x 3,392, past the gallery; prism lenses and spore sacs fired point-blank fans as they drifted past — they now hover near the right edge on a `waypoint` mover, then drift off up or down; zone G's opening stretch was too tight for its mote streams (flatter floor, the ceiling from x 800); the rush cubes aim at the ship and mostly flew off the left edge — the pillars give them rock to stack on (11 cubes stacked at once in the god-mode run, from 1). Measured with the 4-way bot and god mode: F 219.8 s (MANTLE REGENT 23.5 s), G 234.1 s (FACET MONARCH 34.8 s); without god mode both clear with no death. **Review (round 1, 1 major, fixed):** the prism gallery's `ground` entrance opened with a gallery turret still standing — a `ground` window weighs the ground kills made while armed against the ground enemies that appeared while armed, and the facet turret at event x 2,000 (world x 2,400) was still on screen when the window armed at camera 2,300, so shooting it stood in for a gallery turret; it now spawns at x 1,830 and has despawned (camera > 2,268) before the window arms (1,830 kept both playtest runs as they were; 1,820 gave the no-god run two deaths). The engine rule stayed; new content check: every shipped stage with a `ground` entrance (`bonus-range`, `zone-g`), played from its start with nothing shot, has no ground enemy of an earlier event standing when the window arms; the authoring rule is in `content/stages/README.md`. Round 2 clean. **Golden replays re-blessed** in `31f35db` (only hashes changed in all 38 older files — inputs, ticks, headers, outcomes identical); new `zone-f-god` (13,190 ticks), `zone-g-god` (14,043) and `glimmer-cache-god` (1,815); the review fix re-blessed `zone-g-god` again (the bot's inputs and hashes changed; its 14,043 ticks, 54,060 points and `stageClear` did not; every other file byte-identical); the test round added `zone-f-arcade`, `zone-f-deaths`, `zone-f-boss`, `zone-g-boss`, `zone-g-bot` (46 in all; no existing golden changed). The Tizen `app.js` is **320.3 KB gzip of its 350 KB budget** (313.5 after M2-12). **Bugs found by the TEST agent:** (1) core/behaviors `boss.squid` / `boss.facet`: a phase limited the arms' curl to its own sweep, so a phase with a smaller sweep than the curl the previous one left (or a squid phase after a facet wave had curled the arms away from the core's row) snapped the arms at its first turn point — jumps of 50, 42 and 67 units in the tests — breaking the documented rule that a phase change never makes them jump; the arms now turn from wherever they are, however far (the shipped phases never exceed their sweeps: no golden changed). (2) Flaky allocation guards (present before this round): `behaviors-facet-alloc` / `behaviors-squid-alloc` sometimes measured 66 KB against the 64 KB budget under the full suite's load, also with the fix reverted — they now take the best of five windows instead of three — still not enough: during the docs step `behaviors-facet-alloc` failed once more at 66,736 bytes in the full suite and once run beside the squid guard (also on the code without the docs commit's comment-only edits); reruns passed, the budget needs another look. Docs: [`docs/dev/zones-f-and-g.md`](docs/dev/zones-f-and-g.md), the tester guide's zone F / G sections with GLIMMER CACHE (TV checks 65–69), API reference and the touched guides. |
| M2-14 | Final zones H & I, endings & credits | done | 2 | 102 | dc6c908, 42a1d7f, 9061af0, dacbab7 (+ this row fix) | **Zone H, IRON CITADEL** (`zone-h.stage.json`, 9,600 px, `terrain-citadel`, camera keys `0.8 → 0.7 → 0.6 → 1.3 → 0.75`, four checkpoints) and **zone I, ABYSSAL THRONE** (`zone-i.stage.json`, 9,600 px, `terrain-abyss`, `0.8 → 0.7 → 0.75 → 1.3 → 0.75`) replace the last stubs, each with a roster (`zone-h` / `zone-i.enemies.json`, five new types each against A–G), a 26-entry `directItems` plan, a stage song and a final-boss song on the **`FinalBoss`** cue (`zone-h` / `boss-h` SOVEREIGN OF STEEL, `zone-i` / `boss-i` THE HOLLOW KING — not `Boss`), procedural art (`citadel.mjs` 14 sprites, `abyss.mjs` 19; `TERRAIN_PALETTES` `tiles/terrain-citadel` / `-abyss`) and the DSL pattern `abyss.gulp`. H: hatches on floor and ceiling (`hatch.spawner` → `rammer.aimed` mites), the **piston hall** (eleven `block` events of the `solid` tile swinging ±22 px out of a flat floor and ceiling — M2-07's moving blocks), laser emitters (new **`emitter.laser`**, attached lanes), the **parade** hangar and **IRON SOVEREIGN** (IS-08, new **`boss.sovereign`**: four phases — plates and lanes, a turning shield wheel with rings, reversing with drones as its `minion`, the overdrive spiral). I: depth mines (new **`mine.burst`**), `worm.burst` trench eels, `pattern.loop` gulpers, the **ABYSS ARK** raid (AA-09, new **`boss.ark`**: four hull sections, two turret rows of three with heading frames, the heart, `rocket.homing` hooks, a 5,400-tick time limit whose escape sets `bossEscaped`) and **THE HOLLOW KING** (HK-10, its `inner` boss, new **`boss.angler`**: a `whenOpen` mouth with jaws and a lure swaying on M2-13's curling-arm rule). **Endings and credits:** `endings[].scene` (`ENDING_SCENES` none / citadel / abyss), `endings[].text` (≤ 8 lines of ≤ 40), campaign `credits` (≤ 24 sections of ≤ 16 lines of ≤ 60; `creditsLineCount`); stage `music.ending` / `music.credits` (optional; `stageMusicCues` prepares them with the final zone's set); `EndingScene` plays the sprite scene (new `ui/ending-*` UI sprites from `ending.mjs`; a dawn for `noDeath`, the ARK sailing off for `bossEscaped`) and the epilogue (`ENDING_LINE_TICKS` 90), then the card, then the new **`CreditsScene`** (id `credits`, 1 px every 2 ticks through 24 string slots, OK / Back after 60 ticks); songs `ending` (AFTER THE LAST WAVE) and `credits` (THANK YOU, PILOT); five shipped endings, the credits (12 sections) listing zones, bosses, engine packages and every placeholder-art generator (checked against `PROCEDURAL_GENERATORS`). `core/behaviors` → `implemented`. **As-built deviations:** the parade is four M2-09 **captains** (BULWARK / MAW / BASTION / REGENT ECHO, 960-tick time limits, earlier bosses' sprites and behaviours with fewer parts) — a `bossRush` stage cannot hold a normal zone's timeline; new **`ScriptApi.sleepUntilNear`** (`Enemy.nearRange`, tested in the enemy system's movement phase, hashed) — the first `mine.burst` polled every 6 ticks and four waiting mines allocated 294 KB per 10,000 ticks; new **`BossScriptApi.spiral`** (`Boss.spiral*`, fired by the boss system in `runScript`, hashed) — a spiral volley every 10 ticks woke the boss script that often at ≈ 80 bytes a wake; IRON SOVEREIGN's shield-wheel hub starts turned 45° (`angle` 128) — at rest a pod sat in the core's line of fire and the bot needed 252 s to break the plates; the final zones' boss songs use `FinalBoss` (stage-scoped) and the ending / credits themes are the final stage's `music.ending` / `music.credits` cues so they are resident when the zone clears (one music set at a time); the ARK's camera path keeps its turrets off the ship's column (point-blank turrets killed the bot twice) and zone I's undertow has no eels (at 1.3 px/tick the bot met a rising eel); the content check's zone I fight flies fully powered (a bare ship cannot beat the ARK's time limit); UI string slots 224 → 256; the heavy boss allocation guards (M2-13's facet and squid too) allow a fixed budget per script wake (`WakeCount`, `SCRIPT_WAKE_BYTES` 96) on top of 64 KB — the facet guard failed the full suite once more at 66,648 bytes; `stageSkip: 'boss'` lands before zone H's parade (it stops at the first `warning` **or** `boss` event, as before zone B's mid-boss — and the debug key 8 pressed after the parade jumps back to it). Measured with the 4-way bot and god mode: H 252.9 s (IRON SOVEREIGN 42.8 s), I 270.9 s; without god mode both clear with no death; the 16 routes end in the real finales (H 239 s, I 248 s with the carried loadout), each ending with its scene and epilogue. **Review (round 1, 1 major, fixed in `42a1d7f`):** a no-death run whose ABYSS ARK escaped matched *THE DEEP IS STILL* (`throne-flawless`, first and without an exclusion) — its epilogue sinks a HOLLOW KING who never appeared; it now lists `none: [bossEscaped]`, so an escape always gets *THE FLAGSHIP SLIPS AWAY* (the escape scene still draws the dawn); selection expectations fixed in the content and campaign-edge tests (no golden changed — the selection is outside the sim). Round 2 clean. **Golden replays re-blessed** in `dc6c908` (new sprites, UI sprites and scripts shift the sorted ids; the hash gained `nearRange` and the spiral fields — all 49 older files kept inputs, ticks, headers and outcomes); new `zone-h-god` (15,174 ticks) and `zone-i-god`; the test round added `zone-h-boss`, `zone-h-arcade`, `zone-h-deaths`, `zone-i-boss`, `zone-i-escape` (53 in all) and re-blessed `zone-i-god` for its `boss.ark` fix (16,146 ticks instead of 16,256, 145,550 points, still no death). The Tizen `app.js` is **331.5 KB gzip of its 350 KB budget** (320.3 after M2-13); the atlas has 427 sprites on a 1024×1024 page. **Bugs found by the TEST agent:** (1) core/behaviors `boss.ark`: a hook launch moved its starting gun while still searching, so with `count` > 1 it skipped guns and could cast two hooks from the same gun at the same spot — now each on-screen gun casts at most once per launch, taken in turn (`zone-i-god` re-blessed). (2) core/bosses: a phase change stopped the spiral stream but kept its heading, so a later phase's spiral carried on from the old heading instead of starting at 0 as `boss.sovereign`'s docs say — the phase change now resets `spiralAngle` (no golden changed). Docs: [`docs/dev/zones-h-and-i.md`](docs/dev/zones-h-and-i.md), the tester guide's zone H / I sections and [The endings and the credits](docs/client/preview-build.md#the-endings-and-the-credits) (TV checks 70–75), controls, debug tools, API reference and the touched guides. |
| M2-15 | Front-end screens & attract mode | done | 3 | 95 | 1691596, 2fe3508, ac750bf, 8c57fa6, 83759e7 (+ this row fix) | **Mode select** = the title's menu (no separate scene): 1 PLAYER / 2 PLAYERS / **PRACTICE** / OPTIONS / **SOUND TEST** / EXIT (EXIT only with `platform.exit`; PRACTICE disabled without a campaign) — `TitleItem` Start 0, TwoPlayers 1, Practice 2, **Options 3**, SoundTest 4, **Exit 5**; every test and e2e spec that walked to OPTIONS presses Down once more; the skeleton's placeholder scene ids `attract` / `select` became `demo`, `story`, `practice`, `soundTest` (plus `nameEntry`, `hiScore`); `core/scenes` → `implemented`. **Attract loop:** title idle `TITLE_ATTRACT_TICKS` 720 on `PRESS OK` (a press or a held key resets it; the open menu never idles out) → `DemoScene` → `HiScoreScene` (the chosen ship / difficulty's 1P table first, ≤ 4 pages × `HI_SCORE_PAGE_TICKS` 300) → `StoryScene` → title; any input on those three → title (Back too, never the exit dialog); no demos → the tables first, no story → the tables return to the title. **Demos are bundled content:** new `core/data` kind **`replay`** (`content/demos/<id>.replay.json` + README + example, `ContentDb.demos` / `demoIndex`, `DemoSpec`, `MAX_DEMO_TICKS` 18,000; the loader checks the structure and the stage), nine demos (one per zone, the 4-way bot with god mode, 2,400 ticks, zones B / E / H in the MANTA, `DEMO_BUILD_ID` `demo`, ≈ 1.7 KB each) recorded by `test/golden/demos.ts`, locked by `test/golden/demos.test.ts` (every hash, attract and golden paths) and **re-recorded by `pnpm golden:update`**; Prettier skips them. **`core/replay` split** into `replay/format.ts` (header, recorder, playback, file format — no `core/game` import) and `replay/demo.ts` (`DemoPlayback`, `createDemoPlayback(replay, content, { events })` — a bare World with its own debug flags; a desync or the recording's end stops it) to avoid a scenes → replay → game → scenes import cycle; `index.ts` re-exports both (public API unchanged). The demo World pushes into a private queue and forwards only the visual events (`DEMO_SILENT_KINDS` drops Sfx, Music, ducking, rumble, host requests): **the demo is silent**. **Story crawl** is campaign content: `campaign.story` (≤ 8 pages, `STORY_SCENES` none / dawn / invasion / launch, ≤ 6 lines of ≤ 40 characters; three shipped pages of original text), 1 px every `STORY_SCROLL_TICKS` 4 through 10 string slots taken by row number; its scenes reuse the ending sprites, the ships and the logo — no new art. **Hi-score tables per difficulty × ship × mode:** `hiScoreModeKey(config, mode)` — 1P keys unchanged (no save migration), co-op adds `-2p`, practice `-practice` (`HI_SCORE_MODES`, `HiScoreMode`, `parseHiScoreModeKey`); the ship dimension is the power-up model (one ship per model); old co-op rows move into their `-2p` table in `sanitizeSave` (no save version bump — M2-16 owns save v2); co-op and practice Worlds play against their own table's best and never raise the 1P session hi-score. **Name entry:** `core/ui` `NameEntry` / `createNameEntry` / `nameEntryTick` / `drawNameEntry` (`NAME_ENTRY_GLYPHS` A–Z 0–9 . - ! space, 3 letters; four directions and OK only); rows are still recorded as `---` at the game's end (the save written then) and renamed afterwards, found by object identity (`SaveStore.renameScore`); **`finishGame()` replaced `toTitle()` at the end of every game** (game over, `TO BE CONTINUED`, a practice clear, an ending without credits, the credits) → `NameEntryScene` (each pending row, `NAME_ENTRY_TIMEOUT_TICKS` 1,800) → the result table (`HI_SCORE_RESULT_TICKS` 900, lock 30) → title. **Practice select** (`PracticeScene`: ZONE, CHECKPOINT, LOADOUT — `PRACTICE_LOADOUTS` default / full —, START) sits before the difficulty / ship / weapon select; its START goes through that chain and **`FlowControl.launchGame()`** (replacing three `stack.reset(game)` calls) starts the run with `RunState.loadout` (`runWorldConfig` applies it; `startPractice(zone, checkpoint, loadout)`). **Sound test** (`SoundTestScene`): new `SimEventKind.SoundTest` (16), `SceneFlowHost` / `GameOptions.soundTest` (`SoundTestSetup { music }`), `AudioEngine.playTrack(index, fade)` (keeps one extra track outside the prepared set) and the shell's `connectSoundTest` (the boot hands the flow the library's titles); OK is masked from `menuTick` on MUSIC / SFX. **Continue countdown polish:** a draining bar (red for the last three seconds, the digit flashing), the score, a blinking `PRESS OK` / `BACK: GIVE UP`. UI string slots 256 → **384**. The Tizen `app.js` is **343.8 KB gzip of its 350 KB budget** (331.5 after M2-14 — ~9 KB of scene code, ~3 KB of demos). No simulation change: **no golden re-bless**. **Gotcha (allocation guards):** a long replay-*recording* session in the same worker makes any later World allocate ≈ 12 bytes a tick, so the demo guard plays a synthetic recording (`packReplayInput`) instead of recording one. **Review (round 1, 2 major, fixed in `2fe3508`):** (1) practice select: Left on CHECKPOINT did not wrap within a smaller zone's checkpoints (the choice wrapped to its own last label, past the zone's count, and `clampCheckpoint` sent it back to START) — a backward wrap from START now goes to the zone's last (new test with zones of 2, 3 and 0 checkpoints); (2) the sound test played every positional SFX panned 60 % left (`Sfx` at x 0 against the title's camera at 0) — it now pushes them at `PLAYFIELD_W / 2`. **Round 2 (1 major, fixed in `ac750bf`):** a practice score leaked into the next 1P game's session hi-score (`beginRun` cleared `run.practice` before the old World was recorded) — `GameScene.worldPractice` now decides from the World itself, as co-op does from `world.config.coop`. Round 3 clean. **Tests** (95, `8c57fa6`): front-end edge cases, the sound test end to end (shell runtime, audio engine edges), the mode-select allocation guard, the `front-end.spec.ts` browser spec; the TEST agent found no new bug. Docs: [`docs/dev/front-end-and-attract.md`](docs/dev/front-end-and-attract.md), the tester guide's [The front end](docs/client/preview-build.md#the-front-end-attract-mode-high-scores-practice-and-the-sound-test) (TV checks 76–82), controls, API reference and the touched guides; the docs pass also tidied the step's TSDoc (the audio engine's module comment, stale `SceneFlowView` / title-menu / continue docblocks, the save's §15 / §16 spec refs, a `content:check` claim that belonged to `demos.test.ts`). |
| M2-16 | Options, rebinding & accessibility | done | 3 | 149 | 5d0ff2e, 4c6e358, 499f033, 6ba7575, 4c0f92a (+ this row fix) | **Options regrouped:** MASTER / MUSIC / SFX, then the pages **CONTROLS / DISPLAY / GAME**, BACK (`OptionsItem` Controls 3, Display 4, Game 5, Back 6 — was a flat list to `Back 10`); the M1-17 profile row moved to CONTROLS (`ControlsItem.Profile`), the M2-02 / M2-08 / M2-09 display rows unchanged to DISPLAY (`DisplayItem`); each page is an overlay scene (`controls`, `display`, `gameOptions`, plus `rebind` and `inputTest`) storing its own group on close; every test / e2e spec that walked the old list was updated. **CONTROLS:** PROFILE, AUTOFIRE `ALWAYS / TOGGLE / HOLD` (disabled on a remote-mode host — the TV), RATE (`AUTOFIRE_INTERVALS` 8 … 2 ticks shown as shots a second), SOCD `PROFILE / NEUTRAL / LAST WINS`, DEBOUNCE `AUTO / 0 … 10 TICKS` (the "advanced debounce slider" as a choice), REBIND KEYS / PAD, INPUT TEST; SOCD / DEBOUNCE stored at once and pushed as the new `UserOptionKind.InputSettings` (10). **Autofire modes are sim config:** `GameConfig.autofireMode` (default `always`) joins the kept `autofire` boolean (`false` = hold) and `autofireInterval`; `toggle` flips a per-player `WeaponSystem.firing` on each Shot press, hashed **only in the toggle mode**, so no golden hash changed. **Game options** (`UserGameOptions` — named so to avoid `core/game`'s `GameOptions`; difficulty, lives 1–5, penalty, Auto Power-Up, magnet, one-button) live in the save's `options.game` and are folded into every difficulty's config by `withUserGameOptions` in the flow's `rearm` — the next game or a RETRY STAGE only; the difficulty menu's choice is remembered (`options.game.difficulty`); ship and loadout stay session-only. **Rebinding:** overrides per profile and context (`BindingOverrides`: each override replaces the action's whole key set as `code:` / `key:` / `button:` tokens, ≤ 16 profiles, ≤ 4 tokens); `@shmup/input-web` `rebind` → implemented (`rebindAction` moved / swapped / refused / rejected, `resetBindings`, `customizeInputProfile` — a lock-out keeps the content's table —, `captureToken`, key labels); Esc and the remote's Back cancel a capture and never move; capture through `WebInput.beginCapture` (`KeyCapture` — only new keys, a remote's fake pair ignored — and the lowest new pad button); core rebind widget `RebindPanel` (its draw uses index loops and precomputed slots — `for … of` allocated an iterator per redraw); host side `@shmup/shell` `controls` (`createShellControls`), the apps' `customize` / `rebindable`; the input test runs the `'game'` context, hold Pause 1 s to leave. **Save v2** under the same key `save.v1`; the 1 → 2 migration adds the new fields unset and **moves the 2P / practice rows** out of the 1P tables (moved there from `sanitizeSave`); fixture `save-v1.json`. **String table:** `core/ui/strings.ts` (`DEFAULT_UI_TEXT`, 290 ids incl. `sfx.<Cue>`), content kind `strings` (`content/strings/en.strings.json` — the plan's `en.json` renamed by the `<name>.<kind>.json` rule; `ContentDb.uiStrings`), the flow draws from the content's `en` table (`SceneFlow.text`, `buildSceneLabels`); `pnpm content:check` keeps the file equal to the built-in table and a source scan fails on any upper-case literal in the scenes / UI kit. **Budgets:** UI string slots 384 → 512; Tizen `app.js` ≈ 359 KB gzip — `APP_JS_GZIP_BUDGET` raised 350 → 384 KB. **Goldens and demos re-blessed for the header only** (`autofireMode` added; no hash, input or frame changed); each new allocation guard in its own file. **Review round 1 (2 major, fixed in `4c6e358`):** DIFFICULTY / options changed from the pause menu switched the run in progress (next zone, bonus stage, RETRY, its hi-score table) — new `FlowControl.runConfig` frozen at a run / practice start, `rearmRun()` on RETRY at the run's own difficulty, DIFFICULTY disabled over a game; the difficulty menu showed the preset's lives, not the saved LIVES — new `FlowControl.armedConfigs`. **Round 2 (2 major, fixed in `499f033`):** rebinding the `KEYBOARD AS REMOTE` profile skipped conflict detection and the new `key:` token was hidden by the old `code:` entry — `captureToken` names a key the way the profile binds it, `code:ArrowUp` ≡ `key:38` for the holder and reserved checks, dead `key:` entries ignored; rebinding on the split keyboard could give player 1 a player-2 key — now `Rejected` and skipped in hand-edited overrides. Round 3 clean. **Tests** (149, `6ba7575`): the rebind widget, key capture, rebinding edges, save v2, config game options, the autofire goldens `zone-a-manta-toggle` / `zone-a-hold` (`golden-autofire.test.ts`, 55 goldens), the `rebind` / `game-options` browser specs; the TEST agent found no new bug. Docs: [`docs/dev/options-rebinding-and-accessibility.md`](docs/dev/options-rebinding-and-accessibility.md), the tester guide's [Options screen](docs/client/preview-build.md#the-options-screen) (TV checks 83–87), controls ([CONTROLS page, rebinding, input test, one-button play](docs/client/controls.md#rebinding-keys-and-buttons)), API reference and the touched guides; the docs pass also tidied the step's TSDoc (the scenes' stale `OptionsItem` Public API note, `config` / `rebind` / `data` Implements and specRefs for §4 / §21, the string table's Public API and a `ContentDb.uiStrings` param, the shell package doc). |
| M2-17 | Platform polish: Electron, Tizen extras, storage | done | 2 | 228 new (7,296 in the suite) | afc9aa6, 831cc07, c682b68, 59e9d0d (+ this row fix) | **Electron file saves:** `main/saves.ts` `createFileStore` — `<userData>/saves/<key>.json` (`save.v1`, `save.corrupt`, `window`), temp file + `fsync` + copy to `.bak` + rename, per-key queue, a missing / non-JSON file falls back to its backup, 1 MiB a value / 8 MiB the folder (`StorageQuotaError`); IPC `shmup:storage-get` / `-set` in the new `main/ipc-handlers.ts`, which refuses any sender but `app://game/…` (or `SHMUP_DEV_URL`'s origin) — quit included — and bad keys / values; the window blocks navigation away and pop-ups; `FileStore` gained `directory` and `usage()`. **Web build in Electron:** `getElectronBridge` → platform id `'electron'`, `createBridgeStorage` (failed read → memory, failed write rejects), EXIT quits, `audioUnlock: 'immediate'` (`autoplayPolicy`); desktop saves of older builds (web `localStorage`) are not migrated. **Window:** `main/window-state.ts` — fullscreen, scale ×1–×10 fitted to the work area, position (kept only on a screen) in `window.json`; F11 / Alt+Enter, Ctrl+= / Ctrl+- / Ctrl+0 (Cmd on macOS) in `before-input-event`; no in-game Options entry (core UI work outside the step). **120/144 Hz + gamepad** needed no Electron code (M2-08's accumulator and interpolation, the web Gamepad adapter). **Packaging:** `electron-builder.json` + `package` = `pnpm dlx electron-builder@26.15.3 … --publish never` (not a dependency); `electronVersion` pinned in the config (electron-builder cannot read `catalog:`; a test compares the majors); output `release/` ignored; no icons until M2-18. **Tizen:** `public/config.xml` stays the default (no metadata) + the `productinfo` privilege; `scripts/config-xml.mjs` variants via the `configXmlVariant()` Vite plugin — `build:game-mode` / `TIZEN_GAME_MODE=1` add `use.game.mode`; the **gamepad metadata is opt-in only** (`TIZEN_GAMEPADS`) because Samsung's check shows a popup without a listed pad; `check-bundle` validates any variant (`validateConfigXml`), its fixtures now the real `config.xml`. `device-info` implemented (`collectDeviceInfo`, `formatDeviceLine`, `loadWebapis` — only on a TV, only when the debug tools unlock; `DeviceInfo.modelCode` new) → a sixth overlay line (render-pixi `setDebugPanelDevice` / `DebugOverlay.setDevice`, `DEBUG_DEVICE_MAX` 56; shell `DebugToolsOptions.device`; `tizenDebugTools(win, buildId, canvas?)`). `live-reload` implemented (`connectLiveReload`, new `__SHMUP_LIVE_RELOAD__` define, dev builds only) + `scripts/tizen-watch.mjs` (`tizen:watch`: Vite watch + Node HTTP + hand-written RFC 6455 WebSocket); the installed widget navigates to the served page — whether Tizen keeps the widget APIs there is a manual §8.5 check; the placeholder's `'hot-data'` mode dropped. **Storage:** new shell module `storage` — `createWebStorage` replaces the two duplicated `localStorage` adapters (1 MiB budget, 256 KiB a value, UTF-16 counted; a `QuotaExceededError` drops `save.corrupt` and retries once, then keeps **only that value** in memory — other errors still switch to memory for the session). **Debug save export / import** as `window.__shmupDebug.save` (API only, no keys) + core `SaveStore.replace(data)`. **Memory:** new shell module `memory` — `estimateStageMemory` (every campaign zone < 100 MB: A–G ≈ 66.7 MiB, H ≈ 73.6, I ≈ 74.3; the 24 MiB heap baseline is an assumption for the §8.5 device check) and atlas residency on `PrepareStage` (`Shell.atlasResidency`) — nothing unloaded with today's single 1024² page. §8.5 checklist extended (game-mode builds, heap baseline, device line / save export, live reload). **Review round 1 (2 major, fixed):** the device line allocated a string per frame (the ~66-char TV line `slice()`d every frame, ≈ 1.7 MB / 20,000 calls) — `setDevice` now compares its input; the window position was never saved on Linux / Steam Deck (`moved` is macOS / Windows only) — saved 400 ms after the last `move` (`WINDOW_MOVE_SAVE_MS`) and on `close`, `will-quit` waiting for pending writes. **Test agent bug:** `fitWindowScale(NaN)` returned `NaN` despite its ≥ ×1 contract (`NaN` slips through `Math.max` / `Math.min`) — now `Math.floor(scale) \|\| 1`. No simulation change (no golden re-bless); Tizen bundle ≈ 360.9 of 384 KB gzip. Docs: `docs/dev/platform-polish.md`, `docs/client/desktop-app.md`, the remote Web Inspector in `build-test-deploy.md`. |
| M2-18 | v1.0 hardening & release candidate | done | 2 | 91 new (7,456 in the suite) | f4b5cae, cd08547, 18a35b2, 1252622 (+ this row fix) | **Routes × ships:** `test/playtest/campaign-routes.ts` `flyRoutesThrough(exit, ship)` flies the KESTREL or the MANTA (`ROUTE_SHIPS`) over all 16 routes in four parallel files (`campaign-routes-b` / `-c` / `-manta-b` / `-manta-c`); every zone 3–6 min (`ZONE_MIN_SECONDS` / `ZONE_MAX_SECONDS`); the 4-way rules checked on every tick of every route through the new `CampaignFlags.observe`. New rule `rules.ts` `columnGap` (bullets within ±8 px of the ship's column plus every live laser lane must leave a ≥ 16-px open band — the 4-way gap rule generalised from lasers to bullet walls; narrowest measured 38 px), applied by every rule check that already ran, including all route runs. **Findings fixed (sim changes):** the route bot first failed with the MANTA — (1) the LASER → WAVE family's tall piercing waves died on armour in front of MANTLE REGENT's, IRON SOVEREIGN's and THE HOLLOW KING's cores → new `direct.bolt` tunable `passArmour` / `ShotFlag.PassArmour` (32: clink at most once per hit cooldown and fly on, like a blast), set on the four waves; (2) the fifth disc level was a ±16-unit V with a blind spot ahead (CINDER BASTION) → two parallel discs (`oy` ±4); (3) GALVANIC MAW's jaws (gape 4 / 4 / 5 → **8 / 8 / 9**) always caught the HUGE DISC. The boss sweep found (4) SANDGRAVE WIDOW's rank-scaled silk lines overlapping → `boss.widow` waits a whole lane before the next line. `zone-b-god` re-blessed (the jaws; nine ticks longer, same outcome); no other golden or demo changed. **Static audits** (`test/integration/release-audit.test.ts`) are partly headless runtime checks: the budgets and the recovery rule from the data, the patterns measured on probe enemies at rank 2 and 16, the laser lanes in real boss fights with both ships. **Cross-engine determinism:** headless Firefox has no WebGL without a GPU, so the replays run on the web **test** build's game-free `?determinism` page (new `@shmup/shell` module `determinism` — `createDeterminismCheck` / `installDeterminismCheck`, `window.__shmupDeterminism`; web `determinismFromSearch`; folded out of release builds) instead of the game page; `test/e2e/determinism.spec.ts` plays every golden and demo in the Playwright projects `chromium` and `firefox`. No separate `e2e` workflow: `ci.yml` runs the e2e shards with `--project=chromium` plus a new `e2e-firefox` job. **Benches:** `test/bench/zones.perf.ts` (every zone under stress, median < 1 ms/tick, < 1 MB retained) and `soak.perf.ts` (30 min through the scene flow) — the soak measures the heap without V8's code and trusted spaces (`dataHeapBytes`), which grow ~1 MB with JIT work over 30 minutes. **Release checks:** `test/e2e/release-check.spec.ts` — boot to title < 3 s (≈ 1.1 s) on both builds, the Tizen self-checks with a fake `window.tizen` (Back / exit, pause, visibility suspend / resume without a catch-up burst, five resume cycles, user data only under `shmup-cup:`). **Version:** manifests `1.0.0-rc.1`, `config.xml` `1.0.0` (Tizen accepts numeric `major.minor.patch` only); CHANGELOG `[1.0.0-rc.1]` + an empty `[Unreleased]`. **Icons / store:** `scripts/store-assets.mjs` (`pnpm store:assets`, `--check`, `--out`); the skeleton's binary `apps/tizen/public/icon.png` is replaced by the generated icon (512 × 423); the new `apps/electron/build/icon.png` (512 × 512) is named in `electron-builder.json`; placeholders in the ignored `assets/generated/store/`. The store-assets test compares decoded pixels, not PNG bytes (zlib versions differ between machines); the Seller Office image sizes could not be verified online — the listing says to check them before submission. **Review round 1 (1 major, fixed):** the e2e docs were wrong once `pnpm test:e2e` also needed Firefox and CI's commands changed — `build-test-deploy.md` (command, concurrency, CI table, troubleshooting), `rendering-and-shell.md`, README and `repo-layout.md` fixed, with `test/integration/e2e-docs.test.ts` as the regression. **Test agent:** 91 tests (determinism edges, `passArmour` on boss parts, the widow's lane wait, the column rule, store assets, release content relations, the e2e docs), no bug found. Docs: `docs/dev/release-hardening.md`, `docs/client/release-candidate.md` (the v1.0 checklist for the monitors), API reference, TSDoc of the `determinism` module and `store-assets.mjs`. |
| M3-01 | Extra modes & replay features | done | 2 | 51 new (7,604 in the suite) | 51ef09f, d4ed45e, 8959e3b, aab4607 (+ this row fix) | **EXTRA menu:** new title item `TitleItem.Extra` 5 (EXIT moves to **6**) opening the `ExtraScene` — BOSS RUSH, CARAVAN (choice: the zone), ARCADE (choice: LOOP 1 / LOOP 2), REPLAYS, BACK; rows without their content are disabled; OK on a choice row starts the mode (Left / Right change the choice); the modes go through the usual difficulty / ship / weapon select, one player. New hi-score modes `bossrush` / `caravan` / `arcade` (`MAX_HI_SCORE_TABLES` 32 → 64). **Boss rush** is shipped content: `content/stages/boss-rush.stage.json` (`type: "bossRush"`, the nine zone bosses A–I, each with its WARNING — the M2-09 rush machinery); the 4-way bot clears it with god mode and the full loadout (≈ 5.8 min, `test/playtest/boss-rush.test.ts`). **Caravan:** one campaign zone against a 3-minute clock (`CARAVAN_TICKS`), driven by `GameConfig.timeLimit` → `World.timeLeft` / `timeUp` / `clockPaid` (phase 9 `updateClock`); time up ends the World as `stageClear` with a TIME UP card; a clear in time pays 1,000 points per whole second left (`CARAVAN_TIME_BONUS`); the HUD shows `TIME` and the seconds in player 2's place (`hudClockSeconds`, `HUD_STRING_COUNT` 23 → 24); the clock is hashed only when a limit is set. **Loops / ARCADE:** `GameConfig.loop` 1–8; remixed layouts are a stage's optional `remix` list merged per World by `stageForLoop` (loop-1 event indices and hashes unchanged), `minLoop` / `maxLoop` work on any event (`StageRunner.loop`, `stageEventInLoop`); all nine zones ship a remix; bullet speed × `1 + 0.15·(loop − 1)` capped at 1.6 (`loopBulletSpeedScale`); from loop 2 every player kill fires a revenge bullet (an aimed one for specs without `revenge`); the ARCADE run goes from the ending straight into the next loop (no credits, `LOOP n  ZONE A` cards); starting on LOOP 2 needs an ending first (`SaveData.unlocks.loop2`). **Extra Edit:** seven weapons in `content/weapons/types-extra.weapons.json` marked `extra: true`; new `ShotKind`s Control 10 / Upper 11 / HawkWind 12 / SpreadGun 13, the other three reuse existing kinds with a new `flip` art tunable; the Spread Gun is equipped twice (`MeterChoices.doubleLevels`, `Loadout.spread` — carried between zones, hashed only when non-zero); the weapon select's TYPE gains EXTRA after EDIT, locked until `unlocks.extraEdit` (any ending, or a secret code). **Secret codes:** four original 8-direction sequences (`SECRET_CODES`, `SecretCodeTracker`): EXTRA SHIPS and EXTRA EDIT on the title, FULL POWER (`grantFullPower`, once per World) and SELF DESTRUCT (`selfDestruct`) in the pause menu; `startingLives` now 1–9 (`MAX_STARTING_LIVES`) so EXTRA SHIPS can give 7 — the menus still offer 1–5; FULL POWER and SELF DESTRUCT are recorded as run-replay actions; EXTRA SHIPS and FULL POWER mark the run assisted. **Score-milking cap** (`repeatKills` 40 / `repeatPercent` 10 in `content/rules/scoring.rules.json`) applies only to script- or boss-spawned enemies (`Enemy.child`), per kind per World — this changed `captain-range-god` (score 25,120 → 20,980), re-blessed; every other golden and demo changed only in its header (the new config fields and `assists`). **Replays:** the M1-19 header gained `assists` (1 god mode, 2 invincible, 4 speed, 8 secret — `AssistFlag`); new whole-run `run-replay` format (`core/replay` `run.ts`: one segment per World with its start state — `worldStartJson` / `readWorldStart`, `WorldStart` / `prepareWorldStart` — RLE input, hashes and between-tick actions `RunAction` Continue / FullPower / SelfDestruct); the flow records every run (`SceneFlow.recorder`, `RunRecorder`; a debug stage jump makes the run unsaveable — `noteWorldEdited`); `ReplayLibrary`: last game + 3 kept, keys `replay.last` / `replay.1`–`3`; browser PLAY (×1 / ×2 / ×4, pause), KEEP, SHARE, DELETE (`ReplaysScene`, `ReplayScene`, `RunReplayPlayback`); SHARE is a host hook — the web app copies to the clipboard and imports a pasted replay (`copyReplayText`, `listenForPastedReplays`), the TV has none. **Assists and feel** live in the save's `options.play` (`PlayOptions`; save version stays 2 with every new field optional): GAME page OPT RECOVERY, SPEED (100 / 75 / 50 %), INVINCIBLE; CONTROLS page RUMBLE (`rumblePad` / `WebInput.rumble`, `connectRumbleEvents`); game speed slows the frame clock like slow motion, so the simulation, replays and hashes do not change; invincibility is a sim config field (`GameConfig.invincible`, `PlayerShip.invincible`); option recovery drops the lost Options as Free Option items (`GameConfig.optionRecovery`, `EnemySystem.dropAt`); assisted hi-score rows carry `assisted` (drawn `*`). Six new goldens (`zone-a-loop2-god`, `-loop2-boss`, `-caravan`, `-extra`, `-recovery`, `-invincible`). `Game.frame` is split into `bareFrame` and `flowFrame` to keep the allocation guard green; the Tizen bundle is 374.7 KB gzip of its 384 KB budget (not raised). **Review round 1 (fixed):** the replay size cap exceeded the storage adapter's per-value limit and four replays could fill the save's 1 MiB budget — `MAX_REPLAY_TEXT` 200,000 → 120,000 characters (240,042 bytes with its key), new `MAX_KEPT_REPLAY_TEXT` 130,000 shared by the kept slots (KEEP → -1, import → `ReplayStoreResult.Full`), so the library takes ≤ 500,150 bytes; `load()` clears a slot over the caps; tested over the real `createWebStorage` (`storage-replays.test.ts`). **Bugs found by the TEST agent:** (1) `RunReplayPlayback` never applied a flow action recorded before a World's first tick (tick 0), and every later action of that segment was then blocked, so the replay went out of sync — starting a segment now applies its tick-0 actions; (2) with a non-whole slot number the replay library's `keep()` threw a `TypeError`, `exportText()` returned `undefined` instead of `null` and `remove()` wrote a stray `replay.1.5` key — such a slot is now treated as empty. Docs: [`docs/dev/extra-modes-and-replays.md`](docs/dev/extra-modes-and-replays.md), [`docs/client/extra-modes-and-replays.md`](docs/client/extra-modes-and-replays.md), API reference, module docblocks of every touched module, CHANGELOG `[Unreleased]`. |
| M3-02 | Visual & mechanic extras | done | 2 | 7,793 in the suite (7,788 at the TEST agent's run) | 1533645, bc584d8, a5678b3, 48bacde (+ this row fix) | **Mode-7 floor:** one GLSL ES 1.0 Pixi filter (`render-pixi` `effects/shaders.ts` `MODE7_VERTEX` / `MODE7_FRAGMENT`, built by `effects/mode7.ts` `createMode7Filter`) evaluates mode 7's per-row affine matrix per pixel over a full-frame sprite at the bottom of `BG_MID`, sampling the tile straight out of the atlas with `fract` (no separate floor texture); the host passes the plane's **turned axes** from the core's angle tables, so the shader has no trigonometry, and the scale is clamped (`MODE7_MAX_SCALE`) so the horizon row cannot divide by zero. Stages describe it in a new optional `mode7` section (`StageMode7` → `Mode7View`, presentation only — the simulation never reads it and a stage's hash is unchanged), and `createMode7Floor` attaches the filter only while the camera is inside `[from, to)`. **Pseudo-3D stage:** `content/stages/dimension.stage.json` (**HIGH-SPEED DIMENSION**) is a **dev stage** (`?stage=dimension`, the `raster-range` precedent), not a tenth campaign zone — a zone would change the diamond map, every route and every golden for no gain; its art comes from the committed generator `scripts/assets/procedural/dimension.mjs` and `test/integration/dimension-runtime.test.ts` flies it. **Escape sequence:** a zone's optional `escape` stage id (`CampaignZoneSpec.escape` / `escapeId`; the loader rejects one on a zone that still has exits) — the final zone's stage clear goes `ClearNext.Escape` → `FlowControl.enterEscape()`, which swaps the World for one on that stage with the run's carry (`RunState.inEscape` / `escapeStage`, no caravan clock), and the next clear (`ESCAPE COMPLETE`) runs the ending; it is **not** a zone of its own, so the route, the zone count and the hi-score row's `reached` are unchanged (zones H and I ship `content/stages/escape.stage.json` with its own `content/audio/music/escape.music.json`). **CRT filter:** `effects/crt.ts`, one program for both strengths (`CRT_LOOKS` — scanlines at `light`; scanlines, an aperture-grille mask and a vignette at `full`), run over the **upscaled** second pass and capped at `CRT_MAX_HEIGHT` (1080) rows through the filter's resolution (`crtResolution`), the scanline pitch following the frame's scale; the shader only multiplies the colour down, so the flash limiter still holds. **Aspect modes** are a **window** on the display, never a crop and never a wider playfield (the internal 384×216 is fixed — D19): `computeAspectViewport` places the frame in the largest 64:27 (`wide`) or 4:3 (`classic`) rectangle that fits and reports the leftover width as side panels — a dimmed space-navy surround (`PANEL_ALPHA`), not painted side art, so no new art was needed. **Authentic slowdown** is sim-side and deterministic: phase 9 counts the tick's live objects into `World.slowLoad` (enemy bullets + lasers + live enemies + standing boss parts + player shots + items) and steps `World.slowRun`; `stepWorld` then skips the next tick exactly as hit-stop does once the load is over `SLOWDOWN_THRESHOLD` (96). **Graze** (`GameConfig.graze`): an enemy bullet that passes within `GRAZE_MARGIN` of a ship's hurtbox without hitting it is marked once (`BulletFlag.Grazed`) and pays `ContentDb.scoring.graze`. **Black-hole bomb** — the one signature mechanic (feat §7C) — in the new `core/blackhole` module: the Direct ship's Special throws a vortex that drifts with the camera, pulls enemy bullets in and swallows the ones that reach its core (points like a cancel), drags enemies towards its centre, then discharges bolts that destroy every non-immune enemy in reach and damage boss parts; `MAX_BLACK_HOLES` 2 (one per player), stock `MAX_BLACK_HOLE_STOCK` 3, and the yellow Direct item stocks a bomb instead of detonating a smart bomb. **Death-bomb window** (`GameConfig.deathBomb`, 0–`MAX_DEATH_BOMB_TICKS`): a fatal hit on a ship with a bomb opens the window instead of killing it; a Special (Direct) or PowerUp (the meter's armed `!`) press inside it wipes the hit and grants `DEATH_BOMB_INVULN_TICKS`, and a window that runs out kills on the tick it closes. **P2 bosses** GRASPING BLOOM (suction), IRON TALON (grabber) and SHADOW STRIDER (the invincible walker) live in `content/enemies/extras.enemies.json` on the new boss pull field (`BossScriptApi.pull` / `release`, `BossSystem.applyFields`) with `boss.suction` / `boss.grabber` / `boss.walker`. **Options:** the four sim-affecting extras are `GameConfig` fields (replay-recorded) offered on a new **EXTRAS** page (`APPLIES FROM THE NEXT GAME`); the CRT filter and the aspect mode are presentation-only `UserOptions.display` rows applied live through the shell's `DisplayTarget` (`setCrtFilter` / `setAspect`, both optional so a renderer without them is untouched). `hashWorld`'s `mixExtras` mixes each term only in a World that has the feature on (and a boss's pull field only while one is open), so older recordings keep their hashes; goldens and attract demos were re-blessed with `pnpm golden:update` **only** because every replay header gained the four new fields — no `expected` status, score or tick count moved, and the new `zone-a-extras` golden flies zone A with every extra on (`bomberBot`, a black hole every 90 ticks). `BlackHoleSystem.update()` passes **whole-pixel** centres to `bullets.vortex` / `enemies.pullTowards`: a fractional argument of the six-argument (not inlined) call boxed a heap number every tick a vortex was open — found by the new guard `blackhole-alloc.test.ts`. `eslint.config.js` `globalIgnores` gained the git-ignored agent / editor directories (`.claude/`, `.caliber/`, `.playwright-mcp/`). The Tizen bundle is 383.4 KB gzip of its 512 KB budget (374.6 KB at M3-01 — the budget was not touched). **Review round 1 (both fixed, with regression tests that fail without the fix):** (1) a checkpoint restart never cleared the black holes — `BlackHoleSystem.clear()` had no call site, so an open vortex survived into the restarted session and discharged into the objects it had just spawned (`clearSession` now calls it); (2) a boss slot inherited the pull field of the boss that vacated it — `enter()` and the session `clear()` now reset `pullRadius` / `pullStrength` / `pullTicks`, so a `boss.walker` reusing a suction boss's slot cannot suck the ships in. No golden hash moved for either. **Nothing needed hardware, the Tizen CLI or signing.** Docs: [`docs/dev/visual-and-mechanic-extras.md`](docs/dev/visual-and-mechanic-extras.md), [`docs/client/visual-and-mechanic-extras.md`](docs/client/visual-and-mechanic-extras.md), the API reference, the touched dev guides and content READMEs, the module docblocks of every touched module, the package and root READMEs and CHANGELOG `[Unreleased]`. |
| M3-02b | Remote & hardware tuning from the input-probe results | done | 2 | 126 new (7,958 in the suite; 492 of them the input probe's own) | aee4e05, 9240a24, 4bd2c9f (+ this row fix) | Applies the 2026-09-15 input-probe run on both M7 monitors ([results](docs/dev/input-probe-results.md)) to the game. **Profiles:** `tizen-remote-safe` is labelled `REMOTE`, debounces **0** ticks (the remote sends no fake key-up/key-down pairs at all), carries the new `singleKey: true` knob and registers `Guide` (458) and `Extra` (10253) as well; `tizen-remote-diagonal` was removed from the content and `core/config` gained `RETIRED_INPUT_PROFILE_IDS` / `migrateInputProfileId`, applied by `resolveUserOptions` to `options.input.profileId` — **a saved binding override keyed by the retired id is deliberately left in place** (inert, and migrating it would cost bundle bytes). `keyboard-remote-emulation` went to `singleKey` + debounce 0 too and dropped its `lastWins` diagonal / SOCD policies (the hardware never delivers the second key, so `lastWins` was unreachable). **`singleKey`** lives in `InputTuning` (`@shmup/input-web` `remote`) and is honoured by the keyboard source: while any tracked key is physically down, a `keydown` of an untracked key is dropped (a key inside its release-debounce window is already up and does not block); gamepad profiles must not set it. `remote` also exports the measured `REMOTE_REPEAT_DELAY_TICKS` (21) / `REMOTE_REPEAT_INTERVAL_TICKS` (6.5). **Vsync lock** (`core/loop`): the threshold turned out to be **2 whole steps of covered time** (delta + the debt carried from earlier frames), not the 1.5–1.75 steps of raw delta the plan sketched — the M7's jitter reaches a p95 of ~30 ms (1.8 steps) while the short deltas beside it keep the average at 60 Hz, so a raw-delta rule double-ticks on ~5 % of ordinary frames. The debt is bounded to ±1 step, `alpha` is 0 while locked, and the locked branch lives in its own helper so `advance` stays inlinable (a bigger `advance` boxed its fractional argument and blew the M1-06 allocation budget — a new guard `test/loop/loop-vsync-alloc.test.ts` pins it). Shell option `framePacing: 'auto' | 'lock' | 'free'` drives `Game.setVsyncLock` from the refresh probe inside `VSYNC_LOCK_MIN_HZ … MAX_HZ` (55–65); `Game` suspends the lock while frame advance, slow motion or the game-speed assist feed the loop a slowed clock. **Presentation only — no replay, golden or demo hash moved because of it.** **Debug overlay** gained a sixth line: `TPF` (0 / 1 / 2 / 3+ ticks-per-frame counters) and an eight-bucket rAF-delta histogram (`RAF_BUCKET_EDGES_MS`), plus a `LOCK` alert; the device line moved to line 7 and both counters are on `window.__shmupDebug.stats`. **Lifecycle:** `apps/web` `createVisibilityLifecycle` and the Tizen platform's lifecycle take an optional `focus` source (`window`) and are now **edge-triggered** — suspended while hidden **or** unfocused, resumed only when both are back; a repeat of the same state fires nothing (the old "repeated events fire the callbacks again" contract is gone, and `apps/web`'s tests say so). Electron keeps the same web lifecycle, which is the documented policy. **INPUT TEST** exits on `INPUT_TEST_EXIT_PRESSES` (3) Pause presses inside `INPUT_TEST_EXIT_WINDOW_TICKS` (90) as well as on the 60-tick hold; the string became `PAUSE X3 OR HOLD TO EXIT`. No other held-key gesture or chord was left: the shell's debug unlock is four *taps*. **`event.repeat` is no longer trusted** by the shell's debug tools — they track held keys themselves (`keydown` / `keyup` / `blur`, keyed by `keyCode` **and** `code`) — and a repo-wide ESLint rule forbids `.timeStamp` in runtime sources. **Playtest:** `test/playtest/remote-strict.ts` holds the model (`createRemoteStrictModel`) and its checker (`createRemoteStrictCheck`); `fourWayBot()` flies under it by default (`{ remote: false }` = the old bot), `PlaytestBot.remoteStrict` opts a bot in and `PlaytestResult.remoteViolations` / `.remoteViolation` report it. **No zone content needed re-tuning** — every zone A–I, all 16 routes, the boss rush, the caravan zones and the practice runs clear under the stricter bot with zero violations, inside the M1-18 / M2 budgets and with the recovery rule intact. What it exposed was the bot's own play, and three bot changes fixed it (they move every golden): a PowerUp tap waits for a lane clear for `EQUIP_WINDOW_TICKS` (12) with `EQUIP_PATIENCE_TICKS` (150) as the deadline, a boss core's lane is preferred much more strongly (`BOSS_CORE_BONUS` 160, was 60), and **in god mode only** the bot lines up on the core's exact row (always doing it cost zones D/F/H their no-god-mode clears). **One golden expectation changed:** `gimmick-range-god` no longer breaks a destructible brick (the bot flies the high branch) — `destroyed === 0`; the brick coverage stays with `gimmick-range-weaver` and `gimmick-range-deaths`. **Two tests were fixed rather than re-blessed:** `zones-bc-direct` counted Direct-mode drops by pool slot, which the pool's swap-remove makes unreliable. **Probe:** `chooseEventTime` now always returns handler time (keeping `event.timeStamp` only for the dispatch-delay statistic), `FrameSummary` gained a raw `histogram` (`FRAME_BUCKET_EDGES_MS`, `frameBucket`), and the verdicts gained `NO — not delivered` for diagonals and OK-while-arrow, inferred from `maxSimultaneous === 1` after a long hold. The probe was **not** re-run on the monitors (optional). **Review round 1 (1 major, fixed in `9240a24`):** `migrateInputProfileId` read the retired-id table through `Object.prototype`, so `resolveUserOptions` could return a non-string `profileId` — guarded with `Object.prototype.hasOwnProperty.call` plus a `typeof` fallback; round 2 re-verified it against a runtime-poisoned `Object.prototype` and found nothing (0 findings). **Bugs found by the TEST agent:** (1) `render-pixi` `rafDeltaBucket(NaN)` returned the overflow (dropped-frame) bucket instead of the documented 0 — a `<` chain fails every comparison for `NaN`; fixed by negating to `!(ms >= edge)`; (2) `tools/input-probe` `frameBucket` had the same hole and the same fix; (3) both docstrings' `@example` put a 16.5 ms frame in bucket 3 (it is bucket 2). **Notes:** the `.timeStamp` ESLint rule does not reach `apps/web/src` (that block disables `no-restricted-syntax` wholesale for `import.meta`), so the test agent closed the gap with a source scan in `eslint-rules.test.ts` rather than changing the config; and under the vsync lock a display faster than 60 Hz runs one tick per frame, so at `VSYNC_LOCK_MAX_HZ` (65) the game would run ~8 % fast — real panels do not sit there and 58–63 Hz is absorbed by delta snapping, so it is pinned with an explanatory test rather than moved. **Left out:** the first-time "OK / Ch± need the arrow released" hint for remote players — the INPUT TEST shows it live and the player docs say it, while a per-save "seen" flag for a device-specific string would cost `APP_JS_GZIP_BUDGET` headroom better spent on M3-03's localization. Tizen bundle 383.9 KB of 512 KB. **Still manual (plan §8.4):** the overlay's TPF counters while flying on both monitors, Home during play, the INPUT TEST's Back ×3 exit, a 240 fps latency video, and optionally a re-run of the fixed probe. Docs: [`docs/client/controls.md`](docs/client/controls.md), [`docs/client/preview-build.md`](docs/client/preview-build.md) (TV checks 91–95), [`docs/client/debug-tools.md`](docs/client/debug-tools.md), [`docs/client/release-candidate.md`](docs/client/release-candidate.md), `shmup_feat.md` §3 / §4, [`docs/dev/input-profiles.md`](docs/dev/input-profiles.md), [`docs/dev/architecture.md`](docs/dev/architecture.md), [`docs/dev/options-rebinding-and-accessibility.md`](docs/dev/options-rebinding-and-accessibility.md), [`docs/dev/input-probe.md`](docs/dev/input-probe.md), [`docs/dev/saves-and-options.md`](docs/dev/saves-and-options.md), [`docs/dev/debug-and-replays.md`](docs/dev/debug-and-replays.md), [`docs/dev/zone-a-and-playtest.md`](docs/dev/zone-a-and-playtest.md), the API reference, plan §8.2 / §8.4 / §8.5, the package and root READMEs and CHANGELOG `[Unreleased]`. |
| M3-02c | Render profiling: on-device numbers and a render benchmark | done | 2 | 42 new (8,011 in the suite; 262 e2e) | fe1c7d8, ca85564, aee250c (+ this docs commit) | Instruments the renderer without changing anything it measures (the render review's **F10**: nothing in the repo measured `renderer.render()` at all). **Three deliverables.** (1) **The headless gate** `test/bench/render.perf.ts`: it builds `test/bench/render-harness/` with Vite and the repo's own `shmupContent()` / `shmupAssets()` plugins, serves it on an ephemeral port and drives it in Playwright's Chromium — the real renderer over the real simulation and the real atlas, one page per scenario (Pixi's `TexturePool` is a global that never releases a texture). (2) **The on-device readout**: the debug overlay's new `REB` / `RT` line. (3) **F8's A/B switch**. **As-built deviations** (all recorded under the step in [`shmup_plan.md`](shmup_plan.md)): the two overlay figures got **their own line** rather than the TPF / RAF one (line 5 is full to column 46), so `REB` / `RT` are the panel's **seventh** line (row 6, the index the plan's As-built and the code use) and the M2-17 device line moved down to the eighth — `PANEL_LINES` 6 → 7, one `DrawList` per colour, allocation-free (guarded); the pooled-target total is measured with a **hook** (`createRenderTargetMeter` wraps Pixi's global `TexturePool.createTexture` once and accumulates `w × h × 4`) because walking `_poolKeyHash` every frame would allocate — the very thing the module forbids; the structure-rebuild count is a **renderer option** (`countStructureRebuilds`, alongside `countDrawCalls`), read from the scene render group's `structureDidChange` **before pass 1** because Pixi clears it while rendering, and `PixiRenderer.structureRebuilds` is -1 when not counting; the bench drives a **purpose-built page, not the game app** (the plan said "the built web bundle", but the bench has to choose the internal frame size, the stage and the exact scene load, and the shipped shell exposes none of them — so no shipped code grew a bench-only knob); the **resolution parameter is compared on the layer-effect scenario**, where 384×216 → 768×432 takes the pooled filter target 512 KB → 2,048 KB and the frame render texture 324 KB → 1,296 KB (the scene's *content* is still 384×216 of world, so the knob measures fill and render-target cost, not content density); **budgets** are `DRAW_CALL_BUDGET = 20` and the heap delta as the sharp gates with `RENDER_P95_BUDGET_MS = 16` deliberately loose; and **F8's dev switch is two switches** because the TV has no query string — `?gl=2` (`webGLVersionFromSearch`, new in `@shmup/shell` `boot`) on the web and `localStorage['shmup-cup:gl']` (`WEBGL_VERSION_KEY`) on Tizen, read only by a build that has the debug tools, so the release bundle never looks at it and `APP_JS_GZIP_BUDGET` is untouched. **WebGL1 stays the shipped default everywhere.** **Measured baselines** (the before/after gate for M3-02d / M3-02e, recorded in [`docs/dev/input-probe-results.md`](docs/dev/input-probe-results.md) §11.3): load 512/512 enemy bullets, ≥ 489/512 point items and ≥ 489/512 particles on **every** measured frame; **655–659 of 660 frames rebuild the scene's whole instruction set** (F1's mechanism confirmed against a real browser, not just Pixi's source); 4 draw calls with CRT off, 5 with CRT light *and* full, 7 with layer effects / Mode-7 / 768×432; pooled render targets 0 KB (CRT off), 2,048 KB (CRT light **and** full — F2: `light` costs what `full` costs and pools the same target), 512 KB (a 384×216 filter pass, pooled as 512×256 — F3), 2,048 KB + a 1,296 KB frame target at 768×432; p95 2.4–3.2 ms and heap 325–485 KB over 600 frames; the leak fixture retains 19.6 MB and correctly fails the 1 MB heap gate. **Those milliseconds are SwiftShader's and are not predictive of the Mali-G51** — what transfers to the TV is the counted quantities and the scenario-to-scenario comparisons, which is stated wherever the numbers are printed. **Review round 1 (1 blocker, 1 major; both fixed in `ca85564`):** (1) *blocker* — CI's `build · benchmark` job ran `pnpm bench` without ever installing Playwright's Chromium, so the render bench failed in CI while passing locally off a stale `~/.cache/ms-playwright` (`pnpm install` downloads no browser: Playwright ships no postinstall script and the repo sets no `pnpm.onlyBuiltDependencies`); the job now runs `pnpm exec playwright install --with-deps chromium` between the build and the bench, byte for byte the e2e jobs' step, and `timeout-minutes` went 15 → 25 (CI run 35067863246 green). (2) *major* — the scenarios were documented as carrying 512 point items, but the bomber's screen clear only re-ran below half a pool, so the item pool sawtoothed 512 → 256 and the measured frames held ~278–359, asserted at a toothless `> 200`. Fixed by making the **load** real rather than softening the prose — and fixing it exposed that `cancelAll` only *marks* a bullet slot dead while `pools.flushAll()` frees it in the step's removal phase, so topping bullets up before `step()` spawned nothing: the bench would have measured an **empty scene while printing confident numbers**. The tick is now `cancelAll` → `step()` → `fillBullets` → `fillParticles`, the clear re-runs on every tick that freed an item slot, and the harness reports the **minimum** live count over the measured frames rather than the last frame's reading. Rebuild baselines moved 659/660 → 655/660 on the three filtered / Mode-7 rows under the heavier load and every recorded figure was corrected. Round 2 verified both fixes independently (replayed the tick order against `packages/core/dist`, read the render frame's own batches — 512 sprites on the enemy-bullet layer, 503 on items — and reproduced the defect with the old order): **0 findings**. **Bug found by the TEST agent:** `createRenderTargetMeter` — a meter stopped while a *later* meter wrapped it went on accumulating bytes after `stop()`, contradicting its own docstring and the debug tools' `destroy()` contract; fixed with a `stopped` flag in `packages/render-pixi/src/debug/index.ts` (it must never steal the later meter's hook, so out of order it stays in the chain as a counting-free pass-through). The tests also moved the bench's brain out of the browser page into the DOM-free `render-harness/load.ts` / `gates.ts` / `protocol.ts`, which `test/integration/render-bench.test.ts` drives in Node as part of `pnpm test` — the wrong tick orders now fail loudly with the gate's own message. **Deliberately not done** (it belongs to M3-02d / M3-02e — an instrument must not fix what it measures): no CRT rewrite, no render groups, no boot warm-up frame, no `estimateMemory` correction. Tizen `app.js` 384.1 KB gzip of its 512 KB budget. **Still manual (plan §8.4):** the owner's §4 measurement table M1–M8 on the monitors — recipe in [`docs/dev/rendering-and-shell.md`](docs/dev/rendering-and-shell.md#measuring-on-the-tv), numbers land in [`docs/dev/input-probe-results.md`](docs/dev/input-probe-results.md) §11; **nothing is blocked on it**. Docs: [`docs/client/debug-tools.md`](docs/client/debug-tools.md) (the `REB` / `RT` line and the render-profiling checks), [`docs/client/preview-build.md`](docs/client/preview-build.md), [`docs/dev/rendering-and-shell.md`](docs/dev/rendering-and-shell.md), [`docs/dev/debug-and-replays.md`](docs/dev/debug-and-replays.md), [`docs/dev/build-test-deploy.md`](docs/dev/build-test-deploy.md), [`docs/dev/repo-layout.md`](docs/dev/repo-layout.md), [`docs/dev/input-probe-results.md`](docs/dev/input-probe-results.md) §11, the API reference, `test/README.md`, the package and root READMEs and CHANGELOG `[Unreleased]`. |
| M3-02d | Fold the full-screen effects into their draw passes | pending | — | — | — | Added 2026-09-16 (findings F2, F3, F4, F5, F6): CRT as a pass-2 mesh instead of a filter (a 2048² 16.8 MB pooled target + a second full-screen pass today), Mode-7 likewise, the memory estimator corrected, a boot warm-up frame for shader compiles and Pixi's first-draw allocation. |
| M3-02e | Cut the per-frame scene-graph rebuild | pending | — | — | — | Added 2026-09-16 (findings F1, F9): `visible` toggles make Pixi rebuild the whole ~6,400-object instruction set every frame. Render groups first, degenerate-quad parking next, `ParticleContainer` only as a last resort. Sized by M3-02c's bench. |
| M3-02f | Render telemetry: guided capture streamed to a log server | pending | — | — | — | Added 2026-09-16 at the owner's request: the input probe streamed its results to a Node log server, and the render profile should too — an on-screen checklist walks the M1–M8 table, samples stream as distributions (p95, not a glanced reading), and an analyzer emits the §11 tables. Reuses `tools/input-probe/server/log-server.mjs`, `reporter.ts`/`report.ts` (XHR + `text/plain` for Chromium 69) and `checklist.ts`. Best run before M3-02e, which its numbers size. |
| M3-03 | Reach: localization, more platforms, tracker music | pending | — | — | — | — |
