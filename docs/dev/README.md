# Developer documentation

| Page | Contents |
|---|---|
| [`repo-layout.md`](repo-layout.md) | Annotated tree of the monorepo: what goes where, tooling decisions, package resolution |
| [`architecture.md`](architecture.md) | Layers and dependency direction, the sim/presentation split, one frame end to end (rAF → fixed-step loop → input poll → `stepWorld` → two-pass render), the World in brief, input / rendering / audio pipelines, lifecycle (suspend, resume, Back), the `Platform` contract per host, determinism rules, module status tracking, extension points |
| [`engine-foundations.md`](engine-foundations.md) | The deterministic primitives (`core/rng`, `math`, `events`, `pools`): why they exist, how to use them correctly, regenerating the trig tables, testing, gotchas |
| [`content-data.md`](content-data.md) | Game data end to end: `content/` files, kinds and `formatVersion`, the `shmupContent()` Vite plugin and `virtual:shmup-content`, `loadContent()` passes, issue paths, the `<field>Id` resolution convention, the `ContentDb`, the `s` schema combinators, adding fields / kinds / migrations, `pnpm content:check`, tests, gotchas |
| [`asset-pipeline.md`](asset-pipeline.md) | Placeholder art end to end: `*.sprite.json` pixel maps, procedural generators, real-art PNG / Aseprite overrides by name, the pixel font, `@flash` hit silhouettes, the MaxRects packer, the atlas manifest format, `pnpm assets` and its input-hash cache, the `shmupAssets()` Vite plugin and `virtual:shmup-assets`, the Tizen bundle rule, extending it, tests, gotchas |
| [`rendering-and-shell.md`](rendering-and-shell.md) | The render contract (`RenderFrame`, `WorldView`, sprite batches, `DrawList`, `LayerId`), how `@shmup/render-pixi` draws it without per-frame allocation (atlas frame ids, bindings, quad pools, bitmap text, the two passes), the shared browser boot `@shmup/shell` (boot sequence, boot error screen, content owners, event dispatch, frame loop, dev scenes incl. the fx gallery), the thin apps, `pnpm test:e2e`, extending it, gotchas |
| [`sim-world.md`](sim-world.md) | The simulation World end to end: `createWorld` / `stepWorld`, the fixed 9-phase tick pipeline (plan §3.2) and hit-stop (exact frozen ticks), the camera and its scroll velocity, the SoA pool registry, the `WorldView` mirror batch, the KESTREL (states and the life cycle, fly-in, speed levels, diagonals, clamp, banking), the collision toolkit (closed scalar shape tests, layer masks, the counting-sort grid whose queries equal brute force), the `hashWorld` state hash, the free-flight scene, the allocation guard (`measureHeapGrowth`) and the V8 number-boxing pitfalls it found, extending it, tests, gotchas |
| [`stage-runtime.md`](stage-runtime.md) | Scrolling stages end to end: what `loadContent` makes of stage and tileset files (order / range checks, flag numbering, the deterministic `heightfield` generator, RLE rows, the third terrain pass), the stage runner (the five-step tick, camera keys with ramps / pans / exact-stop locks, the event cursor and `StageHooks`, invisible checkpoints and `restartAt` re-derivation, the hashed `Float64Array` state), pixel-exact half-open terrain queries, the parallax and terrain views, `GameConfig.stage` and `?stage=`, the `test-range` stage, extending it, tests, gotchas (key vs speed-event order, V8 hidden classes) |
| [`enemies-and-behaviors.md`](enemies-and-behaviors.md) | Enemies end to end: the `enemies` and `paths` data as loaded (defaults, script-id and tunable checks, centripetal Catmull-Rom paths baked into 1-px arc-length tables), the enemy system in the tick phases (64 fixed slots, spawning and camera frames, formations with follow tracks and ghost leaders, capsule drop and bonus, off-screen / settle / despawn rules, contact, damage, outcomes, hashing), the sleeping-coroutine script runner and `ScriptApi`, the eight movers, the M1 behaviour roster and writing a behaviour, the `test-range` roster, the hot-path rules the allocation guard found, extending it, tests, gotchas |
| [`bullets-and-patterns.md`](bullets-and-patterns.md) | Enemy bullets, lasers, attack patterns and rank end to end: `BULLET_KINDS` and the engine sprites (`ENGINE_SPRITES`, `loadContent`'s `extraSprites`), the bullet system and its two registered pools (the bullet pool *is* the `ENEMY_BULLETS` batch), spawning and aiming (`AIM_AT_TARGET`, `GameConfig.aimDirections`), the per-tick update (camera ride, delay, change, homing, acceleration, culling at view ± 16 px and on terrain), lasers (telegraph → grow → active → fade, attached / fixed, detach), bullets and lasers × ships, `cancelAllBullets`, the fire primitives and the `ScriptApi` fire rule, the roster's patterns, rank (`computeRank`, `rankScale` curves exactly 1 on Normal), the laser binding and beam art, determinism, the V8 findings of the allocation guards, extending it, tests, gotchas |
| [`weapons-and-options.md`](weapons-and-options.md) | Player weapons and Options end to end: the Type A arsenal as loaded (`WEAPON_BEHAVIOR_PARAMS` tunables, `checkWeaponBehaviors`, preset → four roles compiled into typed arrays), `Loadout` and `GameConfig.loadout` (`?loadout=full`), shooters, always-on autofire (`autofireInterval` / `missileInterval`, remote rule 1) and caps per shooter, the Double rule, the 96-slot shot pool and one shot tick (camera ride, laser head / length / wall, missile fall and floor slide, culling), grid hits (lowest slot vs piercing with pooled cooldown tables, armour clinks, kill credit), the screen-space Option trail (D26: bunching, spreading, fly-in resets), drawing shots and Options, hashing, the V8 findings of the allocation guards, extending it, tests, gotchas |
| [`powerups-and-shields.md`](powerups-and-shields.md) | The power meter, capsules, Force Field and Mega Crash end to end: `powerUpMode` / `autoPowerUp` / `autoPowerUpOrder` / `pickupMagnet`, the 7-slot meter (advance and wrap, equip on the `PowerUp` pressed edge, greyed slots, Double / Laser exclusivity, `equippable` for the HUD), Auto Power-Up's next wanted slot, the 32-slot item pool (drops → capsules, culling, the 16-px magnet, pickups and their outcomes), the Force Field on `PlayerShip.shield` inside `playerHit` (hits, i-frames, no terrain, break, wear frames), Mega Crash, the events, hashing and restarts, the hot-path rules, extending it, tests, gotchas |
| [`death-and-scoring.md`](death-and-scoring.md) | The life cycle, score and game-feel timers end to end: `deathPenalty` / `startingLives`, the death sequence in phase 7 (why `playerHit` only records, the tick's score and pickups first, events, hit-stop, shake, bullet cancel), dying / dead / respawn timings and the invulnerability blink, the `classic` / `arcade` / `casual` penalties (`loseOneLevel`, the checkpoint restart and `clearSession`), lives and game over, `core/scoring` (crediting kills, formation bonuses to the last killer, capsules, exactly once; the unhashed session hi-score; the flight HUD), `core/fx` (hit-stop counted only on frozen ticks, decaying shake, flash kinds), events, hashing, the hot-path rules, extending it, tests, gotchas |
| [`bosses-and-warning.md`](bosses-and-warning.md) | Bosses end to end: the `boss` section of the `enemies` kind as loaded (parts, weak points, cores, phases; the fields a boss omits and the loader fills; the reference pass that keeps bosses out of spawns), the boss system in the tick phases (one boss, 16 parts placed parent + offset, the shared hit path with `damagePart` and its clink rules, cascades, phases on HP / parts / time, own motion, contact), the WARNING (status, the stage brake to a lock, siren / dim / flash / music events, the D10 text, `WarningView` and the flight scene's band), the death sequence timeline (cancel, chain, blast with hit-stop, tally, `stageClear`), the boss roster and `BossScriptApi`, the test boss and `?stage=test-boss`, events, hashing, the hot-path rules, extending it, tests, gotchas |
| [`fx-and-game-feel.md`](fx-and-game-feel.md) | Game feel end to end: which sim event draws what (`connectFxEvents`), the particle presets of `content/fx/` (format, triggers on FX and SFX cues, validation and ownership, the shipped presets), the 256-particle pool (world space, simulated ticks, oldest recycled, the presentation RNG), the screen effects (the shake mirroring the sim, flash looks and the ≤ 3-a-second limiter, reduced flashing, the playfield dim), score popups and the new `Score` event, draw order, the renderer's frame, the shell's wiring, the `?scene=fx-gallery` dev scene, determinism, the hot-path rules the allocation guards found, extending it, tests, gotchas |
| [`input-profiles.md`](input-profiles.md) | Remote-first input end to end: the data-driven input profiles of `content/input/` (validation, compiled tables, the `0` placeholders), choosing and persisting the active profile, the `game` / `menu` binding contexts without phantom presses, the release debounce, diagonal and SOCD policies, Tizen key registration, dev overrides (`?profile=`, `?debounce=`), extending it, tests, gotchas |
| [`api-reference.md`](api-reference.md) | Every public export of `@shmup/core`, `@shmup/input-web`, `@shmup/audio-web`, `@shmup/render-pixi`, `@shmup/shell`, the apps' modules and the repo tooling, plus the declared types and planned functions of every placeholder module |
| [`build-test-deploy.md`](build-test-deploy.md) | Prerequisites (Node, pnpm 12), root and per-package scripts, Turborepo caching, build outputs, the Chromium 69 Tizen build contract, packaging/installing on the TV, Electron, tests (incl. the Playwright browser tests), CI, troubleshooting |
| [`conventions.md`](conventions.md) | Module layout and TSDoc rules, TypeScript settings, `@shmup/core` purity, Chromium 69 API rules, zero-allocation rules, tests, formatting, content rules, checklists for new modules / packages |
| [`input-probe.md`](input-probe.md) | The Tizen input-probe spike (`tools/input-probe/`): architecture (pure modules vs. DOM/Tizen glue), module map and public APIs, key-state model and verdict rules, Chromium 69 build contract, configuration, packaging/deploying from Windows, remote-logging payload & JSONL format, tests, extension points, gotchas |

Planned pages (filled in by later steps): testing strategy (golden replays), performance
budgets. Content *formats* for authors live next to the data, in
[`../../content/README.md`](../../content/README.md) and each content folder's README.

Monitor setup (Developer Mode, certificate with DUIDs) is shared by every Tizen build and
documented for testers in [`../client/install-on-tv.md`](../client/install-on-tv.md).
Every package and app also has its own `README.md` next to the code.
