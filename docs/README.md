# Shmup Cup documentation

| Folder | Audience | Contents |
|---|---|---|
| [`client/`](client/README.md) | Players, testers, the project owner | How to install and play, controls (Samsung remote / gamepad / keyboard), the run across the zone map (nine zones, five per run, the zone tally, choosing the next zone, the endings), the real zones B (BRINE NEBULA), C (DUNE EXPANSE), D (MAGMA DEEP — the dive into the caves, the brick maze), E (TEMPEST RIDGE — enemies from behind), F (CELL VAULT — chasing cells, regrowing tissue walls, grabbing tentacles) and G (PRISM LABYRINTH — crystal walls, the cube rush) and the final zones H (IRON CITADEL — moving floors, laser emitters, a parade of earlier bosses) and I (ABYSSAL THRONE — depth mines, a battleship raid with a boss inside) with their bosses, the ending scenes and the credits, and the hidden bonus stages (zone B's PEARL GROTTO, zone G's GLIMMER CACHE), two players at once (2 PLAYERS, joining with START, the split keyboard), the front end (the attract loop with a demo of each zone, the high-score tables and the story; typing your initials; practice; the sound test), the ship select (the KESTREL or the MANTA), the weapon select (weapon types A–D, Weapon Edit, the Option types, the `?` shields and `!` choices, Auto Power-Up and its order), the MANTA's colour items, weapons and Arm, the Option Hunter, the browser's test stages (the gimmick range of the later zones' stage mechanics, the raster range of the SNES-style picture effects and the four advanced-boss ranges — mid-bosses, a battleship raid, twin bosses, a boss rush — among them), the Options screen (volumes and, since M2-16, its CONTROLS page — control profile, autofire modes and rate, SOCD, the hiccup protection, rebinding every key and button, the input test —, DISPLAY page — colour-blind bullet colours, picture size, screen shake, flashes, the hitbox marker, the boss HP bar — and GAME page — difficulty, lives, penalty, Auto Power-Up, the magnet, one-button play), saved settings and high scores, the debug build's developer tools and the M1 release check (since M2-17 the device line, the save export and the game-mode build), the desktop app (M2-17: window keys, save files, installers), the title's EXTRA menu (M3-01: BOSS RUSH, CARAVAN, the looping ARCADE mode, replays with fast-forward and sharing, the Extra Edit weapons and unlocks, secret codes, the game-speed and invincibility assists, option recovery, rumble), troubleshooting on the TV |
| [`dev/`](dev/README.md) | Contributors (humans and agents) | Repository layout, architecture, build & deploy, conventions |

## All pages

| Page | Audience | Contents |
|---|---|---|
| [`client/preview-build.md`](client/preview-build.md) | Testers / owner | The current game build — version 1.0.0-rc.1, the v1.0 release candidate (the title screen with its menu — 1 PLAYER, 2 PLAYERS, since M2-15 PRACTICE and SOUND TEST —, since M2-15 the attract loop (a demo of each zone played by the computer, the high-score tables, the story), typing your initials after a high score with the arrows and OK, the high-score tables per difficulty, ship and mode, practising one zone from a checkpoint with its own tables, the sound test and the polished CONTINUE? countdown, two players at once (player 2 joining with START on a gamepad or the split keyboard, separate ships, scores and continues, the split HUD), the difficulty box, the SHIP SELECT box (the KESTREL or the MANTA) and the WEAPON SELECT screen — weapon types A–D, Weapon Edit, the Option types (TRAIL, SNAKE, FORMATION, ROTATE), the five `?` shields and the `!` choices, AUTO and the Auto Power-Up order, the live preview —, the Options screen — MASTER / MUSIC / SFX volume and, since M2-16, three pages: CONTROLS (the control profile, AUTOFIRE always / toggle / hold, RATE, SOCD, DEBOUNCE, rebinding keys and buttons, the input test), DISPLAY (BULLETS — colour-blind bullet colours with shape-coded centres —, SCALE, SHAKE, FLASHES, HITBOX, BOSS HP) and GAME (difficulty, LIVES, PENALTY, AUTO POWER, MAGNET, ONE BUTTON) —, settings and high scores kept between launches, the in-game HUD with the power meter, the pause menu, the game-over and stage-clear screens and quitting with the TV remote's Back; AZURE VERGE, the first real zone START plays on every device — its parts, enemies, capsules and the boss HALCYON BULWARK, `?skip=boss` in a browser; since M2-10 the run across the zone map — the zone title card, the ships flying out after the boss, the zone result tally (kill rate, time bonus), the ZONE MAP where you choose the next zone, the ending after zone H or I, what carries over between zones — and the hidden bonus stages of the browser's bonus range (a gap, all ground targets, a score digit; 1UPs and 1,000-point capsules; locked out after a death); since M2-11 the real zones B, BRINE NEBULA (bubbles that split, fish inside bubbles, wavy water, the mid-boss SPUME HERALD, the mechanical fish GALVANIC MAW with its mouth weak point and homing rockets, the secret gap into the bonus stage PEARL GROTTO), and C, DUNE EXPANSE (sand worms bursting from the dunes, beetles on the ceiling, the spider SANDGRAVE WIDOW with its drones and silk lines), with their own music; since M2-12 the real zones D, MAGMA DEEP (erupting volcanoes, the dive from the surface into the caves, falling rocks, the brick maze, the lava river, the battleship CINDER BASTION with its turning shield arms), and E, TEMPEST RIDGE (the storm, kites and jets from behind, thunderheads, the seahorse SQUALL STEED sending out homing minis), with their own music; since M2-13 the real zones F, CELL VAULT (chasing cells, cells that divide when shot, tissue walls that grow back, grabbing tentacles, the squid MANTLE REGENT whose tentacles guard its eye), and G, PRISM LABYRINTH (crystal walls, the cube rush, the crystal core FACET MONARCH behind its crystals, the secret bonus stage GLIMMER CACHE behind a gallery of four turrets), with their own music; since M2-14 the final zones H, IRON CITADEL (hatches, a piston hall of moving floors and ceilings, laser emitters, a parade of four earlier bosses in reduced form, the finale IRON SOVEREIGN with its turning shield wheel and overdrive spiral), and I, ABYSSAL THRONE (depth mines, trench eels, the ABYSS ARK raid that escapes after 90 seconds, THE HOLLOW KING inside it), and the endings — a sprite scene per final zone, a dawn for a no-death run, the epilogue, the result card and the credits, with their own songs; steering the KESTREL with the remote, keyboard or gamepad while its gun fires on its own; the scrolling test stage with its enemies, their bullets and shooting them down, the fully powered ship with Options, laser and Force Field, power capsules and equipping them with OK, bullets cancelled by a destroyed boss or a Mega Crash turning into gold diamonds worth 10 points each, losing the ship — the freeze, the blinking comeback, what a loss costs, GAME OVER — and the score and `HI`, the boss range with its WARNING sign and the first boss, the MANTA with its colour items (red, green, blue, orange, yellow, the octagon), its nine-level guns, the Arm shield and the speed toggle, the hunter range with the Option Hunter and the blue capsule, the direct range with its pincer waves, the gimmick range with the stage mechanics of the later zones (bricks to shoot through, walls that grow back, falling rocks, splitting bubbles, a volcano, a suction pod, tentacles, the cube rush, moving blocks, a stop with a pan, a fork, a very fast stretch — M2-07), the raster range with the SNES-style picture effects (a waving, colour-cycling sea, a pseudo-3D checker floor, heat haze — M2-08), the advanced-boss ranges (M2-09: four mid-bosses on the scrolling screen, the battleship IRON LEVIATHAN the view flies around with a heart inside and a time limit, the EMBER AND FROST TWINS taking turns, a boss rush), smoother motion on 120 / 144 Hz monitors, the explosions, sparks, screen shake, flashes (the Mega Crash now brightens the picture) and score numbers, the sound effects and the music — the stage theme, the WARNING siren, the boss theme, the stage-clear jingle —, sprite showcase, calibration screen and effects gallery in a browser on request; since M2-17 the desktop app's EXIT, sound from the start and remembered window, the TV checks of the device line, the game-mode build and memory; since M3-01 the title's EXTRA entry and the GAME page's OPT RECOVERY / SPEED / INVINCIBLE and the CONTROLS page's RUMBLE — the EXTRA menu itself in `client/extra-modes-and-replays.md`): opening it on the TV, in a browser and in Electron, how the ship should respond, what to check on the monitors, the loading bar and the boot error screen, what a correct picture and sound look like, what to report, troubleshooting |
| [`client/bestiary.md`](client/bestiary.md) | Players / testers | **The field guide to the Iron Tide**: every enemy of every zone with its own art, hit points, score and behaviour, the nine zone bosses and the mid-bosses assembled part by part from their own data, the Option Hunter and the blue carrier, the stage hazards, and the bosses that only appear in the EXTRA menu (captains, the IRON LEVIATHAN raid, the twins, GRASPING BLOOM, IRON TALON, SHADOW STRIDER, TRIAL WARDEN). Pictures come from `pnpm docs:images` and `pnpm docs:screens` — never drawn by hand |
| [`client/arsenal.md`](client/arsenal.md) | Players / testers | **The field guide to your own ship**: the KESTREL's power meter and the MANTA's Direct mode side by side, the capsules and items, the seven meter slots, weapon types A–D and the EXTRA EDIT weapons one picture at a time, the four Option types, the five `?` shields and five `!` choices, and Direct mode's colour items, nine-level families, sub-weapon, Arm and black-hole bomb |
| [`client/release-candidate.md`](client/release-candidate.md) | Testers / owner | The v1.0 release candidate 1.0.0-rc.1 (M2-18): where the version shows (the TV's 1.0.0), what the full play-through of all 16 routes with both ships fixed for players (MANTA waves through armour, the straight small discs, GALVANIC MAW's wider jaws, SANDGRAVE WIDOW's one silk line at a time), the placeholder icon, installing it, what the automated release checks cover, the v1.0 checklist for both monitors (plan §8.5 / §8.6), the store-listing placeholders, troubleshooting |
| [`client/extra-modes-and-replays.md`](client/extra-modes-and-replays.md) | Players / testers | The title's **EXTRA** menu (M3-01): BOSS RUSH (every zone's boss in a row), CARAVAN (a zone against a three-minute clock, TIME UP, the time bonus), ARCADE (the run looping on — different waves, faster bullets, revenge bullets — and LOOP 2), what an ending unlocks (Extra Edit, LOOP 2), the seven Extra Edit weapons, the four secret codes, REPLAYS (the last game and three kept ones, PLAY at ×1 / ×2 / ×4, KEEP, SHARE in a browser, DELETE, OUT OF SYNC), the GAME page's OPT RECOVERY / SPEED / INVINCIBLE and the CONTROLS page's RUMBLE, assisted scores marked `*`, fewer points for farming, TV checks, troubleshooting |
| [`client/visual-and-mechanic-extras.md`](client/visual-and-mechanic-extras.md) | Players / testers | The visual & mechanic extras (M3-02): the DISPLAY page's **CRT** (OFF / LIGHT / FULL) and **ASPECT** (NORMAL / ULTRA-WIDE / CLASSIC 4:3, with dim side panels instead of black bars — the picture is never cropped), the new **EXTRAS** options page (SLOWDOWN, GRAZE, DEATH BOMB, BLACK HOLE; they apply from the next game), the **black-hole bomb** for the MANTA (yellow items stock it, Special throws it, what the vortex does), the final zone's **escape sequence** (ESCAPE COMPLETE before the ending), the browser's showcase stage **HIGH-SPEED DIMENSION** (`?stage=dimension`) with SHADOW STRIDER, IRON TALON and GRASPING BLOOM, TV checks, troubleshooting |
| [`client/debug-tools.md`](client/debug-tools.md) | Testers / owner | Version 0.1.0 and the M1 release check: the debug build (`build:dev`) and its developer tools — opened with Play/Pause then Ch ▲ ×3 on the remote (F1–F8 in a browser) — the measuring panel and frame graph, hit-area outlines, invincibility, freeze and single steps, slow motion, jumps to the next checkpoint and the boss; since M2-17 the TV's device line (model, firmware), copying a save out for a bug report (`__shmupDebug.save`) and the extra §8.5 checks (memory, game mode); since M3-02b the frame-pacing line (`TPF`, the rAF histogram, the `LOCK` alert) and the §8.4 remote & hardware checks; since M3-02c the render-profile line (`REB`, `RT`) and the §8.4 render-profiling checks — with M3-02e's rule for reading `REB`: **hide the panel and take the figure from the capture**, because the panel's own numbers inflate it by a machine-dependent amount; since M3-02f the **guided render capture** (start the log server, build with `VITE_REPORT_URL`, play the on-screen M1–M8 checklist, run `analyze-render.mjs`, paste the tables — with reading the panel by hand kept as the fallback when no server is reachable); the on-device checklist for both monitors; troubleshooting |
| [`client/desktop-app.md`](client/desktop-app.md) | Players / testers / owner | The desktop app (M2-17 — Windows, macOS, Linux, Steam Deck): starting it, the window and its keys (F11 / Alt+Enter fullscreen, Ctrl + = / - / 0 window size, remembered), sound from the start, EXIT, where the save files live (with backups; the one-time fresh start after older desktop builds), making installers, troubleshooting |
| [`client/controls.md`](client/controls.md) | Players / testers | Default controls: Samsung Smart Remote, gamepad, keyboard — in the game and in menus (the zone map and the ending since M2-10, the credits since M2-14, the attract loop, the name entry, the hi-score tables, the practice select and the sound test since M2-15); two players (who controls which ship, joining with START, the SPLIT KEYBOARD profile) (Special — Ch ▲ — or a held PowerUp spreads FORMATION / ROTATE Options; Speed — Ch ▼ — switches the MANTA's speed); the developer keys of debug builds (moving through the title, pause, weapon select and Options menus, Back and Pause, quitting on the TV); control profiles (REMOTE on the TV — the one profile since M3-02b —, keyboard-as-remote for desktop testers) and choosing one under OPTIONS → CONTROLS, the CONTROLS page (autofire always / toggle / hold, rate, SOCD, the hiccup protection), rebinding keys and buttons per device (M2-16), the input test (M3-02b: three Pause presses leave it), one-button play, the desktop app's window keys (M2-17), trying them in a browser, troubleshooting; since M3-02b what the measured remote really does — one key at a time, OK never stops a direction, no diagonals, no held Back, Home pauses |
| [`client/install-on-tv.md`](client/install-on-tv.md) | Testers / owner | Developer Mode + certificate setup on the Smart Monitor M7, installing / starting / removing dev builds (input probe, game preview and its debug build), what happens to saved settings and high scores on update and uninstall, since M2-17 the game-mode build for the latency A/B test (and why the gamepad-check build must never ship) and live reload while developing (`tizen:watch`), install troubleshooting |
| [`client/outstanding-work.md`](client/outstanding-work.md) | Owner | **What is left, in one place** (M3-03, the plan's last step): every manual action that used to be spread over plan §8.2 and §8.4 … §8.9 — the on-monitor checks, the Seller Office, LG webOS, Steam and the Steam Deck, itch.io, a native speaker over the Spanish and Japanese, a pixel artist over the katakana, the latency video and the tracker-music CPU benchmark — grouped by what each one blocks, plus what "placeholder" means for the art, audio and translations |
| [`client/webos.md`](client/webos.md) | Owner / testers | **LG webOS TVs** (M3-03): what you need (a webOS 5+ set, the SDK, an LG developer account), Developer Mode, pairing with `ares-setup-device`, build → package → install → run, `ares-inspect`, and the on-device checklist — **nothing here has ever been run** |
| [`client/steam.md`](client/steam.md) | Owner | **Steam and the Steam Deck** (M3-03): what the desktop build already does (achievements derived from the save, Steam Cloud around the file store), the eleven achievement API names, the five things only the owner can do, and the Deck verification checklist — **nothing has touched Steam** |
| [`client/store-submission.md`](client/store-submission.md) | Owner | **Samsung TV Seller Office** (M3-03): the trade-dress review, the Tizen mandatory checklist, the account work, the store assets and their unverified sizes, the ≤ 50-DUID alpha test — **nothing has been submitted** |
| [`client/web-release.md`](client/web-release.md) | Owner | **The browser build on itch.io** (M3-03): `pnpm itch:package`, what the zip holds, the upload settings, what to check first — **nothing has been uploaded, and the packer has never run against a real build** |
| [`client/input-probe.md`](client/input-probe.md) | Testers / owner | Input Probe tester guide: screen, controls, 9-step test protocol, reading the verdicts, latency filming, recording results, troubleshooting |
| [`dev/repo-layout.md`](dev/repo-layout.md) | Contributors | Annotated monorepo tree, tooling decisions, package resolution, common commands |
| [`dev/architecture.md`](dev/architecture.md) | Contributors | Layers, sim/presentation split, one frame end to end, the World in brief, input/render/audio pipelines (M3-02b: the single-key remote, flagless repeats, the loop's vsync lock), lifecycle (edge-triggered over hidden ∨ unfocused since M3-02b), `Platform` per host, determinism rules, extension points |
| [`dev/engine-foundations.md`](dev/engine-foundations.md) | Contributors | The deterministic engine primitives (`core/rng`, `math`, `events`, `pools`): determinism and zero-allocation rules, usage recipes, regenerating the trig tables, tests, gotchas |
| [`dev/content-data.md`](dev/content-data.md) | Contributors | Game data end to end: content files and versions, the `virtual:shmup-content` Vite plugin, `loadContent()` and id resolution, the schema combinators, extending kinds and formats, `pnpm content:check`, gotchas |
| [`dev/asset-pipeline.md`](dev/asset-pipeline.md) | Contributors | Placeholder art end to end: pixel maps, procedural generators, real-art overrides, the pixel font, hit-flash sprites, the packer and atlas manifest, `pnpm assets`, the `virtual:shmup-assets` Vite plugin, extending it, gotchas |
| [`dev/rendering-and-shell.md`](dev/rendering-and-shell.md) | Contributors | The render contract (`RenderFrame`, sprite batches, draw lists, layers), the zero-allocation Pixi renderer (atlas, bindings, bitmap text), the shared browser boot `@shmup/shell` (boot error screen, content owners, event dispatch, frame loop, the scene flow's view and the dev scenes: free flight, showcase, calibration, fx gallery), `pnpm test:e2e`, measuring render performance (M3-02c: the `pnpm bench` render benchmark and its harness, what SwiftShader numbers do and do not transfer to the TV, the overlay's `REB` / `RT`, and the M1–M8 recipe for the monitors), M3-02e's render groups (one per high-churn layer, why `BG_FAR` / `BG_MID` / `DEBUG` are deliberately plain, the draw-call trade and how to read `REB` with the panel hidden), M3-02d's fold of the full-screen effects into their draw passes (the CRT as the pass-2 blit's own shader, the Mode-7 mesh, the boot warm-up, the `screenPass` escape hatch, the `OES_element_index_uint` dependency and what `destroy()` now frees), M3-02f's guided capture (the sampler that perturbs nothing, the pooled percentiles and why they are the bench's statistic but not its magnitude, the checklist, the log server and `analyze-render.mjs`), extending it, gotchas |
| [`dev/sim-world.md`](dev/sim-world.md) | Contributors | The simulation World: `createWorld` / `stepWorld` and the fixed 9-phase tick pipeline, hit-stop, the camera, the pool registry and view, the player ship (states and life cycle, fly-in, movement, clamp, banking), collision (shape tests, layers, the uniform grid), `hashWorld`, the free-flight scene, the allocation guard and V8 boxing pitfalls, extending it, tests, gotchas |
| [`dev/stage-runtime.md`](dev/stage-runtime.md) | Contributors | Scrolling stages: the stage and tileset formats as loaded (checks, flag ids, heightfield generator, RLE rows), the stage runner (tick order, camera keys / ramps / pans / locks, the event timeline and hooks, checkpoints and `restartAt`, hashed state), pixel-exact terrain queries, parallax and terrain views, `GameConfig.stage` / `?stage=`, `jumpTo`, the `test-range` and zone A stages, extending it, tests, gotchas |
| [`dev/enemies-and-behaviors.md`](dev/enemies-and-behaviors.md) | Contributors | Enemies: the `enemies` / `paths` data as loaded (spline paths baked into arc-length tables), the enemy system in the tick phases (spawning, formations, capsule + bonus, off-screen rules, contact, damage, hashing), sleeping behaviour coroutines and the `ScriptApi`, the eight movers, the M1 behaviour roster, the `test-range` roster, hot-path rules, extending it, tests, gotchas |
| [`dev/bullets-and-patterns.md`](dev/bullets-and-patterns.md) | Contributors | Enemy bullets and lasers: the built-in bullet kinds and engine sprites, the 512-slot SoA bullet pool (kinematics, delays, changes, homing, camera ride, culling), telegraphed lasers (phases, attached / fixed, capsule hitbox only at full width), collision with the ships, bullet cancel and cancel into point items (M2-02), bending lasers (M2-02), the `core/patterns` fire primitives and the `ScriptApi` fire rule, constant rank and `rankScale`, drawing bullets and lasers, hot-path rules, extending it, tests, gotchas |
| [`dev/weapons-and-options.md`](dev/weapons-and-options.md) | Contributors | Player weapons and Options: the Type A arsenal and its tunables, loadouts and `?loadout=full`, always-on autofire and caps per shooter, the Double rule, the shot pool (laser, ground-sliding missile), grid hits on enemies (piercing cooldowns, armour clinks, kill credit), the screen-space Option trail, drawing, hashing, hot-path rules, extending it, tests, gotchas |
| [`dev/powerups-and-shields.md`](dev/powerups-and-shields.md) | Contributors | The power meter and power-ups: the meter's slots, equip on the `PowerUp` edge and greyed slots, Auto Power-Up, capsules (drops, magnet, pickups), the Force Field inside `playerHit`, Mega Crash, events, hashing, hot-path rules, extending it, tests, gotchas |
| [`dev/death-and-scoring.md`](dev/death-and-scoring.md) | Contributors | The life cycle and score: the death sequence (hit-stop, shake, bullet cancel, events), dying / dead / respawn and invulnerability, the three death penalties and the arcade checkpoint restart, lives and game over, `core/scoring` (crediting, formation bonus, hi-score, HUD), `core/fx` (hit-stop, shake, flash), hashing, hot-path rules, extending it, tests, gotchas |
| [`dev/bosses-and-warning.md`](dev/bosses-and-warning.md) | Contributors | Bosses and the WARNING (the M1-13 foundation; M2-09's additions in `dev/advanced-bosses.md`): the boss section of the enemies data, the boss system (parts, weak points and clinks, phases, motion, contact), the WARNING (camera brake, status, events, the game's own text, the flight scene's band), the death sequence, the boss behaviours, the test boss (`?stage=test-boss`), hashing, hot-path rules, extending it, tests, gotchas |
| [`dev/zone-a-and-playtest.md`](dev/zone-a-and-playtest.md) | Contributors | Zone A (AZURE VERGE) section by section, its roster, the boss HALCYON BULWARK and `boss.bulwark`, the 4-way design rules in `pnpm content:check`, the debug stage skip (`?skip=boss`), the default stage, the headless playtest harness and 4-way bot, the `remote-strict` model the bot flies under since M3-02b (and the bot / golden changes it caused), extending it, gotchas |
| [`dev/fx-and-game-feel.md`](dev/fx-and-game-feel.md) | Contributors | Game feel: which sim event draws what, the particle presets of `content/fx/` and their triggers, the 256-particle pool, screen shake / flash (with its limiter) / dim, score popups and the `Score` event, draw order, the shell's wiring, `?scene=fx-gallery`, hot-path rules, extending it, tests, gotchas |
| [`dev/scenes-and-ui.md`](dev/scenes-and-ui.md) | Contributors | Scenes, menus and the HUD: bare gameplay vs the scene flow (`GameOptions.scenes`), the scene stack (deferred transitions, hooks), the M1 flow (boot, title, game, pause, stage clear, game over, the YES / NO dialog), Back / Pause / resume and the Tizen exit confirmation, one World per game and one event queue, the composed frame and UI list, the canvas UI kit (list menu, slider, toggle, choice, confirm; auto-repeat, Confirm buffer and lock), the Options overlay, the change-detected HUD, the `ui/logo`, the shell's scene view, determinism, hot-path rules, extending it, tests, gotchas |
| [`dev/saves-and-options.md`](dev/saves-and-options.md) | Contributors | Saves and options: the versioned save document (key `save.v1`, format 2 since M2-16: options, hi-score tables, stats), loading and the corrupt-save fallback, migrations, retired input profile ids (M3-02b), sanitising, `SaveStore` (write only on change), hi-scores in the scene flow, `UserOptions` and the volume curve, the Options screen and the `Choice` widget, live `UserOption` events, selectable input profiles, the shell's boot order and boot timing, extending it, tests, gotchas |
| [`dev/audio.md`](dev/audio.md) | Contributors | Audio: which sim event plays what, the `content/audio/` SFX bank and music tracks, the deterministic synth and sample-exact loops, loading phases and the OGG path, the audio engine, the SFX voice manager, the music player, the shell's wiring, `pnpm audio:preview`, extending it, tests, gotchas |
| [`dev/input-profiles.md`](dev/input-profiles.md) | Contributors | Remote-first input: data-driven input profiles (`content/input/`, the split keyboard since M2-06), validation and compiled tables, profile choice (OPTIONS → CONTROLS, kept in the save), `game` / `menu` binding contexts, release debounce, diagonal / SOCD policies, the `singleKey` model and what the 2026-09-15 input probe changed (M3-02b), Tizen key registration, `?profile=` / `?debounce=`, extending it, gotchas |
| [`dev/debug-and-replays.md`](dev/debug-and-replays.md) | Contributors | Plan step M1-19: release vs dev / test builds (`__SHMUP_DEV__`, `__SHMUP_BUILD__`, `build:test`, `build:dev`), the debug switches and controls (god mode, outlines, frame advance, slow motion, checkpoint jump, stage skip), `Game.frame`'s debug timing, the shell's keys and the TV unlock sequence, `window.__shmupDebug`, the overlay (panel, frame graph, outlines, draw calls), replays (header, recorder, playback, desync report, file format, build lock), golden replays and `pnpm golden:update`, `pnpm bench`, the Tizen bundle budgets, the M1-19 e2e specs, version 0.1.0, extending it, tests, gotchas |
| [`dev/difficulty-and-rank.md`](dev/difficulty-and-rank.md) | Contributors | Plan step M2-01: the difficulty presets (the `rules` content kind, `resolveGameConfig` / `withDifficulty`, replay headers), the rank formula and when the World recomputes it, what the rank scales, per-enemy rank modifiers, revenge bullets, extends and the lives cap, continues (the World side, the score's last digit, the countdown scene), the difficulty menu under START and per-difficulty hi-scores, hashing and golden replays, extending it, tests, gotchas |
| [`dev/pattern-dsl.md`](dev/pattern-dsl.md) | Contributors | Plan step M2-02: the BulletML-inspired pattern DSL (`content/patterns/` format, expressions and their compiler, the pattern compiler — inlining, params as values, shared bullet programs, the program layout — and the zero-allocation `PatternVm` interpreter: enemy emitters stepped by `pattern.loop`, bullets' own programs), the bending lasers, bullets cancelled into point items, the colour-blind bullet palettes, hashing and golden replays, extending it, tests, gotchas |
| [`dev/meter-arsenal.md`](dev/meter-arsenal.md) | Contributors | Plan step M2-03: the meter arsenal — `GameConfig.weaponPreset` / `weaponEdit` / `megaChoice` / `shieldChoice` with `withArsenal` / `arsenalMatches` and replay headers, the Types A–D presets and weapon `name`s, `resolveArsenal` and `setArsenal`, the nine new behaviours (Spread Bomb blasts, 2-Way volleys, the Photon Torpedo, Tail Gun / Vertical / Free Way pairs, the Ripple's ring hitbox, Cyclone and Twin beams), the `!` / `?` choices (`MeterChoices`, LIFE OPTION, FULL BARRIER …), the HUD meter's weapon names, the weapon select (rows, flow, the Auto order editor) and its live preview World on the weapon range, assets, hashing and the golden re-bless, hot-path rules, extending it, tests, gotchas |
| [`dev/options-shields-hunter.md`](dev/options-shields-hunter.md) | Contributors | Plan step M2-04: the Option types (Snake, Formation, Rotate — `optionChoice`, spread / extend by holding PowerUp or pressing Special), the meter shields (front / Free / Rotate Shield pods wearing independently, Reduce's hurtbox steps, FULL BARRIER's refill), the Option Hunter (appears only with Options, steals the chain, freed by Mega Crash as drifting items), the blue capsule, the weapon select's OPTION row, hashing and goldens, hot-path rules, extending, tests, gotchas |
| [`dev/direct-mode.md`](dev/direct-mode.md) | Contributors | Plan step M2-05: Direct mode and the ship select — `GameConfig.shipId` / `powerUpMode: 'direct'` (`withShip`), the MANTA's content (ship `mode` / `startSpeedLevel`, shot `families`, the stage's `directItems` plan, the `powerup` drop), the six colour items and their effects, the 9-level families (direct roles, level volleys), the Arm tiers, the Speed toggle (Ch−), the Direct death penalty and power rank, the ship select and per-mode hi-scores, the HUD tier pips, the pincer carriers, golden replays, extending, tests, gotchas |
| [`dev/coop.md`](dev/coop.md) | Contributors | Plan step M2-06: two-player simultaneous co-op — `GameConfig.coop` / `coopExtra`, the drop-in join and per-player continues, the co-op drop scaling, player 2's palette swap, the co-op HUD and end screens, device routing by seats and the split keyboard, goldens, extending, tests, gotchas |
| [`dev/advanced-stages.md`](dev/advanced-stages.md) | Contributors | Plan step M2-07: destructible and regenerating tiles (`DestructibleTerrain`, the checkpoint rollback, the renderer's change log), moving blocks in the terrain queries, falling rocks (the `Ballistic` mover), in-stage branches and region triggers, holds / diagonal pans / high-speed sections, the six gimmick behaviours (bubbles, volcano, suction, tentacle, cube rush, rocks) and their script calls, `world.gimmicks`, hashing and goldens, the `gimmick-range` dev stage, the Tiled importer `pnpm content:tiled`, extending, tests, gotchas |
| [`dev/presentation-polish.md`](dev/presentation-polish.md) | Contributors | Plan step M2-08: raster effects (wavy water, heat haze, line-band floors) and palette cycling as stage data drawn by one GLSL ES 1.0 filter per layer, the additive Mega Crash flash, the display options (scale modes, screen shake, reduced flashing, hitbox markers — saved and applied live), render interpolation and the refresh probe, the `raster-range` dev stage, extending, tests, gotchas |
| [`dev/advanced-bosses.md`](dev/advanced-bosses.md) | Contributors | Plan step M2-09: four boss slots, turned parts (heading frames, circle hurtboxes), captains (mid-bosses), battleship raids with boss-relative camera segments (`StageRunner.follow`, the held timeline), double bosses (turns, enrage), the boss inside a boss, time limits and escapes (the `BossEscaped` ending flag), the boss HP bar (model, HUD, option), boss rushes (`bossRush` stages), data, behaviours, hashing and goldens, hot-path rules, extending, tests, gotchas |
| [`dev/campaign-and-bonus-stages.md`](dev/campaign-and-bonus-stages.md) | Contributors | Plan step M2-10: the zone map as data (`campaign` kind, graph checks, endings and run flags), campaign runs (the run state, players carried between zones, the launch card, the fly-out, the zone tally, the map screen, `PrepareStage`), hidden bonus stages (entrances, warp, lock-out, 1UPs and bonus capsules), the ending hook, practice plumbing, the stub zones (B–I then; D–I since M2-11, F–I since M2-12, H–I since M2-13), hashing and goldens, extending, tests, gotchas |
| [`dev/zones-b-and-c.md`](dev/zones-b-and-c.md) | Contributors | Plan step M2-11: zones B (BRINE NEBULA) and C (DUNE EXPANSE) section by section, their rosters, the mid-boss SPUME HERALD, the bosses GALVANIC MAW and SANDGRAVE WIDOW (parts, phases), zone B's hidden bonus stage PEARL GROTTO, the behaviours `rocket.homing` / `worm.burst` / `boss.maw` / `boss.widow` and `BossPart.restX` / `restY`, the procedural art and zone tilesets, stage-scoped songs, Direct-mode plans, the playtests and the shared recovery helper, content checks, goldens, the bundle budget, a recipe for the next zones, tests, gotchas |
| [`dev/zones-d-and-e.md`](dev/zones-d-and-e.md) | Contributors | Plan step M2-12: zones D (MAGMA DEEP) and E (TEMPEST RIDGE) section by section, the dive (a scroll stop with a pan into a 400-px-tall map), the destructible brick maze, rear attackers (`rear.swoop`, kites on rear-entry paths), heavy-weather parallax, CINDER BASTION's rotating shield arms as data (a hub part) and SQUALL STEED's chest and homing minis (`boss.bastion`, `boss.steed`), the procedural art and zone tilesets, songs, Direct-mode plans, balance found by the bot, content checks at the camera's height, goldens, the bundle budget, tests, gotchas |
| [`dev/zones-f-and-g.md`](dev/zones-f-and-g.md) | Contributors | Plan step M2-13: zones F (CELL VAULT) and G (PRISM LABYRINTH) section by section, the regenerating tissue walls, the grabbing tentacles, chasing and dividing cells (`cell.chase`), the crystal labyrinth, the seeded cube rush stacking onto crystal pillars, the prism gallery's `ground` entrance into the second hidden bonus stage GLIMMER CACHE (and the rule for ground enemies around a `ground` window), MANTLE REGENT and FACET MONARCH on the shared curling-arm rule (`boss.squid`, `boss.facet`), the procedural art and zone tilesets, songs, Direct-mode plans, balance found by the bot, content checks, goldens, the bundle budget, tests, gotchas |
| [`dev/zones-h-and-i.md`](dev/zones-h-and-i.md) | Contributors | Plan step M2-14: the final zones H (IRON CITADEL) and I (ABYSSAL THRONE) section by section, hatches, laser emitters (`emitter.laser`), the piston hall of moving floors and ceilings, the parade of four earlier bosses in reduced form, depth mines (`mine.burst`) and the proximity wake (`sleepUntilNear`), IRON SOVEREIGN's four-phase finale and the spiral stream (`boss.sovereign`, `BossScriptApi.spiral`), the ABYSS ARK raid and THE HOLLOW KING inside it (`boss.ark`, `boss.angler`), the endings (scenes, epilogues, selection and the escape rule), the ending and credits screens and their music, the procedural art, balance found by the bot, content checks, goldens, allocation guards with wake allowances, the bundle budget, tests, gotchas |
| [`dev/front-end-and-attract.md`](dev/front-end-and-attract.md) | Contributors | Plan step M2-15: the mode select (1 PLAYER, 2 PLAYERS, PRACTICE, OPTIONS, SOUND TEST, EXIT), the attract loop (demo play of bundled bot replays — the `replay` content kind, `createDemoPlayback`, locked like golden replays —, the hi-score tables, the story crawl), hi-score tables per difficulty × ship × mode, the 3-letter name entry with the four directions and OK, the practice select and its own tables, the sound test (`SoundTest` event, `AudioEngine.playTrack`), the continue countdown's polish, budgets, allocation guards, extending it, tests, gotchas |
| [`dev/options-rebinding-and-accessibility.md`](dev/options-rebinding-and-accessibility.md) | Contributors | Plan step M2-16: the Options screen's CONTROLS / DISPLAY / GAME pages, the autofire modes (always / toggle / hold) and rate, SOCD and the remote's debounce, the game options and the one-button preset folded into the next games' configs (a run keeps its own), rebinding per device and context (binding tokens in the save, the capture, conflict detection, reset, the rebind screen and its shell host side), the input test and its three-press exit (M3-02b), save v2 with its migration, the UI string table (`content/strings/`), budgets, determinism, extending it, tests, gotchas |
| [`dev/platform-polish.md`](dev/platform-polish.md) | Contributors | Plan step M2-17: Electron file saves (atomic write + backup, quota) over IPC with sender checks, the web build inside Electron (platform `'electron'`, EXIT, audio at boot), the remembered window and its shortcuts, packaging with electron-builder, the Tizen `config.xml` variants (game mode, gamepad check) and their validation, `device-info` and the overlay's device line, the shared `localStorage` adapter with quota checks, the debug save export / import, live reload to the TV (`tizen:watch`), the memory estimator and atlas unloading between zones, extending it, tests, gotchas |
| [`dev/release-hardening.md`](dev/release-hardening.md) | Contributors | Plan step M2-18: the v1.0 release gate — the 16 routes × both ships (zone durations, the 4-way rules on every tick, the ship's open column `columnGap`), the release audit (capsule / item budgets, recovery, every pattern at rank 2 and 16, every boss fight), what the checks found and fixed (`passArmour`, the parallel discs, GALVANIC MAW's gape, `boss.widow`'s whole-lane wait), cross-engine determinism in Chromium and Firefox (`@shmup/shell` `determinism`, `?determinism`), the per-zone benches and the 30-minute soak, boot-to-title and the Tizen self-checks, `pnpm store:assets`, the version `1.0.0-rc.1` / `config.xml` `1.0.0`, what stays manual, gotchas |
| [`dev/extra-modes-and-replays.md`](dev/extra-modes-and-replays.md) | Contributors | Plan step M3-01: the EXTRA menu and its modes (BOSS RUSH, CARAVAN's clock, ARCADE's loops — stage remixes, faster and revenge bullets), the Extra Edit weapons and unlocks, the secret codes, the score-milking cap, the assists and the assisted flag, rumble, whole-run replays (format, recorder, playback), the replay library sized to the storage adapter, the replay screens, sharing on the web, determinism and goldens, extending it, tests, gotchas |
| [`dev/real-assets.md`](dev/real-assets.md) | Contributors / artists | Plan step M3-03: handing **real art, music and translations** to the pipeline — Aseprite → `assets/source/sprites/` (a PNG overrides the pixel map of the same name; `@flash`, `@p2` and the colour-blind variants are derived), Furnace / OpenMPT → OGG with sample-exact loop points, sfxr → the SFX bank, the placeholder translations and the bitmap font, and the checklist a hand-off batch must pass |
| [`dev/api-reference.md`](dev/api-reference.md) | Contributors | Map of every public export (packages, apps, tooling) and the declared API of the placeholder modules |
| [`dev/visual-and-mechanic-extras.md`](dev/visual-and-mechanic-extras.md) | Contributors | Plan step M3-02: the Mode-7 floor filter and the dimension dev stage, the CRT / scanline pass capped at 1080p, the aspect modes and their side panels, the deterministic authentic slowdown, graze scoring, the black-hole bomb (`core/blackhole`) and the death-bomb window, boss pull fields and the three P2 bosses, the final zone's escape sequence, the EXTRAS options page, hashing and the header-only golden re-bless, budgets, tests, gotchas |
| [`dev/build-test-deploy.md`](dev/build-test-deploy.md) | Contributors | Prerequisites, scripts (incl. `build:test` / `build:dev`, `golden:update`, `bench`), Turborepo, build outputs, Chromium 69 Tizen build contract and size budgets, TV packaging/installing (the `config.xml` variants, live reload, the remote Web Inspector), Electron (file saves, window keys, packaging), tests (incl. `pnpm test:e2e`, golden replays, the benchmarks — the simulation ones and, since M3-02c, the browser-driven render bench), CI, troubleshooting |
| [`dev/conventions.md`](dev/conventions.md) | Contributors | Module layout, TSDoc, TypeScript, core purity, the TV engine floor (Chromium **68** on webOS 5 vs 69 on Tizen — the API rules, and what the lint cannot see), zero allocation, tests, formatting, content rules, checklists |
| [`dev/input-probe.md`](dev/input-probe.md) | Contributors | Input probe architecture, modules & APIs, verdict rules (including M3-02b's `NO — not delivered` and the handler-clock fix), Chromium 69 build contract, configuration, packaging / deploying, remote-logging format (M3-02f: the same log server also receives the game's `rp-…` render-profile sessions), tests, extension points, gotchas |
| [`dev/input-probe-results.md`](dev/input-probe-results.md) | Contributors / owner | What the input probe measured on both M7 monitors (2026-09-15): the single-key remote, flagless repeats, release-only Back / Play/Pause, key codes, the DualShock 4, Home as a `blur`-only overlay, rAF jitter, the environment — the probe's timestamp bug, the raw logs and analyzer, what plan step M3-02b changes, and §11's render profile (M3-02c's tables to fill in on the hardware, with the headless bench's figures per step — before the fold, after M3-02d's, and after M3-02e's render groups took the scene rebuilds to 0; since M3-02f they are pasted from `analyze-render.mjs` rather than typed) |
| [`dev/render-performance-review.md`](dev/render-performance-review.md) | Contributors / owner | How the renderer behaves on the M7 (Pixi v8 as a renderer only, 384x216 upscaled, one atlas page): the per-frame scene-graph rebuild, what the CRT and Mode-7 filters really cost, the render-target memory the estimator misses, shader compiles and Pixi-internal allocation during gameplay, a verdict on each piece of third-party Tizen/Pixi advice, what still has to be measured on the monitors, and (§7) what a later 768×432 / 960×540 / 1080p internal resolution would change — plan steps M3-02c / M3-02d / M3-02e, whose updates at the top record every finding as fixed |

## Elsewhere in the repository

Design and research documents live at the repository root:

| File | Contents |
|---|---|
| [`../shmup_feat.md`](../shmup_feat.md) | Feature & functionality catalog (P0/P1/P2) |
| [`../shmup_tech.md`](../shmup_tech.md) | Platform constraints, library research, recommended stack |
| [`../input_probe_spec.md`](../input_probe_spec.md) | Spec of the Tizen input-probe spike (`tools/input-probe/`) |
| [`../shmup_plan.md`](../shmup_plan.md) | Implementation plan: decisions, milestones M1–M3, ordered build steps, manual on-device checklist |
| [`../CHANGELOG.md`](../CHANGELOG.md) | Release notes per version (0.1.0 = milestone M1) |
| [`../shmup_progress.md`](../shmup_progress.md) | Execution progress: one row per plan step (status, review rounds, tests, commits, as-built notes) |
| [`../content/README.md`](../content/README.md) | Content formats for authors (player ships, weapons, enemies, movement paths, stages, tilesets, input profiles, particle presets, sound effects and music, the campaign, the attract demos, the UI string tables — M2-16), one README per folder |
| [`../assets/README.md`](../assets/README.md) | Art sources and the placeholder pipeline for artists: pixel-map format, real-art overrides by name, art rules (original work, native resolution, VA-friendly palette) |

Per-project READMEs sit next to the code (e.g. [`../tools/input-probe/README.md`](../tools/input-probe/README.md),
[`../apps/tizen/README.md`](../apps/tizen/README.md)).

Contributors: start with [`dev/repo-layout.md`](dev/repo-layout.md) to find your way around the code, then
[`dev/architecture.md`](dev/architecture.md). Testers: start with [`client/preview-build.md`](client/preview-build.md); for the M1 on-device checks, [`client/debug-tools.md`](client/debug-tools.md).
